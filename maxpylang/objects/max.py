"""MaxObject stubs for max objects. Auto-generated by import_objs()."""

import os as _os
import sys as _sys
from maxpylang.maxobject import MaxObject

__all__ = [
    "abs_",
    "absolutepath",
    "accum",
    "acos",
    "acosh",
    "active",
    "anal",
    "append",
    "array",
    "array_change",
    "array_compare",
    "array_concat",
    "array_deserialize",
    "array_every",
    "array_expr",
    "array_fill",
    "array_filter",
    "array_flatten",
    "array_foreach",
    "array_frombuffer",
    "array_group",
    "array_index",
    "array_indexmap",
    "array_indexof",
    "array_insert",
    "array_iter",
    "array_join",
    "array_length",
    "array_map",
    "array_max",
    "array_mean",
    "array_median",
    "array_min",
    "array_mode",
    "array_pop",
    "array_push",
    "array_random",
    "array_reduce",
    "array_regexp",
    "array_remove",
    "array_replace",
    "array_reverse",
    "array_rotate",
    "array_routepass",
    "array_scramble",
    "array_sect",
    "array_shift",
    "array_slice",
    "array_some",
    "array_sort",
    "array_split",
    "array_stddev",
    "array_stream",
    "array_subarray",
    "array_thin",
    "array_tobuffer",
    "array_tolist",
    "array_tostring",
    "array_tosymbol",
    "array_tuplewise",
    "array_union",
    "array_unique",
    "array_unshift",
    "array_wrap",
    "asin",
    "asinh",
    "atan",
    "atan2",
    "atanh",
    "atodb",
    "atoi",
    "attrui",
    "autopattr",
    "bag",
    "bangbang",
    "bendin",
    "bendout",
    "bgcolor",
    "bitand",
    "bitor",
    "bline",
    "bondo",
    "borax",
    "bpatcher",
    "bucket",
    "buddy",
    "button",
    "capture",
    "cartopol",
    "change",
    "chooser",
    "clip",
    "clocker",
    "closebang",
    "coll",
    "coll_codebox",
    "colorpicker",
    "combine",
    "comment",
    "conformpath",
    "console",
    "cos",
    "cosh",
    "counter",
    "cpuclock",
    "crosspatch",
    "ctlin",
    "ctlout",
    "cycle",
    "date",
    "dbtoa",
    "decide",
    "decode",
    "defer",
    "deferlow",
    "delay",
    "detonate",
    "dial",
    "dialog",
    "dict_",
    "dict_codebox",
    "dict_compare",
    "dict_deserialize",
    "dict_group",
    "dict_iter",
    "dict_join",
    "dict_pack",
    "dict_print",
    "dict_route",
    "dict_serialize",
    "dict_slice",
    "dict_strip",
    "dict_unpack",
    "dict_view",
    "div",
    "dropfile",
    "drunk",
    "equals",
    "error",
    "expr",
    "filedate",
    "filein",
    "filepath",
    "filewatch",
    "float_",
    "flonum",
    "flush",
    "folder",
    "follow",
    "fontlist",
    "forward",
    "fpic",
    "freebang",
    "fromsymbol",
    "fswap",
    "ftom",
    "funbuff",
    "function",
    "funnel",
    "gamepad",
    "gate",
    "gestalt",
    "getattr_",
    "grab",
    "greaterthan",
    "greaterthaneq",
    "gswitch",
    "gswitch2",
    "hi",
    "hid",
    "hint",
    "histo",
    "hover",
    "if_",
    "imovie",
    "incdec",
    "inlet",
    "int_",
    "itable",
    "iter_",
    "itoa",
    "jit_cellblock",
    "join",
    "js",
    "jstrigger",
    "jsui",
    "jweb",
    "jweb_tilde",
    "key",
    "keyup",
    "kslider",
    "lcd",
    "led",
    "lessthan",
    "lessthaneq",
    "line",
    "linedrive",
    "listbox",
    "listfunnel",
    "loadbang",
    "loadmess",
    "logand",
    "logor",
    "makenote",
    "mappings",
    "match",
    "matrix",
    "matrixctrl",
    "maximum",
    "maxurl",
    "mc_function",
    "mc_getattr",
    "mc_line",
    "mean",
    "menubar",
    "message",
    "messageview",
    "metro",
    "midiflush",
    "midiformat",
    "midiin",
    "midiinfo",
    "midiout",
    "midiparse",
    "minimum",
    "minus",
    "modifiers",
    "modulo",
    "mousefilter",
    "mousestate",
    "movie",
    "mpeconfig",
    "mpeformat",
    "mpeparse",
    "mtof",
    "mtr",
    "multirange",
    "multislider",
    "mxj",
    "next_",
    "nodes",
    "notein",
    "noteout",
    "notequals",
    "nrpnin",
    "nrpnout",
    "nslider",
    "number",
    "numkey",
    "offer",
    "onebang",
    "onecopy",
    "opendialog",
    "osc_codebox",
    "osc_packet",
    "outlet",
    "pack",
    "pak",
    "panel",
    "param",
    "param_osc",
    "past",
    "patcher",
    "patcherargs",
    "pattr",
    "pattrforward",
    "pattrhub",
    "pattrmarker",
    "pattrstorage",
    "pcontrol",
    "peak",
    "pgmin",
    "pgmout",
    "pictctrl",
    "pictslider",
    "pipe",
    "playbar",
    "plus",
    "poltocar",
    "poly",
    "polyin",
    "polymidiin",
    "polyout",
    "pong",
    "pow_",
    "prepend",
    "preset",
    "print_",
    "prob",
    "project",
    "pv",
    "pvar",
    "qlim",
    "qlist",
    "qmetro",
    "quickthresh",
    "radiogroup",
    "random",
    "rdiv",
    "receive",
    "regexp",
    "relativepath",
    "repl",
    "rminus",
    "round_",
    "route",
    "routepass",
    "router",
    "rpnin",
    "rpnout",
    "rslider",
    "rtin",
    "savebang",
    "savedialog",
    "scale",
    "schedule",
    "screensize",
    "select",
    "send",
    "seq",
    "serial",
    "setclock",
    "shiftleft",
    "shiftright",
    "sin",
    "sinh",
    "slide",
    "slider",
    "speedlim",
    "spell",
    "split",
    "spray",
    "sprintf",
    "sqrt",
    "standalone",
    "stepcounter",
    "string",
    "string_append",
    "string_bytes",
    "string_change",
    "string_compare",
    "string_concat",
    "string_contains",
    "string_endswith",
    "string_frombytes",
    "string_fromsymlist",
    "string_fromutf8",
    "string_index",
    "string_indexof",
    "string_iter",
    "string_length",
    "string_prepend",
    "string_regexp",
    "string_remove",
    "string_replace",
    "string_replaceall",
    "string_reverse",
    "string_rotate",
    "string_slice",
    "string_split",
    "string_sprintf",
    "string_startswith",
    "string_substring",
    "string_toarray",
    "string_tolist",
    "string_tolower",
    "string_tosymbol",
    "string_toupper",
    "string_trim",
    "string_trimend",
    "string_trimstart",
    "string_utf8",
    "string_withpass",
    "stripnote",
    "strippath",
    "substitute",
    "suckah",
    "suspend",
    "sustain",
    "swap",
    "swatch",
    "switch",
    "sxformat",
    "sysexin",
    "tab",
    "tabcontroller",
    "table",
    "tan",
    "tanh",
    "tempo",
    "text",
    "text_codebox",
    "textbox",
    "textbutton",
    "textedit",
    "themecolor",
    "thisobject",
    "thispatcher",
    "threadcheck",
    "thresh",
    "timepoint",
    "timer",
    "times",
    "togedge",
    "toggle",
    "tosymbol",
    "touchin",
    "touchout",
    "translate",
    "transport",
    "trigger",
    "trough",
    "ubutton",
    "udpreceive",
    "udpsend",
    "umenu",
    "universal",
    "unjoin",
    "unpack",
    "urn",
    "uzi",
    "v8",
    "v8_codebox",
    "v8ui",
    "value",
    "vdp",
    "vexpr",
    "vstscan",
    "when",
    "xbendin",
    "xbendout",
    "xctlin",
    "xctlout",
    "xmidiin",
    "xnotein",
    "xnoteout",
    "zl",
    "zl_change",
    "zl_compare",
    "zl_delace",
    "zl_ecils",
    "zl_filter",
    "zl_group",
    "zl_indexmap",
    "zl_iter",
    "zl_join",
    "zl_lace",
    "zl_len",
    "zl_lookup",
    "zl_median",
    "zl_mth",
    "zl_nth",
    "zl_queue",
    "zl_reg",
    "zl_rev",
    "zl_rot",
    "zl_scramble",
    "zl_sect",
    "zl_slice",
    "zl_sort",
    "zl_stack",
    "zl_stream",
    "zl_sub",
    "zl_sum",
    "zl_swap",
    "zl_thin",
    "zl_union",
    "zl_unique",
    "zmap",
]

_NAMES = {
    "abs_": "abs",
    "absolutepath": "absolutepath",
    "accum": "accum",
    "acos": "acos",
    "acosh": "acosh",
    "active": "active",
    "anal": "anal",
    "append": "append",
    "array": "array",
    "array_change": "array.change",
    "array_compare": "array.compare",
    "array_concat": "array.concat",
    "array_deserialize": "array.deserialize",
    "array_every": "array.every",
    "array_expr": "array.expr",
    "array_fill": "array.fill",
    "array_filter": "array.filter",
    "array_flatten": "array.flatten",
    "array_foreach": "array.foreach",
    "array_frombuffer": "array.frombuffer",
    "array_group": "array.group",
    "array_index": "array.index",
    "array_indexmap": "array.indexmap",
    "array_indexof": "array.indexof",
    "array_insert": "array.insert",
    "array_iter": "array.iter",
    "array_join": "array.join",
    "array_length": "array.length",
    "array_map": "array.map",
    "array_max": "array.max",
    "array_mean": "array.mean",
    "array_median": "array.median",
    "array_min": "array.min",
    "array_mode": "array.mode",
    "array_pop": "array.pop",
    "array_push": "array.push",
    "array_random": "array.random",
    "array_reduce": "array.reduce",
    "array_regexp": "array.regexp",
    "array_remove": "array.remove",
    "array_replace": "array.replace",
    "array_reverse": "array.reverse",
    "array_rotate": "array.rotate",
    "array_routepass": "array.routepass",
    "array_scramble": "array.scramble",
    "array_sect": "array.sect",
    "array_shift": "array.shift",
    "array_slice": "array.slice",
    "array_some": "array.some",
    "array_sort": "array.sort",
    "array_split": "array.split",
    "array_stddev": "array.stddev",
    "array_stream": "array.stream",
    "array_subarray": "array.subarray",
    "array_thin": "array.thin",
    "array_tobuffer": "array.tobuffer",
    "array_tolist": "array.tolist",
    "array_tostring": "array.tostring",
    "array_tosymbol": "array.tosymbol",
    "array_tuplewise": "array.tuplewise",
    "array_union": "array.union",
    "array_unique": "array.unique",
    "array_unshift": "array.unshift",
    "array_wrap": "array.wrap",
    "asin": "asin",
    "asinh": "asinh",
    "atan": "atan",
    "atan2": "atan2",
    "atanh": "atanh",
    "atodb": "atodb",
    "atoi": "atoi",
    "attrui": "attrui",
    "autopattr": "autopattr",
    "bag": "bag",
    "bangbang": "bangbang",
    "bendin": "bendin",
    "bendout": "bendout",
    "bgcolor": "bgcolor",
    "bitand": "bitand",
    "bitor": "bitor",
    "bline": "bline",
    "bondo": "bondo",
    "borax": "borax",
    "bpatcher": "bpatcher",
    "bucket": "bucket",
    "buddy": "buddy",
    "button": "button",
    "capture": "capture",
    "cartopol": "cartopol",
    "change": "change",
    "chooser": "chooser",
    "clip": "clip",
    "clocker": "clocker",
    "closebang": "closebang",
    "coll": "coll",
    "coll_codebox": "coll.codebox",
    "colorpicker": "colorpicker",
    "combine": "combine",
    "comment": "comment",
    "conformpath": "conformpath",
    "console": "console",
    "cos": "cos",
    "cosh": "cosh",
    "counter": "counter",
    "cpuclock": "cpuclock",
    "crosspatch": "crosspatch",
    "ctlin": "ctlin",
    "ctlout": "ctlout",
    "cycle": "cycle",
    "date": "date",
    "dbtoa": "dbtoa",
    "decide": "decide",
    "decode": "decode",
    "defer": "defer",
    "deferlow": "deferlow",
    "delay": "delay",
    "detonate": "detonate",
    "dial": "dial",
    "dialog": "dialog",
    "dict_": "dict",
    "dict_codebox": "dict.codebox",
    "dict_compare": "dict.compare",
    "dict_deserialize": "dict.deserialize",
    "dict_group": "dict.group",
    "dict_iter": "dict.iter",
    "dict_join": "dict.join",
    "dict_pack": "dict.pack",
    "dict_print": "dict.print",
    "dict_route": "dict.route",
    "dict_serialize": "dict.serialize",
    "dict_slice": "dict.slice",
    "dict_strip": "dict.strip",
    "dict_unpack": "dict.unpack",
    "dict_view": "dict.view",
    "div": "div",
    "dropfile": "dropfile",
    "drunk": "drunk",
    "equals": "equals",
    "error": "error",
    "expr": "expr",
    "filedate": "filedate",
    "filein": "filein",
    "filepath": "filepath",
    "filewatch": "filewatch",
    "float_": "float",
    "flonum": "flonum",
    "flush": "flush",
    "folder": "folder",
    "follow": "follow",
    "fontlist": "fontlist",
    "forward": "forward",
    "fpic": "fpic",
    "freebang": "freebang",
    "fromsymbol": "fromsymbol",
    "fswap": "fswap",
    "ftom": "ftom",
    "funbuff": "funbuff",
    "function": "function",
    "funnel": "funnel",
    "gamepad": "gamepad",
    "gate": "gate",
    "gestalt": "gestalt",
    "getattr_": "getattr",
    "grab": "grab",
    "greaterthan": "greaterthan",
    "greaterthaneq": "greaterthaneq",
    "gswitch": "gswitch",
    "gswitch2": "gswitch2",
    "hi": "hi",
    "hid": "hid",
    "hint": "hint",
    "histo": "histo",
    "hover": "hover",
    "if_": "if",
    "imovie": "imovie",
    "incdec": "incdec",
    "inlet": "inlet",
    "int_": "int",
    "itable": "itable",
    "iter_": "iter",
    "itoa": "itoa",
    "jit_cellblock": "jit.cellblock",
    "join": "join",
    "js": "js",
    "jstrigger": "jstrigger",
    "jsui": "jsui",
    "jweb": "jweb",
    "jweb_tilde": "jweb~",
    "key": "key",
    "keyup": "keyup",
    "kslider": "kslider",
    "lcd": "lcd",
    "led": "led",
    "lessthan": "lessthan",
    "lessthaneq": "lessthaneq",
    "line": "line",
    "linedrive": "linedrive",
    "listbox": "listbox",
    "listfunnel": "listfunnel",
    "loadbang": "loadbang",
    "loadmess": "loadmess",
    "logand": "logand",
    "logor": "logor",
    "makenote": "makenote",
    "mappings": "mappings",
    "match": "match",
    "matrix": "matrix",
    "matrixctrl": "matrixctrl",
    "maximum": "maximum",
    "maxurl": "maxurl",
    "mc_function": "mc.function",
    "mc_getattr": "mc.getattr",
    "mc_line": "mc.line",
    "mean": "mean",
    "menubar": "menubar",
    "message": "message",
    "messageview": "messageview",
    "metro": "metro",
    "midiflush": "midiflush",
    "midiformat": "midiformat",
    "midiin": "midiin",
    "midiinfo": "midiinfo",
    "midiout": "midiout",
    "midiparse": "midiparse",
    "minimum": "minimum",
    "minus": "minus",
    "modifiers": "modifiers",
    "modulo": "modulo",
    "mousefilter": "mousefilter",
    "mousestate": "mousestate",
    "movie": "movie",
    "mpeconfig": "mpeconfig",
    "mpeformat": "mpeformat",
    "mpeparse": "mpeparse",
    "mtof": "mtof",
    "mtr": "mtr",
    "multirange": "multirange",
    "multislider": "multislider",
    "mxj": "mxj",
    "next_": "next",
    "nodes": "nodes",
    "notein": "notein",
    "noteout": "noteout",
    "notequals": "notequals",
    "nrpnin": "nrpnin",
    "nrpnout": "nrpnout",
    "nslider": "nslider",
    "number": "number",
    "numkey": "numkey",
    "offer": "offer",
    "onebang": "onebang",
    "onecopy": "onecopy",
    "opendialog": "opendialog",
    "osc_codebox": "osc.codebox",
    "osc_packet": "osc.packet",
    "outlet": "outlet",
    "pack": "pack",
    "pak": "pak",
    "panel": "panel",
    "param": "param",
    "param_osc": "param.osc",
    "past": "past",
    "patcher": "patcher",
    "patcherargs": "patcherargs",
    "pattr": "pattr",
    "pattrforward": "pattrforward",
    "pattrhub": "pattrhub",
    "pattrmarker": "pattrmarker",
    "pattrstorage": "pattrstorage",
    "pcontrol": "pcontrol",
    "peak": "peak",
    "pgmin": "pgmin",
    "pgmout": "pgmout",
    "pictctrl": "pictctrl",
    "pictslider": "pictslider",
    "pipe": "pipe",
    "playbar": "playbar",
    "plus": "plus",
    "poltocar": "poltocar",
    "poly": "poly",
    "polyin": "polyin",
    "polymidiin": "polymidiin",
    "polyout": "polyout",
    "pong": "pong",
    "pow_": "pow",
    "prepend": "prepend",
    "preset": "preset",
    "print_": "print",
    "prob": "prob",
    "project": "project",
    "pv": "pv",
    "pvar": "pvar",
    "qlim": "qlim",
    "qlist": "qlist",
    "qmetro": "qmetro",
    "quickthresh": "quickthresh",
    "radiogroup": "radiogroup",
    "random": "random",
    "rdiv": "rdiv",
    "receive": "receive",
    "regexp": "regexp",
    "relativepath": "relativepath",
    "repl": "repl",
    "rminus": "rminus",
    "round_": "round",
    "route": "route",
    "routepass": "routepass",
    "router": "router",
    "rpnin": "rpnin",
    "rpnout": "rpnout",
    "rslider": "rslider",
    "rtin": "rtin",
    "savebang": "savebang",
    "savedialog": "savedialog",
    "scale": "scale",
    "schedule": "schedule",
    "screensize": "screensize",
    "select": "select",
    "send": "send",
    "seq": "seq",
    "serial": "serial",
    "setclock": "setclock",
    "shiftleft": "shiftleft",
    "shiftright": "shiftright",
    "sin": "sin",
    "sinh": "sinh",
    "slide": "slide",
    "slider": "slider",
    "speedlim": "speedlim",
    "spell": "spell",
    "split": "split",
    "spray": "spray",
    "sprintf": "sprintf",
    "sqrt": "sqrt",
    "standalone": "standalone",
    "stepcounter": "stepcounter",
    "string": "string",
    "string_append": "string.append",
    "string_bytes": "string.bytes",
    "string_change": "string.change",
    "string_compare": "string.compare",
    "string_concat": "string.concat",
    "string_contains": "string.contains",
    "string_endswith": "string.endswith",
    "string_frombytes": "string.frombytes",
    "string_fromsymlist": "string.fromsymlist",
    "string_fromutf8": "string.fromutf8",
    "string_index": "string.index",
    "string_indexof": "string.indexof",
    "string_iter": "string.iter",
    "string_length": "string.length",
    "string_prepend": "string.prepend",
    "string_regexp": "string.regexp",
    "string_remove": "string.remove",
    "string_replace": "string.replace",
    "string_replaceall": "string.replaceall",
    "string_reverse": "string.reverse",
    "string_rotate": "string.rotate",
    "string_slice": "string.slice",
    "string_split": "string.split",
    "string_sprintf": "string.sprintf",
    "string_startswith": "string.startswith",
    "string_substring": "string.substring",
    "string_toarray": "string.toarray",
    "string_tolist": "string.tolist",
    "string_tolower": "string.tolower",
    "string_tosymbol": "string.tosymbol",
    "string_toupper": "string.toupper",
    "string_trim": "string.trim",
    "string_trimend": "string.trimend",
    "string_trimstart": "string.trimstart",
    "string_utf8": "string.utf8",
    "string_withpass": "string.withpass",
    "stripnote": "stripnote",
    "strippath": "strippath",
    "substitute": "substitute",
    "suckah": "suckah",
    "suspend": "suspend",
    "sustain": "sustain",
    "swap": "swap",
    "swatch": "swatch",
    "switch": "switch",
    "sxformat": "sxformat",
    "sysexin": "sysexin",
    "tab": "tab",
    "tabcontroller": "tabcontroller",
    "table": "table",
    "tan": "tan",
    "tanh": "tanh",
    "tempo": "tempo",
    "text": "text",
    "text_codebox": "text.codebox",
    "textbox": "textbox",
    "textbutton": "textbutton",
    "textedit": "textedit",
    "themecolor": "themecolor",
    "thisobject": "thisobject",
    "thispatcher": "thispatcher",
    "threadcheck": "threadcheck",
    "thresh": "thresh",
    "timepoint": "timepoint",
    "timer": "timer",
    "times": "times",
    "togedge": "togedge",
    "toggle": "toggle",
    "tosymbol": "tosymbol",
    "touchin": "touchin",
    "touchout": "touchout",
    "translate": "translate",
    "transport": "transport",
    "trigger": "trigger",
    "trough": "trough",
    "ubutton": "ubutton",
    "udpreceive": "udpreceive",
    "udpsend": "udpsend",
    "umenu": "umenu",
    "universal": "universal",
    "unjoin": "unjoin",
    "unpack": "unpack",
    "urn": "urn",
    "uzi": "uzi",
    "v8": "v8",
    "v8_codebox": "v8.codebox",
    "v8ui": "v8ui",
    "value": "value",
    "vdp": "vdp",
    "vexpr": "vexpr",
    "vstscan": "vstscan",
    "when": "when",
    "xbendin": "xbendin",
    "xbendout": "xbendout",
    "xctlin": "xctlin",
    "xctlout": "xctlout",
    "xmidiin": "xmidiin",
    "xnotein": "xnotein",
    "xnoteout": "xnoteout",
    "zl": "zl",
    "zl_change": "zl.change",
    "zl_compare": "zl.compare",
    "zl_delace": "zl.delace",
    "zl_ecils": "zl.ecils",
    "zl_filter": "zl.filter",
    "zl_group": "zl.group",
    "zl_indexmap": "zl.indexmap",
    "zl_iter": "zl.iter",
    "zl_join": "zl.join",
    "zl_lace": "zl.lace",
    "zl_len": "zl.len",
    "zl_lookup": "zl.lookup",
    "zl_median": "zl.median",
    "zl_mth": "zl.mth",
    "zl_nth": "zl.nth",
    "zl_queue": "zl.queue",
    "zl_reg": "zl.reg",
    "zl_rev": "zl.rev",
    "zl_rot": "zl.rot",
    "zl_scramble": "zl.scramble",
    "zl_sect": "zl.sect",
    "zl_slice": "zl.slice",
    "zl_sort": "zl.sort",
    "zl_stack": "zl.stack",
    "zl_stream": "zl.stream",
    "zl_sub": "zl.sub",
    "zl_sum": "zl.sum",
    "zl_swap": "zl.swap",
    "zl_thin": "zl.thin",
    "zl_union": "zl.union",
    "zl_unique": "zl.unique",
    "zmap": "zmap",
}

_devnull = open(_os.devnull, "w")
_old_stdout = _sys.stdout
_sys.stdout = _devnull


abs_ = MaxObject("abs")

"""
absolutepath - Convert a file name to an absolute path

Converts a file name or path to an absolute path. If the file is not found, the symbol notfound is output.

Inlets:
  0 (symbol): (symbol) file path

Outlets:
  0 (symbol): (symbol) resolved path

Messages: anything, types
"""
absolutepath = MaxObject("absolutepath")

"""
accum - Store, add to, and multiply a number

Stores a value (int or float), then adds or multiplies into it. If the argument is an integer, the multiplication is done in floating-point then converted to integer.

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): int Sets and Outputs Value, bang Outputs Value
  1 (INLET_TYPE): Add Input to Current Value
  2 (INLET_TYPE): Multiplies Current Value by Input

Outlets:
  0 (OUTLET_TYPE): Accumulator Output

Messages: bang, int, float, ft1, in1, in2, set
"""
accum = MaxObject("accum")

"""
acos - Arc-cosine function

Use the acos object to calculate and output the arc-cosine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Acos (x)

Messages: bang, int, float
"""
acos = MaxObject("acos")

"""
acosh - Hyperbolic arc-cosine function

Use the acosh object to calculate and output the hyperbolic arc-cosine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Acosh (x)

Messages: bang, int, float
"""
acosh = MaxObject("acosh")

"""
active - Send 1 when patcher window is active, 0 when inactive

active will output a 1 when the Patcher window becomes active (i.e., it is the front-most window and its title bar is highlighted), and output a 0 when the Patcher window becomes inactive. You can use this to change the user interface of your window. For example, messages from the key object can be turned off when the window is made inactive.

Messages: int
"""
active = MaxObject("active")

"""
anal - Make a histogram of number pairs

Reports how many times a number pair has been received.

Args:
  input-limit (int, optional)

Inlets:
  0 (INLET_TYPE): Incoming Number Stream

Outlets:
  0 (OUTLET_TYPE): Transitions Seen Between Current and Previous

Messages: int, clear, reset
"""
anal = MaxObject("anal")

"""
append - Append arguments to the end of a message

append will add arguments to the end of any message you input.

Args:
  appended-message (any, optional)

Inlets:
  0 (INLET_TYPE): Message to append to

Outlets:
  0 (OUTLET_TYPE): Resulting Appended Message

Messages: bang, int, float, list, anything, set
"""
append = MaxObject("append")

"""
array - Create or duplicate an array object

Create or duplicate a named array object.

Args:
  value (, optional)

Inlets:
  0 (INLET_TYPE): messages in
  1 (INLET_TYPE): set array value, messages in

Outlets:
  0 (OUTLET_TYPE): arrayobj out
  1 (OUTLET_TYPE): atoms out
  2 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, append, array, atoms, clear, delete, dictionary, get, insert, prepend, replace, reserve, shrink, string

Attributes: name, parameter_enable, parameter_mappable, size
"""
array = MaxObject("array")

"""
array.change - Detect array changes

Output an array if the order or value of the elements if different from the previous.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): 1 if different, 0 if identical
  1 (OUTLET_TYPE): 1 if different, 0 if equivalent

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: unordered
"""
array_change = MaxObject("array.change")

"""
array.compare - Compare two arrays for equality

Arrays are compared for their value and order.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): 1 if identical, 0 if different

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: unordered
"""
array_compare = MaxObject("array.compare")

"""
array.concat - Concatenate two array objects

The data in the array received in the right inlet will be appended to the data in the array received in the left inlet and a new array will be output. The original array objects are not modified.

Inlets:
  0 (INLET_TYPE): left array in will be concatenated with the right array
  1 (INLET_TYPE): set right array

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_concat = MaxObject("array.concat")

"""
array.deserialize - Parse a string, symbol or list to an array.

A string/symbol/list representing an array in JSON format (e.g. "[ 25, 50, 75, 100 ]") will be converted into an array object.

Inlets:
  0 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): deserializeped array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_deserialize = MaxObject("array.deserialize")

"""
array.every - Tests all elements in the array

Tests whether all elements in the array pass the provided test. While testing, return the value of the test to the right inlet.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): test response (0/1)

Outlets:
  0 (OUTLET_TYPE): every element of input array passes test (0/1)
  1 (OUTLET_TYPE): test output
  2 (OUTLET_TYPE): element index

Messages: bang, int, float, list, anything, array, cancel, dictionary, in1, string
"""
array_every = MaxObject("array.every")

"""
array.expr - Evaluate a math expression for an array

Performs mathematical calculations using C language-style mathematical operations. Operates on inputs that are arrays rather than collections of single values.

Args:
  expression (list, required)
  constant (number, required)
  format (symbol, required)
  table (symbol, required)
  (other) (symbol, required)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): array result out

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: fillmode
"""
array_expr = MaxObject("array.expr")

"""
array.fill - Generate an array of a specified length

Creates a new array object of a specified length, pre-filled with elements.

Args:
  length (int, required)
  initial-contents (list, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): new array length

Outlets:
  0 (OUTLET_TYPE): filled array

Messages: bang, int, float, list, anything, array, dictionary, in1, string

Attributes: mode, range, seed, urn
"""
array_fill = MaxObject("array.fill")

"""
array.filter - Output elements of an array matching a condition

If the condition set for the object is '0' the element is excluded in the filtered array output. In the case the condition is '1' it is included in the array.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): filter inclusion response (0/1)

Outlets:
  0 (OUTLET_TYPE): filtered array out
  1 (OUTLET_TYPE): filter inclusion output
  2 (OUTLET_TYPE): element index

Messages: bang, int, float, list, anything, array, cancel, dictionary, in1, string
"""
array_filter = MaxObject("array.filter")

"""
array.flatten - Flatten a multi-dimensional array object to a single dimension

Flattening can be simple sequential, or recursive sequential, depending on mode.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): flattened array out

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: mode
"""
array_flatten = MaxObject("array.flatten")

"""
array.foreach - Iterate the elements of an array

Iterate the elements of an array, with index and a done-iterating bang.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): bang when finished
  1 (OUTLET_TYPE): element value
  2 (OUTLET_TYPE): element index

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_foreach = MaxObject("array.foreach")

"""
array.frombuffer - Read audio buffer values into an array object

Determine the properties of a buffer that has been read into an array object.

Args:
  buffername (symbol, optional)

Inlets:
  0 (INLET_TYPE): array, messages in
  1 (INLET_TYPE): array, messages in

Outlets:
  0 (OUTLET_TYPE)
  1 (OUTLET_TYPE): channel count out

Messages: bang, list

Attributes: buffername, channelcount, channelstart, flatsinglechannel, framelength, frameoffset
"""
array_frombuffer = MaxObject("array.frombuffer")

"""
array.group - Output an array when it reaches a certain size

Outputs an array after the number of elements specified by the group size are received. Incoming arrays larger than the group size are split into multiple output arrays.

Args:
  initial-size (int, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): number of elements to group (currently 0)

Outlets:
  0 (OUTLET_TYPE): grouped array

Messages: bang, int, float, list, anything, array, clear, dictionary, in1, string
"""
array_group = MaxObject("array.group")

"""
array.index - Output the indexed element of an array object

Extract an element from an incoming array object, based on the index specified (0-based).

Args:
  index (int, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): position in array to extract

Outlets:
  0 (OUTLET_TYPE): indexed element of the array
  1 (OUTLET_TYPE): array with indexed element removed

Messages: bang, int, float, list, anything, array, dictionary, in1, string

Attributes: remainder
"""
array_index = MaxObject("array.index")

"""
array.indexmap - Reorder the elements of an array object based on an indexed map

Use an array of integers (an index map) to reorder the elements of another array.

Args:
  index-map (list, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): indices list

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_indexmap = MaxObject("array.indexmap")

"""
array.indexof - Search for the index of an array element

Output the position of an array element within a longer array as an integer index (0-based). Output -1 if the element cannot be found.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): index out

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: all, offset
"""
array_indexof = MaxObject("array.indexof")

"""
array.insert - Insert elements into an array object

Elements can be inserted at the beginning, the end, or in the middle, based on the index specified (0-based).

Args:
  index (int, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): position in array to extract
  2 (INLET_TYPE): position in array to insert

Outlets:
  0 (OUTLET_TYPE): inserted element of the array

Messages: bang, int, float, list, anything, array, dictionary, in2, string
"""
array_insert = MaxObject("array.insert")

"""
array.iter - Iterate every element of an array object

Outputs each element in an array object sequentially, as a series of messages.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): iterated array members

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_iter = MaxObject("array.iter")

"""
array.join - Convert an array object to a string object with an optional separator string

Join the elements of an array together to form a string. The optional separator string will be placed between each element.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): separator string in

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, array, clear, string
"""
array_join = MaxObject("array.join")

"""
array.length - Determine the length of an array object

Length is determined by sending a message to the left inlet.

Inlets:
  0 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): length out

Messages: bang, int, float, list, anything, array, clear, dictionary, string
"""
array_length = MaxObject("array.length")

"""
array.map - Perform an operation on every element of an array object, replacing elements in-place

Each element of an incoming array will be output sequentially. After processing the element, it should be passed back into the right inlet of the array.map object, replacing the element in the array. When iteration is complete, the substituted array will be output.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): map response

Outlets:
  0 (OUTLET_TYPE): mapped array out
  1 (OUTLET_TYPE): mapping entry output
  2 (OUTLET_TYPE): element index

Messages: bang, int, float, list, anything, array, cancel, dictionary, string
"""
array_map = MaxObject("array.map")

"""
array.max - Calculate the maximum of the numerical elements of an array

Non-numerical elements (symbols, arrays, strings, dictionaries) are ignored.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): result out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_max = MaxObject("array.max")

"""
array.mean - Calculate the mean of the numerical elements of an array

The mean is the total of all numerical values divided by the number of values (and is also known as the average). Non-numerical elements (symbols, arrays, strings, dictionaries) are ignored.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): result out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_mean = MaxObject("array.mean")

"""
array.median - Calculate the median of the numerical elements of an array

The median is the value halfway between the minimum and maximum of the set of numerical elements. Non-numerical elements (symbols, arrays, strings, dictionaries) are ignored.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): result out

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: evenmode
"""
array_median = MaxObject("array.median")

"""
array.min - Calculate the minimum of the numerical elements of an array

Non-numerical elements (symbols, arrays, strings, dictionaries) are ignored.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): result out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_min = MaxObject("array.min")

"""
array.mode - Calculate the mode of the numerical elements of an array

The mode is the element or elements which appear with the highest frequency. Non-numerical elements (symbols, arrays, strings, dictionaries) are ignored.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): result out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_mode = MaxObject("array.mode")

"""
array.pop - Remove an element from the end of an array

A bang sent to the left inlet will shift elements from the end of the output array (the remaining array from the right outlet and the popped element is sent via the middle outlet) until the array is empty, at which point a bang is sent from the left outlet. Unlike the JavaScript implementation, the input array is not changed in place.

Inlets:
  0 (INLET_TYPE): bang to pop off last element
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): array is empty bang
  1 (OUTLET_TYPE): popped element
  2 (OUTLET_TYPE): popped array out

Messages: bang, int, float, list, anything, array, clear, dictionary, string

Attributes: wrapmode
"""
array_pop = MaxObject("array.pop")

"""
array.push - Add one or more elements to the end of an array

The base array is sent to the right inlet, which clears any elements sent to the left inlet. Additional elements are sent to the left inlet, and can be repeatedly placed at the end of the output array, without the user needing to update the array in the right inlet. Unlike the JavaScript implementation, the input array is not changed in place.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): pushed array out

Messages: bang, int, float, list, anything, array, clear, dictionary, string

Attributes: wrapmode
"""
array_push = MaxObject("array.push")

"""
array.random - Generate a random array of a specified length

Creates a new array object of a specified length, pre-filled with random numbers.

Args:
  length (int, required)
  initial-range (list, optional)

Inlets:
  0 (INLET_TYPE): range in (array)
  1 (INLET_TYPE): new array length

Outlets:
  0 (OUTLET_TYPE): filled array

Messages: bang, int, float, list, anything, array, dictionary, in1, string

Attributes: mode, range, seed, urn
"""
array_random = MaxObject("array.random")

"""
array.reduce - Combine array elements based on a custom function

Perform an operation on every element of an array object, outputting the accumulated response.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): reduce response

Outlets:
  0 (OUTLET_TYPE): reduced value out
  1 (OUTLET_TYPE): accumulated value
  2 (OUTLET_TYPE): element value
  3 (OUTLET_TYPE): element index

Messages: bang, int, float, list, anything, array, cancel, dictionary, string

Attributes: initial
"""
array_reduce = MaxObject("array.reduce")

"""
array.regexp - Use regular expressions to process input

With array.regexp, it's possible to use PERL-compatible regular expressions (PCRE) to match or make substitutions within arrays.

Args:
  expression (symbol, optional)
  substitution (, optional)

Inlets:
  0 (INLET_TYPE): re, substitute, or text in
  1 (INLET_TYPE): re (regular expression) in
  2 (INLET_TYPE): substitute in

Outlets:
  0 (OUTLET_TYPE): substitutions
  1 (OUTLET_TYPE): capture groups
  2 (OUTLET_TYPE): subarrays
  3 (OUTLET_TYPE): unmatched
  4 (OUTLET_TYPE): subarray offsets

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: re, substitute
"""
array_regexp = MaxObject("array.regexp")

"""
array.remove - Remove a range of elements from an array object

Args:
  index-range (list, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): remove start index (0-based)
  2 (INLET_TYPE): remove end index (0-based)

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, in1, in2, string
"""
array_remove = MaxObject("array.remove")

"""
array.replace - Replace elements in an array

Inlets:
  0 (INLET_TYPE): replacement array
  1 (INLET_TYPE): base array
  2 (INLET_TYPE): replacement start index (0-based, -1 = bypass)

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, in2, string
"""
array_replace = MaxObject("array.replace")

"""
array.reverse - Reverse the order of elements in an array object

The output array will contain the elements of the incoming array, but backward.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): reversed array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_reverse = MaxObject("array.reverse")

"""
array.rotate - Rotate the elements in any array object

Move elements forward or backwards in the array. Values will wrap around.

Args:
  rotation amount (int, required)

Inlets:
  0 (INLET_TYPE): array to rotate
  1 (INLET_TYPE): positions to rotate (negative to rotate left)

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, in1, string
"""
array_rotate = MaxObject("array.rotate")

"""
array.routepass - Route a complete input array object based on input matching

Like routepass, but for array objects.

Args:
  match keys (list, required)

Inlets:
  0 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): array out if there are no matches

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: match
"""
array_routepass = MaxObject("array.routepass")

"""
array.scramble - Randomize the order of elements in an array object

When triggered, output the scrambled array and the reordered index list.

Inlets:
  0 (INLET_TYPE): array in (trigger output)
  1 (INLET_TYPE): set array

Outlets:
  0 (OUTLET_TYPE): scrambled array out
  1 (OUTLET_TYPE): reordered indexes list

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_scramble = MaxObject("array.scramble")

"""
array.sect - Return the elements of an array object which intersect with another array object

Intersection means that the elements are identical or equivalent (in the case of array, dictionary and string objects).

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_sect = MaxObject("array.sect")

"""
array.shift - Remove an element from the beginning of an array

Set the array to shift, then send bangs repeatedly to output the elements of the array one at a time. The remainder of the array is sent out the right outlet, and a bang is sent out the leftmost outlet when the array is empty.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): array is empty bang
  1 (OUTLET_TYPE): shifted element
  2 (OUTLET_TYPE): shifted array out

Messages: bang, int, float, list, anything, array, clear, dictionary, string

Attributes: wrapmode
"""
array_shift = MaxObject("array.shift")

"""
array.slice - Output a range of elements of an array object as a new array object

array.slice is similar to array.subarray, but attempts to conform to the JavaScript Array.slice() behavior.

Args:
  slice-indeces (list, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): slice indices (0-based)

Outlets:
  0 (OUTLET_TYPE): sliced array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_slice = MaxObject("array.slice")

"""
array.some - Test the elements of an array object for a matching condition

Each element of an incoming array will be output sequentially. Each element can be tested and a 0 or 1 passed back into the right inlet of the array.some object, indicating whether the element passed the test. When iteration is complete, either a 0 or 1 will be output, signalling whether some elements of the array matched the testing condition.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): test response (0/1)

Outlets:
  0 (OUTLET_TYPE): some elements of input array pass test (0/1)
  1 (OUTLET_TYPE): test output
  2 (OUTLET_TYPE): element index

Messages: bang, int, float, list, anything, array, cancel, dictionary, in1, string
"""
array_some = MaxObject("array.some")

"""
array.sort - Sort the elements of an array object according to a test

Each pair of entries is output, the user compares them and then passes a 1 (left is > right) or a 0 (left is <= right) to determine a final sorted order.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): sort comparison response (0/1)

Outlets:
  0 (OUTLET_TYPE): sorted array out
  1 (OUTLET_TYPE): sort comparison output 1
  2 (OUTLET_TYPE): sort comparison output 2

Messages: bang, int, float, list, anything, array, cancel, dictionary, in1, string

Attributes: simple
"""
array_sort = MaxObject("array.sort")

"""
array.split - Split an array object into two new array objects at a specified index

The position set as an argument or

Args:
  position (int, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): position in array to split

Outlets:
  0 (OUTLET_TYPE): array &lt; of split point
  1 (OUTLET_TYPE): array &gt;= of split point

Messages: bang, int, float, list, anything, array, dictionary, in1, string
"""
array_split = MaxObject("array.split")

"""
array.stddev - Calculate the standard deviation of the numerical elements of an array

The standard deviation is a quantity expressing by how much the members of a group differ from the mean value for the group. Non-numerical elements (symbols, arrays, strings, dictionaries) are ignored.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): result out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_stddev = MaxObject("array.stddev")

"""
array.stream - Make an array of a certain size

array.stream accepts a number in the right inlet which specifies the length of the output array. Following the receipt of this number, the object will collect this number of elements input through the left inlet. After the array-length is complete, and with each subsequent input, the array will be output from the left outlet. A 1 or a 0 will be output from the right outlet depending on whether the array-length has been reached or not. A 1 signifies that the array-length has been reached and that the object is now collecting the stream. Use the clear message to reset the array.

Args:
  array-length (int, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): length in

Outlets:
  0 (OUTLET_TYPE): streamed array out
  1 (OUTLET_TYPE): 1 if 0 elements are defined

Messages: bang, int, float, list, anything, array, clear, dictionary, string
"""
array_stream = MaxObject("array.stream")

"""
array.subarray - Output a range of elements of an array object as a new array object

array.subarray is similar to array.slice, but doesn't conform to the JavaScript Array.slice() behavior.

Args:
  subarray-indexes (list, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): subarray indices (0-based)

Outlets:
  0 (OUTLET_TYPE): subarray out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_subarray = MaxObject("array.subarray")

"""
array.thin - Remove duplicated entries from an array object

Inlets:
  0 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): thinned array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_thin = MaxObject("array.thin")

"""
array.tobuffer - Write array object values to an audio buffer

Values are assumed to be between -1 and 1. If the array contains multichannel data, each channel should be wrapped inside of a subarray (e.g. [ [ -1, 0, 1 ], [ -1, 0, -1 ] ]).

Args:
  buffer-name (symbol, required)

Inlets:
  0 (INLET_TYPE): array, messages in
  1 (INLET_TYPE): array, messages in

Outlets:
  0 (OUTLET_TYPE): bang on successful buffer write

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: buffername, channelstart, framelength, frameoffset, resize
"""
array_tobuffer = MaxObject("array.tobuffer")

"""
array.tolist - Convert an array object to a list

Nested arrays (arrays within arrays) can be automatically flattened with the flatten attribute.

Inlets:
  0 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): list out

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: flatten, stringmode
"""
array_tolist = MaxObject("array.tolist")

"""
array.tostring - Convert an array object to a string object

The string object contains a serialized representation of the array's contents.

Inlets:
  0 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): list out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_tostring = MaxObject("array.tostring")

"""
array.tosymbol - Convert an array object to a symbol

This object is particularly useful for visualization of an array object's contents.

Inlets:
  0 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): list out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_tosymbol = MaxObject("array.tosymbol")

"""
array.tuplewise - Make an array of a certain size (counting iterations)

Args:
  array-length (int, optional)

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): length in

Outlets:
  0 (OUTLET_TYPE): streamed array out
  1 (OUTLET_TYPE): 0-based output index since the last clear message was received

Messages: bang, int, float, list, anything, array, clear, dictionary, string
"""
array_tuplewise = MaxObject("array.tuplewise")

"""
array.union - Combine two arrays into a new array object containing non-duplicate entries of both arrays

Combine two arrays into a new array object containing non-duplicate entries of both arrays. For instance, the arrays [ 0, 1, 1, 2, 8 ] and [ 2, 2, 3, 3, 5 ] would become a new array [ 0, 1, 2, 8, 3, 5 ].

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_union = MaxObject("array.union")

"""
array.unique - Filtering duplicates and subtract arrays

Combine two arrays into a new array object containing non-duplicate entries of the first array which do not appear in the second array.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_unique = MaxObject("array.unique")

"""
array.unshift - Add one or more elements to the beginning of an array

The base array is sent to the right inlet, which clears any elements send to the left inlet. Additional elements are sent to the left inlet, and can be repeatedly placed at the front of the output array, without the user needing to update the array in the right inlet. Unlike the JavaScript implementation, the input array is not changed in place.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): unshifted array out

Messages: bang, int, float, list, anything, array, clear, dictionary, string

Attributes: wrapmode
"""
array_unshift = MaxObject("array.unshift")

"""
array.wrap - Wrap an array inside of an array

Among other things, you can use wrapped arrays in conjunction with array.concat to create multidimensional arrays.

Inlets:
  0 (INLET_TYPE): array in
  1 (INLET_TYPE): array in

Outlets:
  0 (OUTLET_TYPE): wrapped array out

Messages: bang, int, float, list, anything, array, dictionary, string
"""
array_wrap = MaxObject("array.wrap")

"""
asin - Arc-sine function

Use the asin object to calculate and output the arc-sine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Asin (x)

Messages: bang, int, float
"""
asin = MaxObject("asin")

"""
asinh - Hyperbolic arc-sine function

Use the asinh object to calculate and output the hyperbolic arc-sine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Asinh (x)

Messages: bang, int, float
"""
asinh = MaxObject("asinh")

"""
atan - Arc-tangent function

Use the atan object to calculate and output the arc-tangent of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Atan (x)

Messages: bang, int, float
"""
atan = MaxObject("atan")

"""
atan2 - Two-variable arc-tangent function

Use the atan2 object to calculate and output the arc-tangent of any two given numbers where the left input is the y value and the right input is the x value.

Args:
  x-value (number, optional)

Inlets:
  0 (INLET_TYPE): Y value
  1 (INLET_TYPE): X value

Outlets:
  0 (OUTLET_TYPE): atan2(y/x)

Messages: bang, int, float, ft1, in1
"""
atan2 = MaxObject("atan2")

"""
atanh - Hyperbolic arc-tangent function

Use the atanh object to calculate and output the hyperbolic arc-tangent of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Atanh (x)

Messages: bang, int, float
"""
atanh = MaxObject("atanh")

"""
atodb - Convert a linear value to decibels

Converts any given linear value to its corresponding decibel value.

Inlets:
  0 (float): (float) Amplitude Scalar

Outlets:
  0 (float): (float) Gain/Attenuation dB

Messages: bang, int, float, list, set
"""
atodb = MaxObject("atodb")

"""
atoi - Convert characters to integers

Inlets:
  0 (INLET_TYPE): Any message, bang triggers output
  1 (INLET_TYPE): Appends message to current contents
  2 (INLET_TYPE): Any message sets current contents

Outlets:
  0 (list): List of ints output

Messages: bang, int, float, list, anything, clear

Attributes: utf8
"""
atoi = MaxObject("atoi")

"""
attrui - Inspect attributes

Use attrui object to inspect the attribute values of the object(s) it is connected to.

Inlets:
  0 (INLET_TYPE): Messages In

Outlets:
  0 (OUTLET_TYPE): Connect to Objects whose Attribute you Want to Explore

Messages: int, float, list, anything, (mouse)

Attributes: align, annotation_name, arrowcolor, attr, attr_display, attrfilter, bgcolor, bgcolor2, bordercolor, displaymode, htricolor, lock, menu_display, orientation, parameter_enable, paramonly, rounded, showcaption, showgetonly, storeinpreset, style, text_width, textcolor, textjustification, tricolor
"""
attrui = MaxObject("attrui")

"""
autopattr - Expose multiple objects to the pattr system

Causes multiple objects within a patcher to be automatically included in the pattr system.


		Note: you should use only one instance of an autopattr object per level in a patch.

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): messages or data in

Outlets:
  0 (OUTLET_TYPE): include connection
  1 (OUTLET_TYPE): exclude connection
  2 (OUTLET_TYPE): passout
  3 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, getattributes, getstate

Attributes: autoname, autorestore, dirty, greedy, name
"""
autopattr = MaxObject("autopattr")

"""
bag - Store a collection of numbers

Stores and manages a collection of numbers. You can add to or delete an integer from a bag as well as report its contents. bag with any argument maintains multiple entries with the same item; otherwise it holds only one of each.

Args:
  duplicate-flag (symbol, optional)

Inlets:
  0 (INLET_TYPE): list Adds Items, bang Dumps Items
  1 (INLET_TYPE): Item Value in List

Outlets:
  0 (OUTLET_TYPE): Output for Items When bang is Received

Messages: bang, int, float, clear, cut, in1, length, send, list
"""
bag = MaxObject("bag")

"""
bangbang - Output a bang from many outlets

Outputs bang messages out of each outlet (in right-to-left order) when it receives any input. The number of outlets is determined by an argument.

Args:
  outlets (number, optional)

Inlets:
  0 (INLET_TYPE): Any Message, Repeated as One or More bangs

Outlets:
  0 (OUTLET_TYPE): Last bang Output
  1 (OUTLET_TYPE): First bang Output

Messages: bang, int, float, anything
"""
bangbang = MaxObject("bangbang")

"""
bendin - Output MIDI pitch bend values

Outputs pitch bend values received from a MIDI device. The MIDI port and channel can be chosen with messages or by double-clicking on the object.

Args:
  port-and-channel (symbol, required)
  channel (int, required)
  port (symbol, optional)
  midi-device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): Pitch Bend Amount
  1 (OUTLET_TYPE): MIDI Channel

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name
"""
bendin = MaxObject("bendin")

"""
bendout - Send MIDI pitch bend messages

Transmits MIDI pitchbend values to a MIDI device.

Args:
  port-and-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  midi-device (symbol, optional)

Inlets:
  0 (INLET_TYPE): Pitch Bend Amount
  1 (INLET_TYPE): MIDI Channel

Messages: int, float, anything, (mouse), in1, port

Attributes: matchport, name
"""
bendout = MaxObject("bendout")

"""
bgcolor - Set background color

Set the background color of the patcher window. The bgcolor object's functionality is equivalent to a brgb message sent to thispatcher.

Args:
  red (int, optional)
  green (int, optional)
  blue (int, optional)

Inlets:
  0 (INLET_TYPE): Red Value or list of RGB Values
  1 (INLET_TYPE): Green Value
  2 (INLET_TYPE): Blue Value
  3 (INLET_TYPE): Alpha Value (0.-1.)

Messages: bang, int, list, ft3, in1, in2, set
"""
bgcolor = MaxObject("bgcolor")

"""
bitand - Bitwise intersection of two numbers

Performs a bit-by-bit AND of two numbers as expressed in binary. Outputs a number composed of all those bits which are 1 in both of the two numbers.

Args:
  initial-value (int, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left & Right

Messages: bang, int, float, in1, set, list
"""
bitand = MaxObject("bitand")

"""
bitor - Bitwise union of two numbers

Performs a bit-by-bit OR of two numbers (expressed in binary for the task). Outputs a number composed of all those bits which are 1 in either of the two numbers.

Args:
  initial-value (int, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left | Right

Messages: bang, int, float, in1, set, list
"""
bitor = MaxObject("bitor")

"""
bline - Generate ramps using bang

Generates a linear ramp driven by incoming bang messages. It takes a list of breakpoint segments (and the number of events to span) and outputs a smooth ramp between values.

Args:
  initial-value (number, optional)

Inlets:
  0 (INLET_TYPE): bang, list, stop

Outlets:
  0 (OUTLET_TYPE): ramp output
  1 (OUTLET_TYPE): bang when bline reaches destination

Messages: bang, int, float, list, set, stop
"""
bline = MaxObject("bline")

"""
bondo - Synchronize a group of messages

Synchronizes and outputs a set of inputs when any input is received. It can also be set with a time interval value (in milliseconds) to wait before sending its output.

Args:
  inlets-outlets (int, optional)
  delay (int, optional)
  list-flag (symbol, optional)

Inlets:
  0 (INLET_TYPE): Input 1 to be Synchronized
  1 (INLET_TYPE): Input 2 to be Synchronized

Outlets:
  0 (OUTLET_TYPE): Output of Inlet 1
  1 (OUTLET_TYPE): Output of Inlet 2

Messages: bang, int, float, list, anything, set
"""
bondo = MaxObject("bondo")

"""
borax - Report note-on and note-off information

Acquires and outputs comprehensive information regarding note-on and note-off events. Information includes note counts, event details and time between note events.

Inlets:
  0 (INLET_TYPE): Pitch (Also Delta Message)
  1 (INLET_TYPE): Velocity
  2 (INLET_TYPE): Reset, Turns Off All Sounding Notes

Outlets:
  0 (OUTLET_TYPE): Number of Notes Since Last Reset
  1 (OUTLET_TYPE): Voice Allocation Number
  2 (OUTLET_TYPE): Number of Notes Currently Active
  3 (OUTLET_TYPE): Pitch
  4 (OUTLET_TYPE): Velocity
  5 (OUTLET_TYPE): Duration Count
  6 (OUTLET_TYPE): Duration Output
  7 (OUTLET_TYPE): Note Delta Count
  8 (OUTLET_TYPE): Delta Time

Messages: bang, int, delta, in1, list

Attributes: size
"""
borax = MaxObject("borax")

"""
bpatcher - Embed a subpatch with a visible UI

The bpatcher object holds the contents of a patcher or subpatcher, displaying only those visual elements that fall within its box rectangle. The number of inlets and outlets in a bpatcher object is determined by the number of inlet and outlet objects contained in its contained subpatcher.

Messages: (drag), (mouse), replace

Attributes: args, bgcolor, bgmode, border, clickthrough, embed, enablehscroll, enablevscroll, lockeddragscroll, lockedsize, name, offset
"""
bpatcher = MaxObject("bpatcher")

"""
bucket - Pass numbers from outlet to outlet

Outputs incoming values to outlets in bucket-brigade fashion. bucket acts as an n-stage shift register which can shift its contents from outlet to outlet in either direction.

Args:
  outlets (int, optional)
  output-flag (int, optional)

Inlets:
  0 (INLET_TYPE): Number to Bucket-brigade

Outlets:
  0 (OUTLET_TYPE): Delay Stage 1

Messages: bang, int, float, L2R, R2L, clear, freeze, l2r, r2l, roll, set, thaw
"""
bucket = MaxObject("bucket")

"""
buddy - Synchronize arriving data

Outputs incoming data after something has been received in all inlets.

Args:
  inlets (int, optional)

Inlets:
  0 (INLET_TYPE): Input to be Synchronized
  1 (INLET_TYPE): Input to be Synchronized

Outlets:
  0 (OUTLET_TYPE): Synchronized Output of Inlet 1
  1 (OUTLET_TYPE): Synchronized Output of Inlet 2

Messages: bang, int, float, list, anything, clear
"""
buddy = MaxObject("buddy")

"""
button - Blink and send a bang

button blinks when you send it any message, and it sends out a bang when you click on it.

Inlets:
  0 (INLET_TYPE): Cause Indicator to Flash and bang Output

Outlets:
  0 (OUTLET_TYPE): Output Received Message as bang

Messages: bang, int, float, list, anything, (mouse)

Attributes: annotation_name, bgcolor, blinkcolor, blinktime, fgcolor, outlinecolor, param_connect, parameter_enable, parameter_mappable, style
"""
button = MaxObject("button")

"""
capture - Store values to view or edit

Stores items in the order they are received for viewing, editing, and saving.

Args:
  maximum (int, optional)
  display-format (symbol, optional)

Inlets:
  0 (INLET_TYPE): Data to be Captured

Outlets:
  0 (OUTLET_TYPE): Sequential Output from dump Message
  1 (OUTLET_TYPE): Item Count Outlet

Messages: int, float, list, anything, clear, count, (mouse), dump, open, wclose, write

Attributes: listout, precision, size
"""
capture = MaxObject("capture")

"""
cartopol - Convert cartesian to polar coordinates

Converts a cartesian-coordinate pair consisting of real and imaginary values into a polar-coordinate pair consisting of distance and angle values.

Inlets:
  0 (INLET_TYPE): real/x input
  1 (INLET_TYPE): imaginary/y input

Outlets:
  0 (OUTLET_TYPE): amplitude/alpha output
  1 (OUTLET_TYPE): phase/theta output

Messages: bang, int, float
"""
cartopol = MaxObject("cartopol")

"""
change - Filter out repetitions of a number

Output a number only if it is different from the stored number and will reset the stored number to that differing input number. Alternate modes of operation also identify greater-than or less-than the conditions.

Args:
  initial-value (int, float, optional)
  mode (symbol, optional)

Inlets:
  0 (INLET_TYPE): Repeated to Output if Number Changes

Outlets:
  0 (OUTLET_TYPE): Number if it Changes
  1 (OUTLET_TYPE): 1 for Logical Transition from 0 to Non-Zero
  2 (OUTLET_TYPE): 1 for Logical Transition from Non-Zero to 0

Messages: int, float, mode, set
"""
change = MaxObject("change")

"""
chooser - Display a scrolling list of selectable items

The chooser object is similar to the umenu object, but it displays a scrolling list of selectable items rather than a pop-up menu.

Inlets:
  0 (INLET_TYPE): Messages

Outlets:
  0 (OUTLET_TYPE): Index (Message or Single-Click)
  1 (OUTLET_TYPE): Item (Message or Single-Click)
  2 (OUTLET_TYPE): Index (Double-Click)
  3 (OUTLET_TYPE): Item (Double-Click)
  4 (OUTLET_TYPE): Preview Control
  5 (OUTLET_TYPE): Dump Outlet

Messages: bang, int, float, anything, append, clear, count, delete, deselect, dictionary, insert, (mouse), next, play, prev, progress, set, setnext, setprev, sort, stop

Attributes: autopopulate, bgcolor, collection, depth, enabledrag, factorycontent, filekind, filtertext, headerheight, headerlabel, items, keynavigate, margin, multiselect, parameter_enable, parameter_mappable, prefix, prefix_mode, preview, selectedclick, selectioncolor, showdotfiles, stripecolor, style, textcolor, types, useselectioncolor
"""
chooser = MaxObject("chooser")

"""
clip - Limit numbers to a range

Args:
  minimum (number, optional)
  maximum (number, optional)

Inlets:
  0 (INLET_TYPE): value to be constrained
  1 (INLET_TYPE): minimum value
  2 (INLET_TYPE): maximum value

Outlets:
  0 (OUTLET_TYPE): output value

Messages: int, float, list, set

Attributes: mode
"""
clip = MaxObject("clip")

"""
clocker - Report elapsed time, at regular intervals

The clocker object is a metronome that reports the time elapsed since it was started. This object uses the Max time format syntax, so the interval that the clocker object uses can be either fixed or tempo-relative. Its output can be quantized using tempo-relative syntax, and if the autostarttime attribute is set, the object can also start at a tempo-relative point.

Args:
  time-interval (int, symbol, float, required)

Inlets:
  0 (INLET_TYPE): Start/Stop Metronome
  1 (INLET_TYPE): Set Metronome Time Interval

Outlets:
  0 (OUTLET_TYPE): Output Ticks of Metronome

Messages: bang, int, float, list, anything, clock, reset, stop

Attributes: active, autostart, autostarttime, defer, interval, quantize, transport
"""
clocker = MaxObject("clocker")

"""
closebang - Send a bang on close

Sends a bang whenever the patcher window within which it resides is closed.

Inlets:
  0 (INLET_TYPE): Sends bang When Patcher Window Is Closed

Outlets:
  0 (OUTLET_TYPE): Output bang

Messages: bang, (mouse)
"""
closebang = MaxObject("closebang")

"""
coll - Store and edit a collection of data

Allows for the storage, organization, editing, and retrieval of different messages.

Args:
  name (symbol, optional)
  no-search (any, optional)

Inlets:
  0 (INLET_TYPE): Single Item Outputs Data, Multiple Items Store Data

Outlets:
  0 (OUTLET_TYPE): Data Contents
  1 (OUTLET_TYPE): Number or Symbol Associated With Data
  2 (OUTLET_TYPE): bang When Finished Reading Data File
  3 (OUTLET_TYPE): bang When Finished With Dump Output

Messages: bang, int, float, list, anything, append, assoc, clear, (mouse), deassoc, delete, dump, end, filetype, flags, goto, insert, insert2, length, max, merge, min, next, nstore, nsub, nth, open, prev, read, readagain, refer, remove, renumber, renumber2, separate, sort, start, store, sub, subsym, swap, symbol, wclose, write, writeagain

Attributes: embed, name, precision
"""
coll = MaxObject("coll")

"""
coll.codebox - Store and edit a collection of data

Allows for the storage, organization, editing, and retrieval of different messages, using a UI object for display and editing.

Inlets:
  0 (INLET_TYPE): Single Item Outputs Data, Multiple Items Store Data

Outlets:
  0 (OUTLET_TYPE): Data Contents
  1 (OUTLET_TYPE): Number or Symbol Associated With Data
  2 (OUTLET_TYPE): bang When Finished Reading Data File
  3 (OUTLET_TYPE): bang When Finished With Dump Output

Messages: int, float, list, (mouse), anything, append, assoc, bang, symbol, clear, deassoc, delete, dump, end, filetype, flags, goto, insert, insert2, length, max, merge, min, next, nstore, nsub, nth, open, prev, read, readagain, refer, remove, renumber, renumber2, separate, sort, start, store, sub, subsym, swap, wclose, write, writeagain

Attributes: bgcolor, editlocked, embed, linenumbers, linenumberwidth, margin, style, textcolor, precision, name
"""
coll_codebox = MaxObject("coll.codebox")

"""
colorpicker - Select and output a color

Inlets:
  0 (INLET_TYPE): bang opens picker, list sets initial color value

Outlets:
  0 (OUTLET_TYPE): List of RGB Color Values
  1 (OUTLET_TYPE): bang when mouse is released and when color window closes

Messages: bang, list, (mouse)

Attributes: compatibility, currentcolor
"""
colorpicker = MaxObject("colorpicker")

"""
combine - Combine multiple items into a single symbol

Combines a list of items into a single symbol. It works similar to pack and sprintf. The behavior can be modified with attributes that provide number padding and triggered output.

Args:
  inlets (any, optional)

Inlets:
  0 (INLET_TYPE): (0)

Outlets:
  0 (OUTLET_TYPE): merged output
  1 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything

Attributes: padding, triggers
"""
combine = MaxObject("combine")

"""
comment - Explanatory note or label

comment displays text which is typed into it in order to serve as a label or explanatory text.

Inlets:
  0 (INLET_TYPE): Messages in

Messages: append, prepend, set, setwithtruncation, string

Attributes: bgcolor, bubble, bubble_bgcolor, bubble_outlinecolor, bubblepoint, bubbleside, bubbletextmargin, bubbleusescolors, style, suppressinlet, underline
"""
comment = MaxObject("comment")

"""
conformpath - Convert file paths styles

Converts paths between the older colon style formats and the current slash style. It can also be used to conform paths to either absolute, relative, boot volume relative, or Cycling 74 folder relative types.

Args:
  pathstyle (symbol, optional)
  pathtype (symbol, optional)

Inlets:
  0 (symbol): (symbol) Path Input

Outlets:
  0 (symbol): (symbol) Conformed Path Output
  1 (int): (int) Success

Messages: anything, pathstyle, pathtype
"""
conformpath = MaxObject("conformpath")

"""
console - Console Output in Patcher

Mirror and filter messages to the Max window in your patcher.

Inlets:
  0 (INLET_TYPE): Change Filter Attributes

Outlets:
  0 (OUTLET_TYPE): Object Name
  1 (OUTLET_TYPE): Message
  2 (int): Message Type

Messages: clear, write

Attributes: classfilter, patcherfilter, showonlyerrors, textfilter
"""
console = MaxObject("console")

"""
cos - Cosine function

Use the cos object to calculate and output the cosine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Cos (x)

Messages: bang, int, float
"""
cos = MaxObject("cos")

"""
cosh - Hyperbolic cosine function

Use the cosh object to calculate and output the hyperbolic cosine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Cosh (x)

Messages: bang, int, float
"""
cosh = MaxObject("cosh")

"""
counter - Keep count based on bang messages

Outputs the current count of bang message constrained to a specified range. Can be set to count up, down, or up-then-down.

Args:
  options (int, optional)

Inlets:
  0 (INLET_TYPE): int, bang are Counted, set Sets Counter Value
  1 (INLET_TYPE): Set Direction: 0 = Up, 1 = Down, 2 = UpDown
  2 (INLET_TYPE): Resets Counter to Number on Next Clock
  3 (INLET_TYPE): Resets Counter to Number Immediately
  4 (INLET_TYPE): Sets Count Maximum

Outlets:
  0 (OUTLET_TYPE): Current Count
  1 (OUTLET_TYPE): Underflow (Counter Hit Minimum) Flag
  2 (OUTLET_TYPE): Carry (Counter Hit Maximum) Flag
  3 (OUTLET_TYPE): Carry Count

Messages: bang, int, float, carrybang, carryint, dec, down, flags, goto, inc, jam, max, min, next, set, setmin, state, up, updown

Attributes: carryflag, compatmode
"""
counter = MaxObject("counter")

"""
cpuclock - Retrieve the CPU time

Accesses a precise value from the system timer. This allows for timing calculations with very high resolution.

Inlets:
  0 (INLET_TYPE): Bang to get the current system time.

Outlets:
  0 (OUTLET_TYPE): Current system time.

Messages: bang, reset
"""
cpuclock = MaxObject("cpuclock")

"""
crosspatch - Patching Editor for Matrix Objects

Connect a crosspatch to a client object (including matrix~, mcs.matrix~, mc.matrix~, matrix, gate~, mc.gate~, mcs.gate~, gate, selector, mc.selector~, mcs.selector~, and switch) to use a patching interface to edit connections between inputs and outputs.

Inlets:
  0 (INLET_TYPE): list Adds or Deletes a Connection

Outlets:
  0 (OUTLET_TYPE): Connect to matrix~ or matrix Object
  1 (dictionary): From dump Message

Messages: bang, list, clear, dictionary, dump, dumpconnections, indisable, (mouse), outdisable

Attributes: allowdisabled, annotation_name, bgcolor, candycane, candycane2, candycane3, candycane4, candycane5, candycane6, candycane7, candycane8, candymode, colorlabels, connectacrossdividers, dimmedconnectionalpha, dividercolor, dividers, embed, exclusive, gaincaption, gaindragmode, gainradius, gainstyle, incolormap, initialgain, inlabels, labelheight, labelwidth, linecolor, maxgain, numins, numouts, outcolormap, outlabels, overgaincolor, parameter_enable, parameter_mappable, preservegain, showgain, showlabels, style, textcolor
"""
crosspatch = MaxObject("crosspatch")

"""
ctlin - Output received MIDI control values

Output the value from a specific controller number and MIDI channel.

Args:
  port (symbol, optional)
  device (symbol, optional)
  ctrllr-channel (list, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): Controller Value
  1 (OUTLET_TYPE): Controller Number
  2 (OUTLET_TYPE): MIDI Channel

Messages: anything, (mouse), (MIDI), port, set

Attributes: matchport, name
"""
ctlin = MaxObject("ctlin")

"""
ctlout - Transmit MIDI controller messages

Transmits MIDI continuous controller values to a MIDI device.

Args:
  port (symbol, optional)
  device (symbol, optional)
  ctrllr-channel (list, optional)

Inlets:
  0 (INLET_TYPE): Controller Value
  1 (INLET_TYPE): Controller Number
  2 (INLET_TYPE): MIDI Channel

Messages: int, float, anything, (mouse), in1, in2, port

Attributes: matchport, name
"""
ctlout = MaxObject("ctlout")

"""
cycle - Round-robin messages to outlets

Each incoming number is sent to the next outlet, wrapping around to the first outlet after the last has been reached.

Args:
  outlets (int, optional)
  mode (int, optional)

Inlets:
  0 (INLET_TYPE): Number to Send to Successive Outlets

Outlets:
  0 (OUTLET_TYPE): Output of Item 1

Messages: bang, int, float, list, anything, set, symbol, thresh
"""
cycle = MaxObject("cycle")

"""
date - Report current date and time

Reports the current date, time, or the number of 1/60th-second ticks since startup.

Inlets:
  0 (INLET_TYPE): date, time, or ticks Message

Outlets:
  0 (OUTLET_TYPE): Date (Month/Day/Year)
  1 (OUTLET_TYPE): Time (Hour/Minute/Second)
  2 (OUTLET_TYPE): Ticks (1/60th Second)

Messages: date, ticks, time
"""
date = MaxObject("date")

"""
dbtoa - Convert decibels to a linear value

Converts a decibel value to its corresponding linear value.

Inlets:
  0 (float): (float) Gain/Attenuation dB

Outlets:
  0 (float): (float) Amplitude Scalar

Messages: bang, int, float, list, set
"""
dbtoa = MaxObject("dbtoa")

"""
decide - Choose randomly between 1 and 0

Outputs random 1 and 0 messages. The output sequence depends on the seed value to determine the sequence of values.

Args:
  seed (int, optional)

Inlets:
  0 (INLET_TYPE): Make A Decision
  1 (INLET_TYPE): Set Seed (0 = Use System Time)

Outlets:
  0 (OUTLET_TYPE): Randomly Generated 0 or 1

Messages: bang, int, in1
"""
decide = MaxObject("decide")

"""
decode - Send 1 or 0 out a specific outlet

Provides hierarchical switching. The right inlet turns all outlets off, switch, while the middle inlet turns all outlets on. The right inlet overrides the middle inlet, and the middle inlet overrides numnbers sent to the left inlet that turn individual outlets on or off.

Args:
  outlets (int, optional)
  outlets (float, optional)

Inlets:
  0 (INLET_TYPE): Number to decode (lowest priority)
  1 (INLET_TYPE): Enable (medium priority)
  2 (INLET_TYPE): Disable All (highest priority)

Outlets:
  0 (OUTLET_TYPE): Decode 0 (high if input = 0)

Messages: bang, int, in1, in2
"""
decode = MaxObject("decode")

"""
defer - Defer execution of a message

Defers the output of all messages sent through it to the lower priority main thread. This is most applicable when using Overdrive mode.

Inlets:
  0 (INLET_TYPE): Message To Be Deferred

Outlets:
  0 (OUTLET_TYPE): The Deferred Message

Messages: bang, int, float, list, anything
"""
defer = MaxObject("defer")

"""
deferlow - Defer the execution of a message (always)

The deferlow object places all incoming messages at the tail of the low priority queue. This is unlike the defer object, however, which places high priority messages at the front of the low priority queue, and passes low priority messages immediately. The deferlow object is useful to preserve message sequencing that might otherwise be reversed with the defer object and/or guarantee that an incoming message will be deferred to a future servicing of the low priority queue even if that message is low priority itself.

Inlets:
  0 (INLET_TYPE): Message To Be Deferred

Outlets:
  0 (OUTLET_TYPE): The Deferred Message

Messages: bang, int, float, list, anything
"""
deferlow = MaxObject("deferlow")

"""
delay - Delay a bang

Holds a bang for a specified amount of time before sending it to the next object. This object uses the Max time format syntax, so the delay time (which is normally specified in milliseconds) can also be set to other fixed or tempo-relative values.

Args:
  time (any, required)

Inlets:
  0 (INLET_TYPE): bang Gets Delayed, stop Cancels
  1 (INLET_TYPE): Set Delay Time in Milliseconds

Outlets:
  0 (OUTLET_TYPE): Delayed bang

Messages: bang, int, float, list, anything, clock, stop

Attributes: delaytime, quantize, transport
"""
delay = MaxObject("delay")

"""
detonate - Play a score of note events

Provides score playback managed using Max messages. The score may be loaded from a MIDI file, or generated using Max functions. The score is not limited to MIDI notes and values; any information can be stored and played back with detonate.

Args:
  label (symbol, required)

Inlets:
  0 (INLET_TYPE): Control Messages, Time Parameter When Recording
  1 (INLET_TYPE): Pitch Parameter When Recording
  2 (INLET_TYPE): Vel Parameter When Recording
  3 (INLET_TYPE): Dur Parameter When Recording
  4 (INLET_TYPE): Chan Parameter When Recording
  5 (INLET_TYPE): Track Parameter When Recording
  6 (INLET_TYPE): X1 Parameter When Recording
  7 (INLET_TYPE): X2 Parameter When Recording

Outlets:
  0 (OUTLET_TYPE): Time Parameter Output
  1 (OUTLET_TYPE): Pitch Parameter Output
  2 (OUTLET_TYPE): Vel Parameter Output
  3 (OUTLET_TYPE): Dur Parameter Output
  4 (OUTLET_TYPE): Chan Parameter Output
  5 (OUTLET_TYPE): Track Parameter Output
  6 (OUTLET_TYPE): X1 Parameter Output
  7 (OUTLET_TYPE): X2 Parameter Output

Messages: bang, int, float, list, clear, (mouse), delay, export, follow, followat, import, in1, in2, in3, in4, in5, in6, in7, mute, next, nth, open, params, read, record, restore, setparam, start, startat, stop, unmute, unmuteall, wclose, write, writemax
"""
detonate = MaxObject("detonate")

"""
dial - Output numbers using an onscreen dial

Outputs numbers according to its degree of rotation. dial can be set with a certain range, offset, multiplier, as well as numerous visual settings.

Inlets:
  0 (INLET_TYPE): Dial Displays Value Received

Outlets:
  0 (OUTLET_TYPE): Outputs Value Received or Entered

Messages: bang, int, float, (mouse), resize, set, setminmax

Attributes: annotation_name, bgcolor, clip, degrees, fgcolor, floatoutput, inputrangemode, min, mode, mult, needlecolor, outlinecolor, param_connect, parameter_enable, parameter_mappable, size, style, thickness, vtracking
"""
dial = MaxObject("dial")

"""
dialog - Open a dialog box

Displays a dialog box with a selection of appearance modes. In Default mode, the dialog object permits entry of a symbol (as text) and sends it out the outlet when you click on the "OK" button in the dialog box. In the other modes, the dialog object displays text, but doesn't permit data entry.

Args:
  label (symbol, optional)

Inlets:
  0 (INLET_TYPE): Show Dialog
  1 (INLET_TYPE): 0 Sets Output as Symbol, 1 as List/Message

Outlets:
  0 (OUTLET_TYPE): Text Entered in Dialog / bang
  1 (OUTLET_TYPE): bang When User Cancels
  2 (OUTLET_TYPE): bang When User Clicks No in Extended mode

Messages: bang, int, float, clearsymbol, in1, symbol

Attributes: label, mask, mode
"""
dialog = MaxObject("dialog")

"""
dict - Create and access dictionaries

Use the dict object to create named dictionaries, clone existing dictionaries, and query existing dictionaries to access their data.

Args:
  name (symbol, optional)
  filename (symbol, optional)

Inlets:
  0 (INLET_TYPE): dictionary input clones and sends, bang sends
  1 (INLET_TYPE): dictionary input clones but does not send

Outlets:
  0 (OUTLET_TYPE): dictionary
  1 (OUTLET_TYPE): value for an invidual key in response to the 'get' message
  2 (OUTLET_TYPE): list of keys in response to 'getkeys' message
  3 (OUTLET_TYPE): list of dictionaries in response to 'getnames' message
  4 (OUTLET_TYPE): file operation success/failure notifications

Messages: bang, append, clear, clone, contains, (mouse), dictionary, edit, export, get, getkeys, getnames, getsize, gettype, import, parse, pull_from_coll, push_to_coll, read, readagain, readany, remove, replace, set, setparse, wclose, write, writeagain

Attributes: annotation_name, embed, legacy, name, parameter_enable, parameter_mappable, quiet
"""
dict_ = MaxObject("dict")

"""
dict.codebox - Create and access dictionaries

The dict.codebox object is a UI object for display and editing of dictionaries. Use the dict.codebox object to create named dictionaries, clone existing dictionaries, and query existing dictionaries to access their data.

Inlets:
  0 (INLET_TYPE): dictionary input clones and sends, bang sends
  1 (INLET_TYPE): dictionary input clones but does not send

Outlets:
  0 (OUTLET_TYPE): dictionary
  1 (OUTLET_TYPE): value for an invidual key in response to the 'get' message
  2 (OUTLET_TYPE): list of keys in response to 'getkeys' message
  3 (OUTLET_TYPE): list of dictionaries in response to 'getnames' message
  4 (OUTLET_TYPE): file operation success/failure notifications

Messages: bang, clear, remove, getkeys, contains, getnames, getsize, gettype, get, set, append, replace, setparse, parse, clone, dictionary, read, readagain, import, readany, write, export, writeagain, (mouse), edit, wclose, pull_from_coll, push_to_coll

Attributes: bgcolor, editlocked, embed, linenumbers, linenumberwidth, margin, style, textcolor, legacy, name, parameter_enable, parameter_mappable, quiet
"""
dict_codebox = MaxObject("dict.codebox")

"""
dict.compare - Compare two dictionaries for equivalence.

Dictionaries are considered equivalent if they contain the identical keys, and if those keys contain identical data.

Inlets:
  0 (INLET_TYPE): dictionary to compare with dictionary at right inlet
  1 (INLET_TYPE): dictionary to compare with dictionary at left inlet

Outlets:
  0 (OUTLET_TYPE): 1 if equivalent, 0 if different

Messages: bang, dictionary

Attributes: unordered
"""
dict_compare = MaxObject("dict.compare")

"""
dict.deserialize - Create a dictionary from text

Use the dict.deserialize object to create a dictionary from text passed in using Max's dictionary syntax or JSON. Alternatively, compressed forms of either format may be used.

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): dictionary whose contents will be deserialized

Outlets:
  0 (OUTLET_TYPE): deserialized data from dictionary

Messages: anything

Attributes: name
"""
dict_deserialize = MaxObject("dict.deserialize")

"""
dict.group - Build a dictionary iteratively

Use the dict.group object to build up a dictionary by sending key-value pairs as lists. The key-value pairs will be collected into the dictionary until a 'bang' is received. The 'bang' will send out the dictionary and start the process over again.

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): dictionary syntax for appending a key/value pair, bang sends the dictionary and resets

Outlets:
  0 (OUTLET_TYPE): dictionary output

Messages: bang, list, anything

Attributes: name
"""
dict_group = MaxObject("dict.group")

"""
dict.iter - Stream the content of a dictionary

Use the dict.iter object to send all key-value pairs out from a dictionary as a series of standard Max list messages.

Inlets:
  0 (INLET_TYPE): dictionary input

Outlets:
  0 (OUTLET_TYPE)

Messages: array, dictionary
"""
dict_iter = MaxObject("dict.iter")

"""
dict.join - Merge the content of two dictionaries

Use the dict.join object to merge the content of two dictionaries together into a single dictionary.

Args:
  default-values (list, optional)

Inlets:
  0 (INLET_TYPE): dictionary to combined with dictionary at right inlet
  1 (INLET_TYPE): dictionary to combined with dictionary at left inlet

Outlets:
  0 (OUTLET_TYPE): dictionary of entries combined from both inlets

Messages: bang, array, dictionary
"""
dict_join = MaxObject("dict.join")

"""
dict.pack - Create a dictionary and set its values

Use the dict.pack object to create a dictionary and set its values using dedicated inlets.

Args:
  name (symbol, optional)
  default-values (list, optional)

Inlets:
  0 (INLET_TYPE): value for the 'foo' key

Outlets:
  0 (OUTLET_TYPE): dictionary output

Messages: bang, int, float, list, anything, array, dictionary, string

Attributes: keys, name, triggers, unmatched
"""
dict_pack = MaxObject("dict.pack")

"""
dict.print - Post a dictionary to the Max Console

Use the dict.print object to post the content of a dictionary to the Max Console. For more control over how the printing is formatted, use dict.iter and print.

Args:
  identifier (any, optional)

Inlets:
  0 (INLET_TYPE): dictionary input

Messages: array, dictionary
"""
dict_print = MaxObject("dict.print")

"""
dict.route - Compare dictionaries

Use the dict.route object to compare two dictionaries. If the dictionary received at the left inlet meets the specifications set by the dictionary at the right inlet, then pass the dictionary through the left outlet. Otherwise passes the dictionary through the right outlet.

Args:
  default-values (list, optional)

Inlets:
  0 (INLET_TYPE): dictionary input
  1 (INLET_TYPE): dictionary input

Outlets:
  0 (OUTLET_TYPE): dictionary matching specified keys/values
  1 (OUTLET_TYPE): dictionary not-matching specified keys/values

Messages: dictionary
"""
dict_route = MaxObject("dict.route")

"""
dict.serialize - Convert a dictionary's content to text

Use the dict.serialize object to output a serialized form of the contents of dictionary in text format. The text may be Dictionary Syntax, JSON, or Base64-compressed forms of these formats.

Inlets:
  0 (INLET_TYPE): dictionary whose contents will be serialized

Outlets:
  0 (OUTLET_TYPE): serialized data from dictionary

Messages: dictionary

Attributes: compress, mode
"""
dict_serialize = MaxObject("dict.serialize")

"""
dict.slice - Split a dictionary into two dictionaries

Use the dict.slice object to split a dictionary into two dictionaries. The first dictionary will be created from a set of keys you provide, sent to the left outlet. The remaining dictionary content will form the second dictionary, sent to the right outlet.

Inlets:
  0 (INLET_TYPE): dictionary input

Outlets:
  0 (OUTLET_TYPE): dictionary output sliced of specified keys
  1 (OUTLET_TYPE): series of key/value pairs sliced from the dictionary

Messages: dictionary

Attributes: keys
"""
dict_slice = MaxObject("dict.slice")

"""
dict.strip - Remove keys from a dictionary

Use the dict.strip object to remove the specified keys from a dictionary and return their values through an outlet. Following this operation, the dictionary will no longer contain the specified keys.

Args:
  keys (list, optional)

Inlets:
  0 (INLET_TYPE): dictionary input

Outlets:
  0 (OUTLET_TYPE): dictionary output stripped of specified keys
  1 (OUTLET_TYPE): series of key/value pairs stripped from the dictionary

Messages: dictionary

Attributes: keys
"""
dict_strip = MaxObject("dict.strip")

"""
dict.unpack - Extract values from a dictionary

Use the dict.unpack object to return the values of specified keys through dedicated outlets.

Args:
  name (symbol, optional)
  default-values (list, optional)

Inlets:
  0 (INLET_TYPE): dictionary input

Outlets:
  0 (OUTLET_TYPE): value for the 'foo' key
  1 (OUTLET_TYPE): dictionary: unmatched keys

Messages: bang, array, dictionary

Attributes: keys, legacy, unmatched
"""
dict_unpack = MaxObject("dict.unpack")

"""
dict.view - View the contents of a dictionary

Use the dict.view object to view the contents of a dictionary.

Messages: bang, dictionary, expand, expandall, (mouse)

Attributes: bgcolor, stripecolor, style, textcolor
"""
dict_view = MaxObject("dict.view")

"""
div - Divide two numbers

Divides two numbers (according to the specified divisor assignment), and then outputs the result.

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left / Right

Messages: bang, int, float, in1, set, list
"""
div = MaxObject("div")

"""
dropfile - Drag and drop files

dropfile defines a region for dragging and dropping files into and then outputs the filepath and filetype upon file input.

Inlets:
  0 (INLET_TYPE): types Specifies Acceptable File Types

Outlets:
  0 (OUTLET_TYPE): Pathname of Dropped File
  1 (OUTLET_TYPE): Type Code of Dropped File

Messages: (drag)

Attributes: border, bordercolor, folderslash, rounded, types
"""
dropfile = MaxObject("dropfile")

"""
drunk - Output random numbers within a step range

Performs a "drunken" walk by outputting random numbers within a specified step range.

Args:
  maximum value (int, float, optional)
  step size (int, float, optional)

Inlets:
  0 (INLET_TYPE): bang Outputs Random Step, int Sets Value
  1 (INLET_TYPE): Sets Range Size
  2 (INLET_TYPE): Sets Step Size

Outlets:
  0 (OUTLET_TYPE): Random Walk Output

Messages: bang, int, float, list, reset, set, setresetvalue

Attributes: cycle, floatoutput, range, seed, stepsize
"""
drunk = MaxObject("drunk")

"""
equals - Compare numbers for equal-to condition

Compares two values to see if one value is equal to a second. Outputs a 1 if the number is equal to the comparison-number or 0 if it is not.

Args:
  comparison-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left == Right

Messages: bang, int, float, in1, set, list

Attributes: fuzzy
"""
equals = MaxObject("equals")

"""
error - Report Max errors

Listens for and reports Max errors as message output. This will allow for error management in cases where it is not appropriate to display the Max window.

Args:
  on (int, optional)

Inlets:
  0 (INLET_TYPE): 1 Turns Error Listening On, 0 Turns It Off

Outlets:
  0 (OUTLET_TYPE): Error Message Output

Messages: int, float, error
"""
error = MaxObject("error")

"""
expr - Evaluate a mathematical expression

Evaluate an expression using a C-like language. Variables and operators are used to create output values.

Args:
  expression (list, required)
  constant (number, required)
  inlet-format (symbol, required)
  table-info (, required)
  (other) (symbol, required)

Inlets:
  0 (INLET_TYPE): Evaluate Expression, int $i1, float $f1, table $s1

Outlets:
  0 (OUTLET_TYPE): Expression Result

Messages: bang, int, float, ft1, ft2, ft3, ft4, ft5, ft6, ft7, ft8, ft9, in1, in2, in3, in4, in5, in6, in7, in8, in9, set, sm1, sm2, sm3, sm4, sm5, sm6, sm7, sm8, sm9, symbol, list
"""
expr = MaxObject("expr")

"""
filedate - Report the modification date of a file

Inlets:
  0 (INLET_TYPE): File/Folder to Check Modification Date

Outlets:
  0 (OUTLET_TYPE): Mod Date (Month/Day/Year/Hour/Min/Sec)

Messages: anything
"""
filedate = MaxObject("filedate")

"""
filein - Read and access a file of binary data

filein reads a file of binary data and outputs the data at various points in the file given the appropriate input.

Args:
  filename (symbol, optional)
  spool (symbol, optional)

Inlets:
  0 (INLET_TYPE): Read Byte from File
  1 (INLET_TYPE): Read Word from File
  2 (INLET_TYPE): Read Long from File

Outlets:
  0 (OUTLET_TYPE): File Data Output
  1 (OUTLET_TYPE): bang On End of File
  2 (OUTLET_TYPE): bang When Read/Spool Finished

Messages: int, fclose, in1, in2, read, spool
"""
filein = MaxObject("filein")

"""
filepath - Manage and report on the Max search path

Provides access to the Max search path, and allows modification to the search path used by a patch.

Args:
  path-type (symbol, required)
  preference (int, optional)

Inlets:
  0 (INLET_TYPE): set Changes Path, append Adds Path

Outlets:
  0 (OUTLET_TYPE): Path Stored in Preferences

Messages: bang, append, clear, revert, set
"""
filepath = MaxObject("filepath")

"""
filewatch - Watch a file for changes

Watch a specific file and reports a bang whenever that file is altered.

Args:
  filename (symbol, optional)

Inlets:
  0 (INLET_TYPE): File/Folder Name, bang Starts Watching

Outlets:
  0 (OUTLET_TYPE): bang If File Changes

Messages: bang, int, anything, stop
"""
filewatch = MaxObject("filewatch")

"""
float - Store a decimal number

float can store and output any given floating-point number.

Args:
  initial-value (float, optional)

Inlets:
  0 (INLET_TYPE): bang Outputs Value, int Sets and Outputs Value
  1 (INLET_TYPE): Set Value Without Output

Outlets:
  0 (OUTLET_TYPE): Value

Messages: bang, int, float, in1, send, set
"""
float_ = MaxObject("float")

"""
flonum - Display and output a number

Display, input, and output floating-point numbers in a number box.

Inlets:
  0 (INLET_TYPE): Set Displayed Number and Repeat to Output

Outlets:
  0 (OUTLET_TYPE): Output Incoming or Entered Number
  1 (OUTLET_TYPE): bang When Tab Key Pressed

Messages: bang, int, float, max, min, (mouse), select, set

Attributes: bgcolor, bordercolor, cantchange, hbgcolor, htextcolor, htricolor, maximum, minimum, mouseup, numdecimalplaces, outputonclick, parameter_enable, textcolor, triangle, tricolor, triscale
"""
flonum = MaxObject("flonum")

"""
flush - Output MIDI note-offs for held notes

Outputss note-off messages for any held note-ons. flush keeps track of all note-ons passed through it, and produces note-off messages for any held notes when it receives a bang message.

Inlets:
  0 (INLET_TYPE): Pitch Input, bang Flushes Held Notes
  1 (INLET_TYPE): Velocity Input

Outlets:
  0 (OUTLET_TYPE): Pitch Output
  1 (OUTLET_TYPE): Velocity Output

Messages: bang, int, clear, in1
"""
flush = MaxObject("flush")

"""
folder - List the files in a folder

Outputs all of the file names in a given folder. This can be especially useful for loading a umenu.

Args:
  pathname (symbol, optional)

Inlets:
  0 (INLET_TYPE): Path Name of Folder to List

Outlets:
  0 (OUTLET_TYPE): File Name List, Connect to Menu
  1 (OUTLET_TYPE): Count of Items Listed

Messages: bang, int, anything, symbol, types
"""
folder = MaxObject("folder")

"""
follow - Compare a live performance to a recorded performance

follow records pitches, or you can give it a MIDI file, in which case it looks at the file's note-ons and ignores other events. When it is "following" it outputs the index of the last note matched.

Args:
  filename (symbol, optional)

Inlets:
  0 (INLET_TYPE): MIDI Pitch Input, Many Other Messages

Outlets:
  0 (OUTLET_TYPE): Number of Note In Score
  1 (OUTLET_TYPE): MIDI Pitch Output

Messages: bang, int, float, append, delay, dump, follow, in1, next, print, read, record, start, stop, write
"""
follow = MaxObject("follow")

"""
fontlist - List system fonts

Outputs a list of system fonts and, optionally, their system identification numbers. Optionally filters the list by font-family.

Args:
  font-type (symbol, optional)

Inlets:
  0 (INLET_TYPE): bang to output font list

Outlets:
  0 (OUTLET_TYPE): connect to an umenu object

Messages: bang
"""
fontlist = MaxObject("fontlist")

"""
forward - Send messages to specified receive objects

Relays messages to other objects remotely. Unlike the send object, the destination receive object of forward can be changed with each message.

Args:
  receiver (symbol, optional)

Inlets:
  0 (INLET_TYPE): Message To Remote, send Changes Receiver

Messages: bang, int, float, list, anything, send
"""
forward = MaxObject("forward")

"""
fpic - Display an image

Inlets:
  0 (INLET_TYPE): read Changes File, offset Changes Visible Area

Outlets:
  0 (matrix): out

Messages: bang, (drag), (mouse), noscale, offset, pict, read, readany, rect

Attributes: alpha, autofit, destrect, embed, forceaspect, pic, xoffset, yoffset
"""
fpic = MaxObject("fpic")

"""
freebang - Send a bang when a patcher is freed

Inlets:
  0 (INLET_TYPE): Sends bang When Patcher Is Freed

Outlets:
  0 (OUTLET_TYPE): Sends bang When Patcher Is Freed

Messages: bang, (mouse)
"""
freebang = MaxObject("freebang")

"""
fromsymbol - Convert a symbol into numbers/messages

fromsymbol will take the individual characters in a symbol and convert them from a symbol back to numbers/messages.

Inlets:
  0 (INLET_TYPE): Symbol Input to be Analyzed

Outlets:
  0 (OUTLET_TYPE): Input Converted from Symbol

Messages: bang, int, float, anything

Attributes: separator
"""
fromsymbol = MaxObject("fromsymbol")

"""
fswap - Swap position of two numbers

Swaps the values of its inlets, preserving right-to-left ordering. The first outlet type is determined by its argument. The second outlet's type is always a float.

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): Sets Value for Right Outlet, Causes Output
  1 (INLET_TYPE): Sets Value for Left Outlet

Outlets:
  0 (OUTLET_TYPE): Value From Right Inlet
  1 (OUTLET_TYPE): Value From Left Inlet

Messages: bang, int, float, ft1, in1
"""
fswap = MaxObject("fswap")

"""
ftom - Convert frequency to a MIDI note number

ftom converts frequency to MIDI note numbers

Args:
  format (float, optional)

Inlets:
  0 (INLET_TYPE): Frequency In

Outlets:
  0 (OUTLET_TYPE): MIDI Note Number Out

Messages: int, float, list

Attributes: base, map, mapname, mid, ref, round, scale, scalename
"""
ftom = MaxObject("ftom")

"""
funbuff - Store pairs of numbers

Stores, manages, and recalls pairs of numbers.

Args:
  filename (symbol, optional)

Inlets:
  0 (INLET_TYPE): list Inserts Element, int Retrieves Element
  1 (INLET_TYPE): Sets Stored Element Value for Insertion

Outlets:
  0 (OUTLET_TYPE): Stored Element Output
  1 (OUTLET_TYPE): Index Output for next Message
  2 (OUTLET_TYPE): bang Output When next Message Reaches End

Messages: bang, int, float, clear, copy, cut, delete, dump, embed, find, goto, in1, interp, interptab, max, min, next, paste, print, read, select, set, undo, write
"""
funbuff = MaxObject("funbuff")

"""
function - Breakpoint function editor

Draw or store a set of x, y points as floating-point numbers. The output the entire function is useful as an input for line~. You can also get an interpolated y value for any x value.

Inlets:
  0 (INLET_TYPE): bang Outputs All, float Outputs Y at X, list Edits

Outlets:
  0 (OUTLET_TYPE): Interpolated Y (float) for Input X Interpolated Y (float) for Input X
  1 (OUTLET_TYPE): All Points in line Format
  2 (OUTLET_TYPE): dump Message Output (list)
  3 (OUTLET_TYPE): bang When Changed With Mouse

Messages: bang, int, float, list, clear, clearfix, clearsustain, color, copy, dump, fix, getfix, getsustain, lineout, listdump, (mouse), next, nth, paste, quantize_x, quantize_y, set, setcurve, setdomain, setrange, sustain, xyc

Attributes: annotation_name, autosustain, bgcolor, bordercolor, classic_curve, clickadd, clickmove, clicksustain, constrainpointchanges, cursor, cursorcolor, domain, grid, gridcolor, gridstep_x, gridstep_y, legend, linecolor, linethickness, mode, mousemode, mousereport, outputmode, param_connect, parameter_enable, parameter_mappable, pointalign, pointcolor, pointsize, range, shadowalpha, shadowblend, shadowproportion, shadowreflectionpoint, shadowsigned, snap2grid, style, sustaincolor, textcolor, zoom_x, zoom_y
"""
function = MaxObject("function")

"""
funnel - Tag data with its inlet number

Identifies the inlet of incoming data. It can be used to store values into a table or coll based on their source, or used to set a destination with an object such as spray.

Args:
  inlets (int, optional)
  offset (int, optional)

Messages: bang, int, float, list, anything, offset, set
"""
funnel = MaxObject("funnel")

"""
gamepad - Report gamepad controller events

Tracks and outputs the button, joystick, trigger, and sensor events from all connected gamepad controllers. Send a device control messages for haptic feedback rumble events and device led color with the senddevice message. The gamepad object is a lightweight wrapper for the Simple DirectMedia Layer 2.0 Library's gamepad implementation.

Inlets:
  0 (message): control messages

Outlets:
  0 (list): event message
  1 (list): device info for event (instance_id  device_index  controller_name  controller_type)
  2 (message): dumpoutlet

Messages: addmapping, addmappingfile, senddevice, sendinstance

Attributes: interval, rawdata
"""
gamepad = MaxObject("gamepad")

"""
gate - Pass input to an outlet

Args:
  outlets (int, optional)
  initial-state (int, optional)

Inlets:
  0 (INLET_TYPE): 0 Closes gate, Non-zero Opens Gate Outlet
  1 (INLET_TYPE): Incoming Gated Messages

Outlets:
  0 (OUTLET_TYPE): Output of Messages if Open

Messages: bang, int, float, next
"""
gate = MaxObject("gate")

"""
gestalt - Retrieve system information

Inlets:
  0 (INLET_TYPE): symbol Reports System Information

Outlets:
  0 (OUTLET_TYPE): Response to System Information Query
  1 (OUTLET_TYPE): Error Code Returned by Query

Messages: bang, anything, keys, symbol

Attributes: outputmode, selector
"""
gestalt = MaxObject("gestalt")

"""
getattr - Query object attributes

Provides a user interface to query attribute values from an object. You can also retrieve a list of all available attributes for the attached object.

Args:
  attribute (symbol, optional)

Inlets:
  0 (INLET_TYPE): Message in

Outlets:
  0 (OUTLET_TYPE): Attribute Value
  1 (OUTLET_TYPE): Connect to An Object
  2 (OUTLET_TYPE): dumpout

Messages: bang, getattrlist

Attributes: attr, listen, prefix
"""
getattr_ = MaxObject("getattr")

"""
grab - Intercept the output of another object

grab can send a message and extract the result from the receiving object.

		Note: The grab object cannot be used to communicate from a send to a receive between Max for Live devices.

Args:
  number-of-outlets (int, optional)
  receive-name (symbol, optional)

Inlets:
  0 (INLET_TYPE): Message to Send to Object

Outlets:
  0 (OUTLET_TYPE): Result of Remote Message
  1 (OUTLET_TYPE): Connect to Object That Will Receive Message

Messages: bang, int, float, list, anything, set
"""
grab = MaxObject("grab")

"""
greaterthan - Compare numbers for greater than condition

Compares two values to see if one value is greater than a second. Outputs a 1 if the number is greater than the comparison-number or 0 if it is less than or equal to it.

Args:
  initial-comparison-value (number, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left > Right

Messages: bang, int, float, in1, set
"""
greaterthan = MaxObject("greaterthan")

"""
greaterthaneq - Compare numbers for greater than or equal to condition

Compares two values to see if one value is greater than or equal to a second. Outputs a 1 if the number is greater than or equal to the comparison-number or 0 if it is less.

Args:
  comparison-value (number, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left >= Right

Messages: bang, int, float, in1, set

Attributes: fuzzy
"""
greaterthaneq = MaxObject("greaterthaneq")

"""
gswitch - Select output from two inlets

Inlets:
  0 (bang/int): Control Inlet (0=Left, 1=Right)
  1 (bang/int): Left Data Inlet
  2 (bang/int): Right Data Inlet

Outlets:
  0 (OUTLET_TYPE): Output of Selected Inlet

Messages: bang, int, float, list, anything, (mouse)

Attributes: annotation_name, bgcolor, color, inputs, int, param_connect, parameter_enable, parameter_mappable, style, switchcolor
"""
gswitch = MaxObject("gswitch")

"""
gswitch2 - Send input to one of two outlets

Switches the right inlet between two output pathways.

Inlets:
  0 (bang/int): Control Inlet (0=Left, 1=Right)
  1 (bang/int): Data inlet

Outlets:
  0 (OUTLET_TYPE): Left outlet
  1 (OUTLET_TYPE): Right outlet

Messages: bang, int, float, (mouse)

Attributes: annotation_name, bgcolor, color, int, outputs, param_connect, parameter_enable, parameter_mappable, style, switchcolor
"""
gswitch2 = MaxObject("gswitch2")

"""
hi - Human Interface device input (legacy)

Provides input from human interface peripherals (i.e. Trackpad, Keyboard, and others).

Args:
  device (symbol, optional)

Inlets:
  0 (message): (message) control messages

Outlets:
  0 (list): (list) element and value, both integers
  1 (message): (message) device enumeration output in menu format

Messages: bang, int, anything, clear, delta, ignore, info, menu, poll
"""
hi = MaxObject("hi")

"""
hid - Human Interface Device input (modern)

Provides input from human interface peripherals (i.e. Trackpad, Keyboard, and others). The hid object is similar to the legacy hi object but is cross platform with more information and capabilities. This includes providing usage pages, usages, types, and ranges for the information received. (see

			https://www.usb.org/hid

		for more information)

Args:
  device (symbol, optional)

Inlets:
  0 (message): control messages

Outlets:
  0 (list): hid report element info and value
  1 (message): device enumeration output in menu format
  2 (message): dumpoutlet

Messages: bang, int, anything, close, info, menu, poll

Attributes: exclusive
"""
hid = MaxObject("hid")

"""
hint - Display hint text

When you mouse over a hint, you'll see a message appear on the screen below the area defined by the hint. The hint object has a number of messages you can use to change its appearance.

Inlets:
  0 (INLET_TYPE): 1 Enables Hint, 0 Disables

Messages: int, float, (mouse), set

Attributes: delay, enabled
"""
hint = MaxObject("hint")

"""
histo - Create a histogram of numbers received

Records and outputs histogram data of the numbers it receives.

Args:
  size (int, optional)

Inlets:
  0 (INLET_TYPE): Numbers to Keep Track Of (and 'clear' Message)
  1 (INLET_TYPE): How Many of a Number

Outlets:
  0 (OUTLET_TYPE): Number Out
  1 (OUTLET_TYPE): Quantity of a Given Number

Messages: bang, int, clear, in1
"""
histo = MaxObject("histo")

"""
hover - Report object scripting names

Sends out the scripting names of any object over which the cursor is hovering.

Inlets:
  0 (INLET_TYPE): controls/attributes

Outlets:
  0 (OUTLET_TYPE): Reports the scripting name of the object over which the mouse is hovering at any given time.
  1 (OUTLET_TYPE): dumpout
  2 (OUTLET_TYPE): Scripting Name of Object Mouse Has Just Left
  3 (OUTLET_TYPE): none if Object Mouse Has Just Left Has No Scripting Name

Attributes: mode
"""
hover = MaxObject("hover")

"""
if - Conditional statement in if/then/else form

Evaluates input according to a conditional statement specified in an if-then-else form.

Args:
  if (symbol, required)
  then, else (symbol, required)
  $i1, $f1, $s1 (symbol, required)
  send (symbol, required)
  out2 (symbol, required)

Messages: bang, int, float, ft1, ft2, ft3, ft4, ft5, ft6, ft7, ft8, ft9, in1, in2, in3, in4, in5, in6, in7, in8, in9, set, symbol
"""
if_ = MaxObject("if")

"""
imovie - Play video

Plays a movie in a user-interface object within the patcher window.

Inlets:
  0 (INLET_TYPE): int Locates, start Plays, Many Other Commands

Outlets:
  0 (OUTLET_TYPE): Movie Time in Response To time Message
  1 (OUTLET_TYPE): Horiz Mouse Location When Clicked in Movie
  2 (OUTLET_TYPE): Vert Mouse Location When Clicked in Movie

Messages: bang, int, (drag), active, clear, dispose, duration, getduration, getrate, gettime, length, loadintoram, loop, loopend, looppoints, loopset, loopstart, matrix, (mouse), mute, next, nextmovie, palindrome, passive, pause, prev, quality, rate, rd, read, readany, rect, reload, resume, start, startat, stop, switch, time, timescale, vol

Attributes: autofit, border, moviedim, name
"""
imovie = MaxObject("imovie")

"""
incdec - Increment and decrement a value

Increment or Decrement a value. When connected to a number box, Click the upper half of the object to increment, click the lower half to decrement.

Inlets:
  0 (INLET_TYPE): Number to Inc/Dec

Outlets:
  0 (OUTLET_TYPE): Changed Number

Messages: bang, int, float, dec, inc, (mouse), set

Attributes: annotation_name, bgcolor, bordercolor, elementcolor, fgcolor, increment, param_connect, parameter_enable, parameter_mappable, style
"""
incdec = MaxObject("incdec")

"""
inlet - Receive messages from outside a patcher

Receive messages from outside the patcher wherever it is embedded. Each inlet object in a patcher will show up as an inlet at the top of an object box when the patch is used inside another patcher (as an object or a subpatch). Messages sent into such an inlet will be received by the inlet object in the subpatch.

Inlets:
  0 (INLET_TYPE): Outputs Incoming Messages to Patcher

Outlets:
  0 (OUTLET_TYPE): Outputs Incoming Messages to Patcher

Messages: (mouse)

Attributes: comment, cool, style, tricolor
"""
inlet = MaxObject("inlet")

"""
int - Store an integer value

int can store and output any given integer number.

Args:
  initial-value (number, optional)

Inlets:
  0 (INLET_TYPE): bang Outputs Value, int Sets and Outputs Value
  1 (INLET_TYPE): Set Value Without Output

Outlets:
  0 (OUTLET_TYPE): Value

Messages: bang, int, float, in1, send, set
"""
int_ = MaxObject("int")

"""
itable - Data table editor

Provides visual display of the table contents in your patcher window.

Args:
  tablename (symbol, required)

Inlets:
  0 (INLET_TYPE): int Outputs Value at Index, list Stores Value
  1 (INLET_TYPE): Sets Y Value for Next int at Left Inlet

Outlets:
  0 (OUTLET_TYPE): Outputs Requested Indices or Values
  1 (OUTLET_TYPE): Outputs bang When Table Window Edited

Messages: bang, int, float, list, clear, const, dump, fquantile, getbits, goto, handtool, in1, inv, length, linetool, load, max, min, (mouse), next, normal, penciltool, prev, quantile, read, refer, selecttool, send, set, setbits, sum, write

Attributes: annotation_name, autohint, bgcolor, bgcolor2, bordercolor, embed, legend, linecolor, name, notename, param_connect, parameter_enable, parameter_mappable, pointcolor, range, selectioncolor, setresizes, shadowalpha, shadowblend, shadowproportion, signed, size, style, textcolor, tool
"""
itable = MaxObject("itable")

"""
iter - Break a list into individual messages

Unpacks and outputs list contents one element at a time.

Inlets:
  0 (INLET_TYPE): list to be Unraveled

Outlets:
  0 (OUTLET_TYPE): Sequential Output of Incoming list

Messages: bang, int, float, list, anything
"""
iter_ = MaxObject("iter")

"""
itoa - Convert character codes to symbol

Convert a stream or list of up to 256 integer character codes into a symbol.

Inlets:
  0 (int, list): Integer list converted to symbol, bang outputs current symbol
  1 (int, list): Integer list converted and concatenated to current symbol
  2 (int, list): Integer list converted and sets contents of symbol

Outlets:
  0 (OUTLET_TYPE): Symbol output

Messages: bang, int, float, list, clear

Attributes: utf8
"""
itoa = MaxObject("itoa")

"""
jit.cellblock - Edit rows and columns of data

Provides storage, viewing and editing of two-dimensional data. The format is similar to the "grid" display tools found in many other development environments. The current cell location, format, display and contents within jit.cellblock can be set with the mouse or by using Max messages.

Inlets:
  0 (INLET_TYPE): anything in, bang outputs value
  1 (list): sync input

Outlets:
  0 (list): column, row and value
  1 (list): edit values for cell
  2 (list): sync output
  3 (list): dumpout

Messages: bang, list, append, cell, clear, col, deref, dump, jit_deref, jit_matrix, mode, plane, prepend, read, refer, refresh, row, rowblend, select, send, set, setwithoutdirty, signal, sync, text, write, writeagain

Attributes: automouse, bblend, bgcolor, border, bordercolor, colhead, cols, colwidth, datadirty, fblend, fgcolor, grid, gridlinecolor, hcellcolor, headercolor, hscroll, hsync, interval, just, neverdirty, outmode, precision, readonly, rowhead, rowheight, rows, savemode, sccolor, selmode, selsync, sgcolor, signalmode, signalusecols, stcolor, textcolor, vscroll, vsync
"""
jit_cellblock = MaxObject("jit.cellblock")

"""
join - Combine items into a list

Takes separate untyped items and combines them into an output list.

Args:
  inlets (int, required)

Inlets:
  0 (INLET_TYPE): Input (0)
  1 (INLET_TYPE): Input (1)

Outlets:
  0 (OUTLET_TYPE): Output

Messages: bang, int, float, list, anything, set

Attributes: triggers
"""
join = MaxObject("join")

"""
js - Execute Javascript (Legacy Engine)

Exposes the Javascript language (ECMAScript 5) and some Max specific extensions. The js object can be instantiated with a javascript filename or with numerical arguments to specify the number of outlets and inlets respectively. The default number of outlets and inlets are both 1.

Args:
  filename (symbol, optional)
  inlets-outlets (list, optional)
  jsarguments (list, optional)

Inlets:
  0 (INLET_TYPE): js: Inlet 0

Outlets:
  0 (OUTLET_TYPE): js: Outlet 0

Messages: bang, int, float, list, anything, autowatch, compile, delprop, editfontsize, getprop, loadbang, open, setprop, statemessage, wclose

Attributes: annotation_name, parameter_enable, parameter_mappable, template
"""
js = MaxObject("js")

"""
jstrigger - Execute Javascript instructions sequentially

The jstrigger object is similar to the trigger object, except that typed-in arguments within parentheses are passed to the Javascript evaluator. For more information on the Max implementation of Javascript, refer to the Javascript in Max manual. For complete information about Javascript itself, consult a reference book such as Javascript: The Definitive Guide by David Flanagan, published by O'Reilly.

Args:
  sequential-Javascript-instructions (symbol, number, required)

Inlets:
  0 (INLET_TYPE): Message Used for Actions

Messages: bang, int, float, list, anything
"""
jstrigger = MaxObject("jstrigger")

"""
jsui - Javascript user interfaces and graphics (Legacy Engine)

Provides an environment to make user interface elements using Javascript (ECMAScript 5). This provides all of the programming tools available in the js object, but also exposes the mgraphics and sketch drawing routines for visual output.

Inlets:
  0 (INLET_TYPE): none: Inlet 0

Outlets:
  0 (OUTLET_TYPE): none: Outlet 0

Messages: bang, int, float, list, anything, (drag), autowatch, compile, delprop, editfontsize, getprop, jsargs, jsfile, loadbang, (mouse), open, setprop, size, statemessage, wclose

Attributes: annotation_name, border, filename, jsarguments, nofsaa, param_connect, parameter_enable, parameter_mappable, template
"""
jsui = MaxObject("jsui")

"""
jweb - Web browser

The jweb object uses the cross-platform Chromium Embedded Framework (CEF) to host web pages within a Max UI object.

Inlets:
  0 (INLET_TYPE): Control Input

Outlets:
  0 (OUTLET_TYPE): Status and Javascript Output

Messages: anything, back, executejavascript, forward, gotohistory, (mouse), mute, read, readfile, reload

Attributes: disablefind, history, historysize, nonstandardoutletcalls, rendermode, url
"""
jweb = MaxObject("jweb")

"""
jweb~ - Web browser with audio output

The jweb~ object uses the cross-platform Chromium Embedded Framework (CEF) to host web pages within a Max UI object. Audio signals generated by the page are sent out the object's outlets.

Inlets:
  0 (INLET_TYPE): Control Input

Outlets:
  0 (signal): Audio Output
  1 (signal): Audio Output
  2 (signal): Status and Javascript Output

Messages: anything, back, executejavascript, forward, gotohistory, (mouse), mute, read, readfile, reload, signal

Attributes: disablefind, history, historysize, latency, rendermode, url
"""
jweb_tilde = MaxObject("jweb~")

"""
key - Report keyboard presses

Tracks and outputs the key-codes, ASCII values, and modifier-key values of key presses on the computer keyboard.

Outlets:
  0 (OUTLET_TYPE): ASCII Code of Key Pressed
  1 (OUTLET_TYPE): Platform-Specific Keyboard Code of Key Pressed
  2 (OUTLET_TYPE): Modifier Keys of Key Pressed
  3 (OUTLET_TYPE): Platform-Independent Keyboard Code of Key Pressed

Messages: (keyboard)
"""
key = MaxObject("key")

"""
keyup - Report key information on release

Tracks and outputs the key-codes, ASCII values, and modifier-key values of key releases on the computer keyboard.

Outlets:
  0 (OUTLET_TYPE): ASCII Code of Key Released
  1 (OUTLET_TYPE): Platform-Specific Keyboard Code of Key Released
  2 (OUTLET_TYPE): Modifier Keys of Key Released
  3 (OUTLET_TYPE): Platform-Independent Keyboard Code of Key Released

Messages: (keyboard)
"""
keyup = MaxObject("keyup")

"""
kslider - Output numbers from an onscreen keyboard

Outputs and displays note and velocity information using an on-screen keyboard.

Inlets:
  0 (INLET_TYPE): Displays Value Received
  1 (INLET_TYPE): Velocity Value Received

Outlets:
  0 (OUTLET_TYPE): Outputs Key Value Changed or Received
  1 (OUTLET_TYPE): 'Velocity' from Mouse Height on Key

Messages: bang, int, float, chord, clear, flush, ft1, in1, (mouse), set, size

Attributes: annotation_name, blackkeycolor, bordercolor, hkeycolor, inputmode, mode, offset, param_connect, parameter_enable, parameter_mappable, range, selectioncolor, style, whitekeycolor
"""
kslider = MaxObject("kslider")

"""
lcd - Display graphics (deprecated)

Inlets:
  0 (INLET_TYPE): Drawing Commands

Outlets:
  0 (OUTLET_TYPE): Mouse (X,Y) Location When Clicked
  1 (OUTLET_TYPE): Mouse Idle (X,Y) Position
  2 (OUTLET_TYPE): Mouse Down (1/0)
  3 (OUTLET_TYPE): Dump Output

Messages: anything, ascii, backsprite, brgb, clear, clearpicts, clearsprites, closesprite, color, deletepict, deletesprite, drawpict, drawsprite, font, framearc, frameoval, framepoly, framerect, frameroundrect, frgb, frontsprite, getpenloc, getpixel, getsize, hidesprite, line, linesegment, lineto, (mouse), move, moveto, oprgb, paintarc, paintoval, paintpoly, paintrect, paintroundrect, penmode, pensize, readpict, recordsprite, reset, scrollrect, setpixel, size, textface, tilepict, write, writepict

Attributes: bgtransparent, border, enablesprites, idle, local
"""
lcd = MaxObject("lcd")

"""
led - Color on/off button

Inlets:
  0 (INLET_TYPE): int Sets LED, bang Reverses It

Outlets:
  0 (OUTLET_TYPE): Outputs 1 or 0 When LED is Changed or Set

Messages: bang, int, float, (mouse), pict, set, toggle

Attributes: annotation_name, bgcolor, blinktime, offcolor, oncolor, param_connect, parameter_enable, parameter_mappable, style, thickness, useoffcolor
"""
led = MaxObject("led")

"""
lessthan - Compare numbers for less than condition

Compares two values to see if one value is less than a second. Outputs a 1 if the number is less than the comparison-number or 0 if it is equal to or greater than it.

Args:
  initial-comparison-value (number, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left < Right

Messages: bang, int, float, in1, set
"""
lessthan = MaxObject("lessthan")

"""
lessthaneq - Compare numbers as less than or equal to

Compares two values to see if one value is less than or equal to a second. Outputs a 1 if the number is less than or equal to the comparison-number or 0 if it is greater to it.

Args:
  intitial-value (number, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left <= Right

Messages: bang, int, float, in1, set

Attributes: fuzzy
"""
lessthaneq = MaxObject("lessthaneq")

"""
line - Generate timed ramp

Generate ramps and line segments from one value to another within a specified amount of time.

Args:
  initial (number, optional)
  grain (number, optional)

Inlets:
  0 (INLET_TYPE): Destination Value of Ramp
  1 (INLET_TYPE): Total Ramp Time in Milliseconds
  2 (INLET_TYPE): Time Grain in Milliseconds

Outlets:
  0 (OUTLET_TYPE): Ramp Output
  1 (OUTLET_TYPE): Signals End of Ramp

Messages: int, float, list, clock, pause, resume, set, stop

Attributes: compatmode, floatoutput, grain, maxpoints
"""
line = MaxObject("line")

"""
linedrive - Scale numbers exponentially

Scales number from one range to another with an exponential curve. Both the input and output ranges are expressed as single arguments representing the maximum value. The minimum values are the negative values of the ranges (argument * -1).

Args:
  input (number, required)
  output (number, required)
  curve (number, required)
  delay (int, required)

Messages: int, float, in1
"""
linedrive = MaxObject("linedrive")

"""
listbox - Display and output numbers, lists, and messages

The number object displays and outputs either integers, floats, lists of numbers, or any message, depending on its format. flonum is a synonym for number in Float format, listbox is a synonym for number in List format, and textbox is a synonym for number in Text format.

Inlets:
  0 (INLET_TYPE): Set Displayed Input and Repeat to Output

Outlets:
  0 (OUTLET_TYPE): Output Incoming or Entered Values
  1 (OUTLET_TYPE): bang When Tab Key Pressed

Messages: bang, int, float, list, anything, clear, max, min, (mouse), select, set

Attributes: annotation_name, bgcolor, bordercolor, cantchange, format, hbgcolor, htextcolor, htricolor, maximum, minimum, mousefilter, numdecimalplaces, outputonclick, param_connect, parameter_enable, parameter_mappable, style, textcolor, triangle, tricolor, triscale
"""
listbox = MaxObject("listbox")

"""
listfunnel - Index and output list elements

Outputs the elements of an incoming list in the format:

		[index] [element]

		for each element of the list.

Args:
  offset (int, optional)

Inlets:
  0 (INLET_TYPE): list in, Elements Will Be Output Preceded by Their Index

Outlets:
  0 (OUTLET_TYPE): list, (Index, Element)

Messages: int, float, list, anything, offset
"""
listfunnel = MaxObject("listfunnel")

"""
loadbang - Send a bang when a patcher is loaded

Outputs a bang automatically when the file is opened or when the patch is part of another file that is opened.

Inlets:
  0 (INLET_TYPE): Outputs bang When Patcher Window is Loaded

Outlets:
  0 (OUTLET_TYPE): Outputs bang When Patcher Window is Loaded

Messages: bang, (mouse), loadbang
"""
loadbang = MaxObject("loadbang")

"""
loadmess - Send a message when a patch is loaded

Outputs a message automatically when the file is opened, or when the patch is part of another file that is opened.

Args:
  message (symbol, required)

Inlets:
  0 (INLET_TYPE): Double-Click or bang to Re-Trigger Message

Outlets:
  0 (OUTLET_TYPE): Message Output When Patcher Is Loaded

Messages: bang, (mouse), set

Attributes: defer
"""
loadmess = MaxObject("loadmess")

"""
logand - Perform a logical AND

Compares one number to another and outputs a 1 if the two numbers are both non-zero or a 0 if either number is 0.

Args:
  comparison-value (int, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left && Right

Messages: bang, int, float, in1, set
"""
logand = MaxObject("logand")

"""
logor - Perform a logical OR

Compares one number to another and outputs a 1 if the either number is non-zero or a 0 if both number are 0.

Args:
  comparison-value (int, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left || Right

Messages: bang, int, float, in1, set
"""
logor = MaxObject("logor")

"""
makenote - Generate a note-on/note-off pair

Outputs a MIDI note-on message paired with a velocity value followed by a note-off message after a specified amount of time. This allows for generative MIDI output without having to manage note-off generation.

Args:
  velocity (number, optional)
  duration (number, optional)
  channel (number, optional)

Inlets:
  0 (INLET_TYPE): Pitch
  1 (INLET_TYPE): Velocity
  2 (INLET_TYPE): Duration

Outlets:
  0 (OUTLET_TYPE): Pitch Output
  1 (OUTLET_TYPE): Velocity Output (Note-on Velocity, Then 0)

Messages: int, float, list, anything, clear, clock, stop

Attributes: duration, repeatmode
"""
makenote = MaxObject("makenote")

"""
mappings - Utility object for Mappings

The mappings object allows you to enable and disable keyboard and MIDI mapping from your Max patcher, as well as some other useful utilities.

Messages: (mouse), key, midi, open, read, write

Attributes: file
"""
mappings = MaxObject("mappings")

"""
match - Watch for a message match, then output the message

Watches an incoming stream of ints, floats, symbols, lists, or messages, and outputs the stream after it has met the specification of its arguments.

Args:
  match-list (list, required)

Inlets:
  0 (INLET_TYPE): Input Sequence to be Matched

Outlets:
  0 (OUTLET_TYPE): list of Matched Sequence

Messages: int, float, list, anything, clear, set
"""
match = MaxObject("match")

"""
matrix - Event routing matrix

The matrix object controls the connections between inlets and outlets. You can route any inlet to any combination of outlets. Each connection has an associated gain factor and all values travelling through matrix can be scaled. It shares the same control protocol with the signal matrix~ object, but unlike matrix~, the outlets of the matrix object do not add the values of multiple inputs. matrix is best understood as a combination of gate and switch with many more features.

Args:
  number-of-inputs (int, optional)
  number-of-outputs (int, optional)
  default-connect-gain (float, optional)

Inlets:
  0 (list): Connect or Disconnect
  1 (list): Input 0
  2 (list): Input 1

Outlets:
  0 (dictionary): Current Connections
  1 (dictionary): Output 0
  2 (dictionary): Output 1

Messages: list, clear, connect, dictionary, disconnect, dumpconnections

Attributes: defaultgain, exclusive, inhibit, inrange, outscale, scalemode
"""
matrix = MaxObject("matrix")

"""
matrixctrl - Matrix switch control

Provides a user interface control containing a group of cells in a grid. Cell states can either be on/off or incremental steps. This object is especially useful for controlling the matrix~ object.

Inlets:
  0 (INLET_TYPE): bang Outputs All Cells, list Sets Cells

Outlets:
  0 (OUTLET_TYPE): Cell Value (Column, Row, Value)
  1 (OUTLET_TYPE): Row/Column Values As list

Messages: bang, list, bkgndpicture, cellpicture, clear, dictionary, disable, disablecell, enablecell, getcolumn, getrow, (mouse), readanybkgnd, readanycell, set

Attributes: active, annotation_name, autosize, bgcolor, bkgndpict, cellpict, clickedimage, clickvalue, color, columns, dialmode, dialtracking, elementcolor, horizontalmargin, horizontalspacing, imagemask, inactiveimage, invisiblebkgnd, one/column, one/matrix, one/row, parameter_enable, range, rows, scale, style, verticalmargin, verticalspacing
"""
matrixctrl = MaxObject("matrixctrl")

"""
maximum - Output the highest value

Watches an input stream for any numbers which are greater than its most recently set maximum. If the input value is less than or equal to the maximum, the maximum value is output. If the input value is greater, that value is output.

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): Compares Left and Right Inlets
  1 (INLET_TYPE): Value to be Compared

Outlets:
  0 (OUTLET_TYPE): Maximum of Left and Right Inlets
  1 (OUTLET_TYPE): Index of the Maximum Value

Messages: bang, int, float, list, ft1, in1
"""
maximum = MaxObject("maximum")

"""
maxurl - Make HTTP requests

maxurl is a wrapper around libcurl that can perform HTTP requests. Use it to fetch and post web content.	For more information on curl please refer to the

			Curl Docs Page.

		To learn about and practice HTTP Requests, check out

			httpbin.org.

Args:
  thread count (int, optional)

Inlets:
  0 (INLET_TYPE): Start an HTTP request (dictionary or message)
  1 (INLET_TYPE): Dictionary or Messages In

Outlets:
  0 (OUTLET_TYPE): Output dictionary
  1 (OUTLET_TYPE): Progress info (list)

Messages: abort, abortall, delete, dictionary, get, post, put, verbosity
"""
maxurl = MaxObject("maxurl")

"""
mc.function - Breakpoint function editor

Draw or store a set of x, y points as floating-point numbers. The output the entire function is useful as an input for line~. You can also get an interpolated y value for any x value.

Inlets:
  0 (INLET_TYPE): bang Outputs All, float Outputs Y at X, list Edits

Outlets:
  0 (OUTLET_TYPE): Interpolated Y (float) for Input X
  1 (OUTLET_TYPE): All Points in line Format
  2 (OUTLET_TYPE): dump Message Output (list)
  3 (OUTLET_TYPE): bang When Changed With Mouse
  4 (OUTLET_TYPE): Output Channel

Messages: bang, int, float, list, clear, clearchans, clearfix, clearsustain, color, copy, dump, fix, getfix, getsustain, lineout, listdump, (mouse), next, nth, paste, quantize_x, quantize_y, set, setcurve, setdomain, setrange, setvalue, sustain, target, xyc

Attributes: annotation_name, autosustain, bgcolor, bordercolor, candycane, candycane2, candycane3, candycane4, candycane5, candycane6, candycane7, candycane8, chans, classic_curve, clickadd, clickinactive, clickmove, clicksustain, constrainpointchanges, cursor, cursorcolor, displaychan, domain, grid, gridcolor, gridstep_x, gridstep_y, legend, linecolor, linethickness, mode, mousemode, mousereport, outputmode, param_connect, parameter_enable, parameter_mappable, pointalign, pointcolor, pointsize, range, shadowactive, shadowalpha, shadowblend, shadowproportion, shadowreflectionpoint, shadowsigned, snap2grid, style, sustaincolor, textcolor, zoom_x, zoom_y
"""
mc_function = MaxObject("mc.function")

"""
mc.getattr - Query object attributes  (multichannel)

Provides a user interface to query attribute values from an object. You can also retrieve a list of all available attributes for the attached object.

Args:
  attribute (symbol, optional)

Inlets:
  0 (INLET_TYPE): Message in

Outlets:
  0 (OUTLET_TYPE): Attribute Value
  1 (OUTLET_TYPE): Connect to An Object
  2 (OUTLET_TYPE): dumpout

Messages: bang, getattrlist

Attributes: attr, listen, prefix
"""
mc_getattr = MaxObject("mc.getattr")

"""
mc.line - Generate timed ramp  (multichannel)

Generate ramps and line segments from one value to another within a specified amount of time.

Args:
  initial (number, optional)
  grain (number, optional)

Inlets:
  0 (INLET_TYPE): Destination Value of Ramp
  1 (INLET_TYPE): Total Ramp Time in Milliseconds
  2 (INLET_TYPE): Time Grain in Milliseconds

Outlets:
  0 (OUTLET_TYPE): Ramp Output
  1 (OUTLET_TYPE): Signals End of Ramp

Messages: int, float, list, clock, pause, resume, set, stop

Attributes: compatmode, floatoutput, grain, maxpoints
"""
mc_line = MaxObject("mc.line")

"""
mean - Calculate a running average

Calculates the mean (average) of all the numbers it has received and outputs it.

Inlets:
  0 (int/float/list): (int/float/list) Numbers to be averaged

Outlets:
  0 (float): (float) Mean value
  1 (int): (int) Sample size

Messages: bang, int, float, list, clear
"""
mean = MaxObject("mean")

"""
menubar - Put up a custom menu bar

The menubar object provides control over the menu bar. It allows your patch to put up its own menus, and add items to standard File and Edit menus. When a menu item is chosen, the item number is sent out the outlet corresponding to the menu containing the item.

Args:
  display and behavior (int, optional)

Inlets:
  0 (INLET_TYPE): 1 Enables Menus, 0 Disables Menus

Outlets:
  0 (OUTLET_TYPE): Application Menu Item Index Chosen
  1 (OUTLET_TYPE): File Menu Item Index Chosen
  2 (OUTLET_TYPE): Edit Menu Item Index Chosen
  3 (OUTLET_TYPE): Window Menu Item Index Chosen

Messages: int, about, append, appendpermanent, apple, checkitem, closeitem, (mouse), edit, enableitem, end, file, item, markitem, menutitle, newitem, open, saveas
"""
menubar = MaxObject("menubar")

"""
message - Send any message

message displays and sends any given message with the capability to handle specified arguments.

Args:
  message (list, required)
  changeable-arg (symbol, required)
  comma (symbol, required)
  backslash (symbol, required)

Inlets:
  0 (INLET_TYPE): Trigger the Message, set Changes It
  1 (INLET_TYPE): Set the Message Without Output

Outlets:
  0 (OUTLET_TYPE): Message Result

Messages: bang, int, float, list, anything, append, (mouse), prepend, set, setargs, symbol

Attributes: bgcolor, bgcolor2, bgfillcolor, convertobj, dontreplace, gradient, style
"""
message = MaxObject("message")

"""
messageview - View a stream of messages

Use the messageview object to view a stream of messages.

Inlets:
  0 (INLET_TYPE): message input

Outlets:
  0 (OUTLET_TYPE): message output when double clicked

Messages: bang, int, float, list, anything, append, clear, scrolltoend

Attributes: autoscroll, bgcolor, stripe2, style, textcolor
"""
messageview = MaxObject("messageview")

"""
metro - Output a bang message at regular intervals

Acts as a metronome which outputs bang s at a regular, specified interval. This object uses the Max time format syntax, so the interval that the metro object uses can be either fixed or tempo-relative.

Args:
  interval (number, optional)

Inlets:
  0 (INLET_TYPE): Start/Stop Metronome
  1 (INLET_TYPE): Set Metronome Time Interval

Outlets:
  0 (OUTLET_TYPE): Output Ticks of Metronome

Messages: bang, int, float, list, anything, clock, stop

Attributes: active, autostart, autostarttime, defer, interval, quantize, transport
"""
metro = MaxObject("metro")

"""
midiflush - Send MIDI note-offs for hanging note-ons

Analyzes a raw MIDI stream (from midiin or seq), counting the number of note-ons received for each note and MIDI channel. When it is sent a bang, MIDI note-off messages are sent for any notes which have not been turned off.

Inlets:
  0 (INLET_TYPE): Raw MIDI Data, bang flushes held notes

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Data

Messages: bang, int, clear
"""
midiflush = MaxObject("midiflush")

"""
midiformat - Prepare data in the form of a MIDI message

Numbers received in the inlets are used as data for MIDI messages. The data is formatted into a complete MIDI message (with the status byte determined by the inlet) and sent out the outlet as individual bytes.

Args:
  initial-MIDI-channel-number (int, optional)
  initial-MIDI-channel-number (float, optional)

Outlets:
  0 (OUTLET_TYPE): MIDI Message Output
  1 (OUTLET_TYPE): midievent Message Output

Messages: int, float, list, in1, in2, in3, in4, in5, in7

Attributes: hires
"""
midiformat = MaxObject("midiformat")

"""
midiin - Output raw MIDI data

Listens to a specified MIDI port and output the raw MIDI data received.

Args:
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Messages

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name
"""
midiin = MaxObject("midiin")

"""
midiinfo - Fill a pop-up menu with MIDI device names

Outputs a series of messages which will set up a pop-up menu to a list of MIDI output devices when a bang is received. A number in midiinfo's right inlet creates a list of MIDI input devices.

Inlets:
  0 (INLET_TYPE): bang, int List Outputs, int Sets Menu Item
  1 (INLET_TYPE): bang, int List Inputs, int Sets Menu Item

Outlets:
  0 (OUTLET_TYPE): Connect to menu Object

Messages: bang, int, controllers

Attributes: autopollcontrollers, autopollinput, autopolloutput
"""
midiinfo = MaxObject("midiinfo")

"""
midiout - Transmit raw MIDI data

Transmits raw MIDI data to a specified port.

Args:
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): Raw MIDI Messages

Messages: int, float, list, anything, (mouse), port

Attributes: matchport, name
"""
midiout = MaxObject("midiout")

"""
midiparse - Interpret raw MIDI data

Separates raw MIDI bytes into standard message types. This object works particularly well formatting the output of the midiin and seq objects.

Inlets:
  0 (INLET_TYPE): MIDI Message Input

Outlets:
  0 (OUTLET_TYPE): Note-on and Note-off (list: Pitch, Velocity)
  1 (OUTLET_TYPE): Poly Key Pressure (list: Key, Value)
  2 (OUTLET_TYPE): Control Change (list: Controller Number, Value)
  3 (OUTLET_TYPE): Program Change
  4 (OUTLET_TYPE): Aftertouch
  5 (OUTLET_TYPE): Pitch Bend
  6 (OUTLET_TYPE): MIDI Channel
  7 (OUTLET_TYPE): midievent Message

Messages: bang, int, float

Attributes: hires
"""
midiparse = MaxObject("midiparse")

"""
minimum - Output the smallest value

Watches an input stream for any numbers which are less than its most recently set minimum. If the input value is greater than or equal to the minimum, the minimum value is output. If the input value is less, that value is output.

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): Compares Left and Right Inlets
  1 (INLET_TYPE): Value to be Compared

Outlets:
  0 (OUTLET_TYPE): Minimum of Left and Right Inlets
  1 (OUTLET_TYPE): Index of the Minimum Value

Messages: bang, int, float, list, ft1, in1
"""
minimum = MaxObject("minimum")

"""
minus - Subtract two numbers, output the result

Args:
  initial (number, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left - Right

Messages: bang, int, float, in1, set, list
"""
minus = MaxObject("minus")

"""
modifiers - Report modifier key presses

Polls and reports the state of the keyboard's modifier keys.

Args:
  rate (int, optional)

Inlets:
  0 (INLET_TYPE): bang Triggers Modifier Info

Outlets:
  0 (OUTLET_TYPE): 1 if Shift Key Down, 0 if Up
  1 (OUTLET_TYPE): 1 if Caps Lock Down, 0 if Up
  2 (OUTLET_TYPE): 1 if Option/Alt Key Down, 0 if Up
  3 (OUTLET_TYPE): 1 if Ctrl Key Down, 0 if Up
  4 (OUTLET_TYPE): 1 if Command/Fn Key Down, 0 if Up

Messages: bang, (keyboard), interval
"""
modifiers = MaxObject("modifiers")

"""
modulo - Divide two numbers, output the remainder

% takes two numbers, divides one by the other and outputs the remainder of the division.

Args:
  initial-value (int, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left % Right

Messages: bang, int, float, in1, set, list
"""
modulo = MaxObject("modulo")

"""
mousefilter - Gate messages with the mouse

Allows messages to pass only when the mouse button is up (un-clicked).

Inlets:
  0 (INLET_TYPE): Values To Be Filtered

Outlets:
  0 (OUTLET_TYPE): Output If Mouse Button Is Up

Messages: bang, int, float, list, anything
"""
mousefilter = MaxObject("mousefilter")

"""
mousestate - Report the mouse information

Provides button status and cursor position information about the mouse/cursor when the cursor is positioned within a Max patcher window. The mouse buttons are sampled every 50ms, while the mouse position is sampled every input bang.

Inlets:
  0 (INLET_TYPE): bang Triggers Mouse Location Reporte

Outlets:
  0 (OUTLET_TYPE): 1 if Button Down, 0 if Up
  1 (OUTLET_TYPE): Mouse Horizontal Location
  2 (OUTLET_TYPE): Mouse Vertical Location
  3 (OUTLET_TYPE): Mouse Delta Horizontal
  4 (OUTLET_TYPE): Mouse Delta Vertical
  5 (OUTLET_TYPE): 1 if Middle Button Down, 0 if Up
  6 (OUTLET_TYPE): 1 if Right Button Down, 0 if Up
  7 (OUTLET_TYPE): Mousewheel Delta Horizontal
  8 (OUTLET_TYPE): Mousewheel Delta Vertical
  9 (OUTLET_TYPE): Mousewheel Flags (smooth, inertial

Messages: bang, mode, (mouse), nopoll, poll, reset, zero
"""
mousestate = MaxObject("mousestate")

"""
movie - Play a movie in a window

Plays a movie in a separate window. All movie playback control is managed by messages to the object.

Args:
  filename (symbol, optional)

Inlets:
  0 (INLET_TYPE): int Locates, start Plays, Many Other Commands

Outlets:
  0 (OUTLET_TYPE): Movie Time in Response To time Message
  1 (OUTLET_TYPE): Horiz Mouse Location When Clicked in Movie
  2 (OUTLET_TYPE): Vert Mouse Location When Clicked in Movie

Messages: bang, int, (drag), active, clear, (mouse), dispose, duration, ff, getduration, getrate, gettime, length, loadintoram, loop, loopend, looppoints, loopset, loopstart, matrix, mute, next, nextmovie, open, palindrome, passive, pause, pos, prev, quality, rate, rd, read, readany, rect, reload, resume, rw, start, startat, stop, switch, time, timescale, toggleplay, vol, wclose, windowpos

Attributes: autofit, border, name
"""
movie = MaxObject("movie")

"""
mpeconfig - Configure a MIDI device that supports Multidimensional Polyphonic Expression (MPE) messages

The mpeconfig object is used to set up and configure zones for interpreting incoming MIDI to MPE devices.

Inlets:
  0 (INLET_TYPE): MPE Configuration Messages

Outlets:
  0 (OUTLET_TYPE): MIDI Output
  1 (OUTLET_TYPE): midievent Message Output

Messages: bang, clear, createzone, masterbendrange, notebendrange

Attributes: chanrange, masterchan
"""
mpeconfig = MaxObject("mpeconfig")

"""
mpeformat - Prepare data in the form of a Multidimensional Polyphonic Expression (MPE) MIDI message

Numbers received in the inlets from midiformat objects are routed as MPE-compatible MIDI messages. In addition, the object outputs mpeevent messages for use with instruments hosted by the vst~ object.

Args:
  channels (int, required)

Inlets:
  0 (INLET_TYPE): MIDI Input (Zone Master Channel)
  1 (INLET_TYPE): MIDI Input (for MIDI Output Channel 2)
  2 (INLET_TYPE): MIDI Input (for MIDI Output Channel 3)
  3 (INLET_TYPE): MIDI Input (for MIDI Output Channel 4)
  4 (INLET_TYPE): MIDI Input (for MIDI Output Channel 5)
  5 (INLET_TYPE): MIDI Input (for MIDI Output Channel 6)
  6 (INLET_TYPE): MIDI Input (for MIDI Output Channel 7)
  7 (INLET_TYPE): MIDI Input (for MIDI Output Channel 8)
  8 (INLET_TYPE): MIDI Input (for MIDI Output Channel 9)
  9 (INLET_TYPE): MIDI Input (for MIDI Output Channel 10)
  10 (INLET_TYPE): MIDI Input (for MIDI Output Channel 11)
  11 (INLET_TYPE): MIDI Input (for MIDI Output Channel 12)
  12 (INLET_TYPE): MIDI Input (for MIDI Output Channel 13)
  13 (INLET_TYPE): MIDI Input (for MIDI Output Channel 14)
  14 (INLET_TYPE): MIDI Input (for MIDI Output Channel 15)
  15 (INLET_TYPE): MIDI Input (for MIDI Output Channel 16)

Outlets:
  0 (OUTLET_TYPE): MIDI Output
  1 (OUTLET_TYPE): mpeevent Message Output

Messages: bang, int, float, midievent, mpeevent, reset

Attributes: chanrange, masterchan, zone
"""
mpeformat = MaxObject("mpeformat")

"""
mpeparse - Interpret raw MPE MIDI data

Separates raw Multidimensional Polyphonic Expression (MPE) MIDI bytes into standard message types.

Inlets:
  0 (INLET_TYPE): MIDI Message Input

Outlets:
  0 (OUTLET_TYPE): Note-on and Note-off (list: Pitch, Velocity)
  1 (OUTLET_TYPE): Poly Key Pressure (list: Key, Value)
  2 (OUTLET_TYPE): Control Change (list: Controller Number, Value)
  3 (OUTLET_TYPE): Program Change
  4 (OUTLET_TYPE): Aftertouch
  5 (OUTLET_TYPE): Pitch Bend
  6 (OUTLET_TYPE): Voice Number (-1 if Global)
  7 (OUTLET_TYPE): Zone First Channel
  8 (OUTLET_TYPE): Zone Index (-1 if Global)
  9 (OUTLET_TYPE): mpeevent Message (list: mpeevent, Zone First Channel, Zone Index, Voice Number, Channel Number, MIDI Message Number, Data)

Messages: bang, int, float, mpeevent

Attributes: hires, index, strict
"""
mpeparse = MaxObject("mpeparse")

"""
mtof - Convert a MIDI note number to frequency

Performs MIDI-note-number to frequency conversion. Frequency is reported in as a float in Hertz (Hz).

Inlets:
  0 (INLET_TYPE): MIDI note number input

Outlets:
  0 (OUTLET_TYPE): frequency output

Messages: int, float, list

Attributes: base, map, mapname, mid, ref, scale, scalename
"""
mtof = MaxObject("mtof")

"""
mtr - Record and sequence messages

Records messages and provides sequenced playback.

Args:
  tracks (int, optional)

Inlets:
  0 (INLET_TYPE): Control Input for All Tracks
  1 (INLET_TYPE): Track 1 Record/Control Input

Outlets:
  0 (OUTLET_TYPE): next Outputs list with Track and Duration
  1 (OUTLET_TYPE): Track 1 Play Output

Messages: bang, int, float, list, anything, addevent, clear, cleareventat, definelengthandstop, delay, deleteeventat, dictionary, dump, first, info, mute, next, play, playat, playatms, read, record, rewind, stop, timescale, touch, touchdisable, touchenable, unmute, write, writejson

Attributes: autostart, autostarttime, bindto, embed, length, loop, mode, nextmode, quantize, selection, speed, sync, trackspeed, transport
"""
mtr = MaxObject("mtr")

"""
multirange - Graphical function breakpoint editor

multirange is designed to work with mc.evolve~ and mc.gradient to set their breakpoints

Inlets:
  0 (INLET_TYPE): bang Outputs All, float Outputs Y at X, list Edits

Outlets:
  0 (OUTLET_TYPE): Interpolated Y1, Y2, Phase for Input X
  1 (OUTLET_TYPE): All Points in line Format
  2 (OUTLET_TYPE): dump Message Output (list)
  3 (OUTLET_TYPE): bang When Changed With Mouse

Messages: bang, int, float, list, clear, dump, listdump, (mouse)

Attributes: annotation_name, bgcolor, color, domain, elementcolor, legend, param_connect, parameter_enable, parameter_mappable, range, style, textcolor
"""
multirange = MaxObject("multirange")

"""
multislider - Display data as sliders or a scrolling display

Displays data as either an array of sliders or a scrolling display. When configured as sliders, the values are set (and output) as numeric lists. When configured as a scrolling display, multislider receives numbers, plots them, and scrolls the display area.

Inlets:
  0 (list): Sets Sliders

Outlets:
  0 (list): Values of All Sliders
  1 (int/float): Value of One Slider

Messages: bang, int, float, list, anything, copy, drawbars, drawlines, echo, fetch, interp, max, maximum, min, minimum, normalize, paste, pastereplace, peakreset, quantiles, range, scrollclear, select, set, setlist, setmax, setmin, sum

Attributes: annotation_name, bgcolor, border_bottom, border_left, border_right, border_top, bordercolor, candicane2, candicane3, candicane4, candicane5, candicane6, candicane7, candicane8, candycane, compatibility, contdata, drawpeaks, ghostbar, listresize, orientation, param_connect, parameter_enable, parameter_mappable, peakcolor, setminmax, setstyle, settype, shadowalpha, shadowblend, shadoworientation, shadowperbar, shadowproportion, signed, size, slidercolor, spacing, style, thickness
"""
multislider = MaxObject("multislider")

"""
mxj - Execute Java in Max

Execute Java classes as Max objects. The Java classes must be specially written for Max, but work identically within the JVM (Java Virtual Machine) on both Windows and Macintosh.

Args:
  Java-class (symbol, required)
  attributes (list, required)

Messages: bang, int, float, list, anything, (mouse), get, viewsource, zap
"""
mxj = MaxObject("mxj")

"""
next - Detect separation of messages

Sends a bang out its left outlet when an incoming message is not part of the same Max "event" as a previous message. (An "event" is a mouse click, key press, MIDI event, or tick of the scheduler.) Otherwise, next sends a bang out its right outlet.

Inlets:
  0 (INLET_TYPE): Any Message

Outlets:
  0 (OUTLET_TYPE): bang if Message is a New Event
  1 (OUTLET_TYPE): bang if Message is the Same Event

Messages: bang, int, float, anything
"""
next_ = MaxObject("next")

"""
nodes - Interpolate data graphically

The nodes object displays overlapping circular regions ("nodes") and outputs a list of interpolated weights based on the distance between a slider position and each node's center point. You can drag the slider with the mouse or set its position with a list of coordinates.

Inlets:
  0 (INLET_TYPE): list of 2 (0.-1.) floats gives x,y for interpolation

Outlets:
  0 (list): Interpolated weights for each active node
  1 (list): dumpout
  2 (list): Mouse Information (x, y, button)

Messages: bang, list, active, ad, clear, getactive, getad, getnode, getsize, getxy, (mouse), setnode, setnodead, setnodename, setsize

Attributes: annotation_name, bgcolor, bordercolor, candycane, candycane2, candycane3, candycane4, candycane5, candycane6, candycane7, candycane8, clickmoveinactive, disabledalpha, displayknob, dynamic, filternodeschanges, knobbordercolor, knobcolor, knobpict, knobsize, maxdynamicnodes, mousemode, nodecolor, nodenumber, nodesnames, nsize, param_connect, parameter_enable, parameter_mappable, pointcolor, style, textcolor, xplace, yplace
"""
nodes = MaxObject("nodes")

"""
notein - Receive MIDI note messages

Receives its input from a MIDI note-on or note-off message sent by a MIDI input device.

Args:
  port-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): Pitch
  1 (OUTLET_TYPE): Velocity
  2 (OUTLET_TYPE): MIDI Channel

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name
"""
notein = MaxObject("notein")

"""
noteout - Transmit MIDI note messages

Transmits note-on and note-off messages to a MIDI device.

Args:
  port-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): Pitch
  1 (INLET_TYPE): Velocity (0 for Note-off)
  2 (INLET_TYPE): MIDI Channel

Messages: int, float, anything, (mouse), in1, in2, port

Attributes: matchport, name
"""
noteout = MaxObject("noteout")

"""
notequals - Compare numbers for not-equal-to condition

Compares two values to see if one value is not equal to a second. Outputs a 1 if the number is not equal to the comparison-number or 0 if it is equal to it.

Args:
  initial-comparison-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left != Right

Messages: bang, int, float, in1, set, list

Attributes: fuzzy
"""
notequals = MaxObject("notequals")

"""
nrpnin - Output received NRPN values

Output the value from a specific Non-Registered Parameter Number (NRPN) and MIDI channel.

Args:
  param-channel (list, optional)

Inlets:
  0 (INLET_TYPE): Raw MIDI Bytes From midiin

Outlets:
  0 (OUTLET_TYPE): Parameter Value
  1 (OUTLET_TYPE): Parameter Change Delta
  2 (OUTLET_TYPE): Non-Registered Parameter Number
  3 (OUTLET_TYPE): MIDI Channel

Messages: int, list, set

Attributes: hires, permissive
"""
nrpnin = MaxObject("nrpnin")

"""
nrpnout - Format 14-bit MIDI NRPN messages

Format 14-bit MIDI Non-Registered Parameter Number messages to be transmitted using the midiout object.

Args:
  parameter-channel (list, optional)

Inlets:
  0 (INLET_TYPE): Parameter Value (0-16383 or list MSB LSB)
  1 (INLET_TYPE): Parameter Delta Change (0-127)
  2 (INLET_TYPE): Non-Registered Parameter Number (0-16383)
  3 (INLET_TYPE): MIDI Channel

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Bytes to midiout

Messages: bang, int, list, in1, in2, in3, set

Attributes: hires, running
"""
nrpnout = MaxObject("nrpnout")

"""
nslider - Output numbers from a notation display

nslider or "Note Slider" is a musical-notation-based integer value slider.

Inlets:
  0 (INLET_TYPE): Note Value In
  1 (INLET_TYPE): Velocity Value In

Outlets:
  0 (OUTLET_TYPE): Note Value Out
  1 (OUTLET_TYPE): Velocity Value Out

Messages: bang, int, float, list, anything, chord, clear, flush, ft1, in1, (mouse), set

Attributes: annotation_name, bgcolor, bordercolor, clefs, fgcolor, mode, param_connect, parameter_enable, parameter_mappable, rounded, staffs, style
"""
nslider = MaxObject("nslider")

"""
number - Display and output numbers, lists, and messages

The number object displays and outputs either integers, floats, lists of numbers, or any message, depending on its format. flonum is a synonym for number in Float format, listbox is a synonym for number in List format, and textbox is a synonym for number in Text format.

Inlets:
  0 (INLET_TYPE): Set Displayed Input and Repeat to Output

Outlets:
  0 (OUTLET_TYPE): Output Incoming or Entered Values
  1 (OUTLET_TYPE): bang When Tab Key Pressed

Messages: bang, int, float, list, anything, clear, max, min, (mouse), select, set

Attributes: annotation_name, bgcolor, bordercolor, cantchange, format, hbgcolor, htextcolor, htricolor, maximum, minimum, mousefilter, numdecimalplaces, outputonclick, param_connect, parameter_enable, parameter_mappable, style, textcolor, triangle, tricolor, triscale
"""
number = MaxObject("number")

"""
numkey - Interpret numbers typed on the keyboard

Args:
  format (float, optional)

Inlets:
  0 (INLET_TYPE): ASCII Key Codes From key

Outlets:
  0 (OUTLET_TYPE): Value of Number Typed When Entered
  1 (OUTLET_TYPE): Value of Number While It's Being Typed

Messages: bang, int, clear
"""
numkey = MaxObject("numkey")

"""
offer - Store one-time number pairs

Store two ints as an x, y pair, and access them by x value. When a pair is retrieved, it is deleted from the collection.

Inlets:
  0 (INLET_TYPE): X After Y Stores, X Alone Outputs Y, Deletes
  1 (INLET_TYPE): Y Value To Be Stored

Outlets:
  0 (OUTLET_TYPE): Y When Corresponding X Is Input

Messages: bang, int, clear, in1
"""
offer = MaxObject("offer")

"""
onebang - Gate bangs using a bang

Allows a bang in the left inlet to pass through ONLY if a bang has been received in the right inlet. After that, a bang in the left inlet will not get through again until a bang has been received again in the right inlet.

Args:
  initialization (int, optional)

Inlets:
  0 (bang/anything): (bang/anything) Only first bang gets through
  1 (bang/anything): (bang/anything) Set to let one bang through

Outlets:
  0 (bang): (bang) First bang
  1 (bang): (bang) All other bangs

Messages: bang, int, float, list, anything, stop
"""
onebang = MaxObject("onebang")

"""
onecopy - Prevent multiple copies of the same patcher from being opened

Use the onecopy object inside a patcher that you want to place in the extras folder for inclusion in the Extras menu. When the patcher's name is chosen using the Extras menu, its window will be brought to the front instead of opened a second time if it has already been loaded. The patch will be loaded if it is not currently open. The onecopy object cooperates with the Extras menu to ensure that only one copy of the patcher is opened at a time. However, opening the patcher containing a onecopy object by choosing Open... from the File menu will open additional copies.
"""
onecopy = MaxObject("onecopy")

"""
opendialog - Open a dialog to ask for a file or folder

Use the opendialog object to select a file of a specific type or folder from a standard dialog window. To choose a folder, use the "fold" type. opendialog reports the entire pathname of the file or folder chosen, which can be passed to any Max object after the word read or load.

Args:
  folder (symbol, optional)
  soundfile (symbol, optional)
  file-types (symbol, optional)

Inlets:
  0 (INLET_TYPE): bang Shows Dialog, symbol(s) Set File Types

Outlets:
  0 (OUTLET_TYPE): Complete Path of Chosen File
  1 (OUTLET_TYPE): bang When User Cancels

Messages: bang, anything, path, set, setpath, sound, types
"""
opendialog = MaxObject("opendialog")

"""
osc.codebox - Display OSC packets as Dictionaries

The osc.codebox object is a UI object for the display of OSC packets as dictionaries.

Inlets:
  0 (INLET_TYPE): OSC packet to display in dictionary format

Messages: FullPacket, clear, osc_packet

Attributes: bgcolor, editlocked, embed, linenumbers, linenumberwidth, margin, style, textcolor
"""
osc_codebox = MaxObject("osc.codebox")

"""
osc.packet - Store and recall an OSC packet

The osc.packet object stores and recalls OSC packets, and can convert them into different formats.

Inlets:
  0 (INLET_TYPE): OSC packet to store and output immediately
  1 (INLET_TYPE): OSC packet to store

Outlets:
  0 (OUTLET_TYPE): stored OSC packet

Messages: bang, FullPacket, clear, osc_packet

Attributes: outputformat
"""
osc_packet = MaxObject("osc.packet")

"""
outlet - Send messages out of a patcher

Show up as an outlet at the bottom of an object box when the patcher is used inside another patcher (as an object or a subpatch). Messages received by the outlet object will come out of the corresponding outlet in the subpatch's object box.

Inlets:
  0 (INLET_TYPE): Sends Messages Out Patcher's Outlet
  1 (INLET_TYPE): Sends Messages Out Patcher's Outlet

Messages: (mouse)

Attributes: comment, style, tricolor
"""
outlet = MaxObject("outlet")

"""
pack - Create a list

Combine items into an output list. The arguments determine the list format and types of the list elements. The number of inlets is based on the number of arguments.

Args:
  list-elements (any, optional)

Inlets:
  0 (INLET_TYPE): value for the first list element, causes output
  1 (INLET_TYPE): value for the second list element

Outlets:
  0 (OUTLET_TYPE): Output list

Messages: bang, int, float, list, anything, nth, send, set, symbol
"""
pack = MaxObject("pack")

"""
pak - Output a list when any element changes

The pak object (pronounced "pock") offers much of the functionality of pack, but outputs the entire list whenever input is received in any inlet.

Args:
  list-elements (any, optional)

Inlets:
  0 (INLET_TYPE): int to be Element 1 in the list
  1 (INLET_TYPE): int to be Element 2 in the list

Outlets:
  0 (OUTLET_TYPE): Combined list from Inputs

Messages: bang, int, float, list, anything, set
"""
pak = MaxObject("pak")

"""
panel - Colored background area

The panel object lets you create colored panels for use in creating user interfaces. The panel can be a variety of shapes, including circles, triangles, arrows, and rectangles with optional rounded corners. Shadows and gradients can also be used.

Inlets:
  0 (INLET_TYPE): Graphics commands

Messages: (mouse), size

Attributes: angle, arrow_orientation, bgcolor, bgfillcolor, border, bordercolor, drag_window, grad1, grad2, horizontal_direction, mode, rounded, shadow, shape, style, vertical_direction
"""
panel = MaxObject("panel")

"""
param - Define a polyphonic-compatible parameter in poly~

The param object appears as an attribute on the poly~ object, in addition to being published to the Parameter system, and can be attached to UI objects via the Parameter Connect feature. It currently only supports single numeric values (int/float), but not symbols nor lists.

		Parameters defined with the param object will only appear once in the Parameter Window for the top-level patcher containing the hosting poly~ object, regardless of how many voices of polyphony are configured, and changes will be propagated to all voices. This feature is useful for defining "monophonic" parameters which apply to every voice of a polyphonic structure (such as amplitude envelope attack).

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): messages or data in

Outlets:
  0 (OUTLET_TYPE): value out
  1 (OUTLET_TYPE): normalized value out

Messages: bang, int, float

Attributes: annotation_name, default, max, min
"""
param = MaxObject("param")

"""
param.osc - Control and report info about parameters using OSC.

Controls and reports info about parameters using OpenSoundControl (OSC).

Inlets:
  0 (INLET_TYPE): OSC in

Outlets:
  0 (OUTLET_TYPE)

Messages: bang, anything, FullPacket, allparams, info, osc_packet, param, set

Attributes: auto, outputformat, outputmode
"""
param_osc = MaxObject("param.osc")

"""
past - Notify when a threshold is passed

Sends a bang when a number rises above a certain specified value.

Args:
  list (list, required)
  number (int, required)

Inlets:
  0 (INLET_TYPE): Input to be Synchronized

Outlets:
  0 (OUTLET_TYPE): bang When Numbers Rise Above Specified Value

Messages: int, float, list, clear, set
"""
past = MaxObject("past")

"""
patcher - Create a subpatch within a patch

Creates patches within patches.

Args:
  subpatch (symbol, optional)

Inlets:
  0 (INLET_TYPE)

Attributes: accentcolor, assistshowspatchername, bgcolor, bgfillcolor, bottomtoolbarpinned, boxanimatetime, bubble_bgcolor, bubble_outlinecolor, clearcolor, cluebar, color, commentary, defaultfocusbox, description, digest, editing_bgcolor, elementcolor, enablehscroll, enabletransparentbgwithtitlebar, enablevscroll, filepath, fontface, fontname, fontsize, globalpatchername, gridonopen, gridsize, gridsnaponopen, helpsidebarclosed, integercoordinates, isolateaudio, lefttoolbarpinned, locked_bgcolor, objectsnaponopen, openinpresentation, openrect, openrectmode, oscdefer, oscparamenableddefault, oscprefix, oscprefixmode, oscqueryenable, oscreceivemode, oscreceivequantize, oscreceivethreshold, oscreceiveudpport, oscsendmode, oscsendthreshold, oscsendudpaddr, oscsendudpport, oscuseparamprefix, oscvaluemode, patchlinecolor, righttoolbarpinned, selectioncolor, showcommentary, showontab, stripecolor, style, subpatcher_template, syntax_attrargcolor, syntax_attributecolor, syntax_objargcolor, syntax_objectcolor, taborientation, tags, tallnewobj, textcolor, textcolor_inverse, textjustification, title, toolbarvisible, toptoolbarpinned, workspacedisabled, external_mpe_tuning_enabled, is_mpe, latency, minimum_live_version, minimum_max_version, platform_compatibility
"""
patcher = MaxObject("patcher")

"""
patcherargs - Retrieve patcher arguments

Retrieves patcher arguments and parses attribute style arguments. "normal" arguments are sent out the left outlet (first), and attribute style arguments are sent out the right outlet (second).

Args:
  defaults (int, symbol, float, required)

Inlets:
  0 (bang): (bang) trigger output

Outlets:
  0 (list): (list) normal arguments
  1 (list): (list) attribute arguments

Messages: bang, (mouse)
"""
patcherargs = MaxObject("patcherargs")

"""
pattr - Provide an alias with a named data wrapper

Stores its own data, or binds to another object to share its contents with other pattr-based objects (such as pattrstorage). Can be used for data routing or preset creation.

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): messages or data in

Outlets:
  0 (OUTLET_TYPE): attribute value
  1 (OUTLET_TYPE): bindto connection
  2 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, assign, (mouse), dictionary, init

Attributes: annotation_name, autorestore, bindto, default_active, default_interp, default_priority, dirty, initial, invisible, max, min, parameter_enable, parameter_mappable, thru, type
"""
pattr = MaxObject("pattr")

"""
pattrforward - Send any message to a named object

Routes messages or selects new message routing destinations according to the messages it receives. You can also use the pattrforward object to route messages directly to a specific inlet of an object exposed by pattr or autopattr objects, and also send messages directly to a subpatcher, abstraction or bpatcher.

Args:
  target (symbol, optional)

Inlets:
  0 (INLET_TYPE): messages or data in

Outlets:
  0 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, (mouse)

Attributes: send
"""
pattrforward = MaxObject("pattrforward")

"""
pattrhub - Access all pattr objects in a patcher

Centralizes communication with all pattr objects in a patcher.

Inlets:
  0 (INLET_TYPE): messages in

Outlets:
  0 (OUTLET_TYPE): passout
  1 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, getattributes, getstate

Attributes: bound, patcher
"""
pattrhub = MaxObject("pattrhub")

"""
pattrmarker - Provide pattr communication between patchers

The pattrmarker object associates a patcher with a global name, which can be used when looking up named objects. This permits, among other conveniences, name lookup and communication between two or more independent patcher hierarchies.

Args:
  name (symbol, required)

Inlets:
  0 (INLET_TYPE): set name

Outlets:
  0 (OUTLET_TYPE): dumpout

Messages: getmarkerlist, reveal

Attributes: invisible, name
"""
pattrmarker = MaxObject("pattrmarker")

"""
pattrstorage - Save and recall pattr presets

View and modify client object data, and store or recall presets.

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): int, float, messages

Outlets:
  0 (OUTLET_TYPE): dumpout

Messages: int, float, anything, active, alias, clear, client_close, clientwindow, copy, (mouse), delete, dump, fade, fillempty, getactive, getalias, getclientlist, getcurrent, getedited, getinterp, getlockedslots, getpriority, getslotlist, getslotname, getslotnamelist, getstoredvalue, getsubscriptionlist, grab, insert, interp, locate, lock, lockall, priority, purge, read, readagain, recall, recallmulti, remove, renumber, resolvealias, setall, setstoredvalue, slotname, storage_close, storagewindow, store, storeagain, storenext, subscribe, unsubscribe, write, writeagain, writejson, writexml

Attributes: activewritemode, annotation_name, autopattr_vis, autorestore, autowatch, backupmode, changemode, client_rect, dirty, fileusagemode, flat, greedy, notifymode, outputmode, parameter_enable, savemode, storage_rect, subscribemode, writefilter
"""
pattrstorage = MaxObject("pattrstorage")

"""
pcontrol - Open and close subwindows

Serves as a remote control for patcher/subpatcher windows and functions.

Inlets:
  0 (INLET_TYPE): Control Message to Send to Patcher Window

Outlets:
  0 (OUTLET_TYPE): Connect to Inlet of Patcher to be Controlled

Messages: close, help, load, loadunique, open, shroud, shroudunique
"""
pcontrol = MaxObject("pcontrol")

"""
peak - Output larger numbers

Compares a number to a previous peak-value and, if larger, it is sent out the output while the new peak-value is set to that number.

Args:
  format (float, optional)

Inlets:
  0 (INLET_TYPE): Find the highest number (the peak)
  1 (INLET_TYPE): Sets the peak

Outlets:
  0 (OUTLET_TYPE): The highest number so far
  1 (OUTLET_TYPE): Goes high for a new peak
  2 (OUTLET_TYPE): Goes high for a number that isn't a new peak

Messages: bang, int, float, ft1, in1, list
"""
peak = MaxObject("peak")

"""
pgmin - Receive MIDI program changes

Args:
  port-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): Program Change
  1 (OUTLET_TYPE): MIDI Channel

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name, zerobased
"""
pgmin = MaxObject("pgmin")

"""
pgmout - Send MIDI program changes

Args:
  port-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): Program Change
  1 (INLET_TYPE): MIDI Channel

Messages: int, float, anything, (mouse), in1, port, list

Attributes: matchport, name, zerobased
"""
pgmout = MaxObject("pgmout")

"""
pictctrl - Picture-based control

Creating buttons, switches, knobs, and other controls using images from a picture file for its appearance.

Inlets:
  0 (INLET_TYPE): int (0-1) Sets Value, read Opens File

Outlets:
  0 (OUTLET_TYPE): Button or Dial Value

Messages: bang, int, float, (drag), link, (mouse), picture, read, readany, set

Attributes: active, annotation_name, clickedimage, clickincrement, clip, degrees, frames, imagemask, inactiveimage, mode, multiplier, name, offset, param_connect, parameter_enable, parameter_mappable, range, ratio, snap, threshold, trackcircular, trackhorizontal, tracking, trackvertical
"""
pictctrl = MaxObject("pictctrl")

"""
pictslider - Picture-based slider control

A slider control that uses pictures in external files for its appearance. It uses two pictures--one for the "knob" and one for the background over which the knob moves. The pictslider object has default pictures that are used if you do not want to supply pictures of your own, but its intended use is creating controls with customized appearances.

Inlets:
  0 (INLET_TYPE): int: Horizontal Value; list: Both Values
  1 (INLET_TYPE): int: Vertical Value

Outlets:
  0 (OUTLET_TYPE): Horizontal Value
  1 (OUTLET_TYPE): Vertical Value

Messages: bang, int, float, list, (drag), bkgndpicture, knobpicture, (mouse), readanybkgnd, readanyknob, set, track

Attributes: active, annotation_name, bgcolor, bkgnddrag, bkgndpict, bkgndsize, bottommargin, bottomvalue, clickedimage, color, elementcolor, horizontaltracking, imagemask, inactiveimage, invisiblebkgnd, jump, knobpict, leftmargin, leftvalue, movehorizontal, movevertical, param_connect, parameter_enable, parameter_mappable, rightmargin, rightvalue, scaleknob, style, topmargin, topvalue, verticaltracking
"""
pictslider = MaxObject("pictslider")

"""
pipe - Delay numbers, lists or symbols

Delays numbers, lists of numbers or symbols. pipe uses the Max time format syntax, so the delay interval can be either fixed or tempo-relative.

Args:
  initialization (list, optional)

Inlets:
  0 (INLET_TYPE): Anything to be Delayed
  1 (INLET_TYPE): Set Delay Time

Outlets:
  0 (OUTLET_TYPE): Delayed Output

Messages: bang, int, float, list, anything, clear, flush, stop

Attributes: clock, delaytime, quantize
"""
pipe = MaxObject("pipe")

"""
playbar - Control video or audio file playback

Interacts with movie, imovie, sfplay~ or jit.movie to control the playback, position and looping of the media

Inlets:
  0 (INLET_TYPE): Commands For Controller and Its Movie

Outlets:
  0 (OUTLET_TYPE): Connect to Movie to be Controlled
  1 (OUTLET_TYPE): Command Numbers

Messages: bang, loop, (mouse), palindrome

Attributes: bgcolor, color, disabledcolor, hideloop, hiderwff, refreshrate, selectioncolor, style
"""
playbar = MaxObject("playbar")

"""
plus - Add two numbers, output the result

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left + Right

Messages: bang, int, float, in1, set, list
"""
plus = MaxObject("plus")

"""
poltocar - Convert polar to cartesian coordinates

Converts a polar-coordinate pair consisting of distance and angle values into a cartesian-coordinate pair consisting of real and imaginary values.

Inlets:
  0 (INLET_TYPE): amplitude/alpha input
  1 (INLET_TYPE): phase/theta input

Outlets:
  0 (OUTLET_TYPE): real/x output
  1 (OUTLET_TYPE): imaginary/y output

Messages: bang, int, float
"""
poltocar = MaxObject("poltocar")

"""
poly - Allocate notes to different voices

Provides polyphonic voice-allocation by allocating data to different individual voices.

Args:
  voices (number, optional)
  steal-mode (int, optional)

Inlets:
  0 (INLET_TYPE): Pitch or list with Pitch and Velocity
  1 (INLET_TYPE): Velocity

Outlets:
  0 (OUTLET_TYPE): Voice Number
  1 (OUTLET_TYPE): Pitch
  2 (OUTLET_TYPE): Velocity
  3 (OUTLET_TYPE): Overflow Notes

Messages: int, in1, stop
"""
poly = MaxObject("poly")

"""
polyin - Received MIDI poly pressure

Args:
  port-channel (list, required)
  key-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): Key Pressure
  1 (OUTLET_TYPE): Key Number
  2 (OUTLET_TYPE): MIDI Channel

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name
"""
polyin = MaxObject("polyin")

"""
polymidiin - Output Multidimensional Polyphonic Expression (MPE) MIDI data

The polymidiin object is used inside a poly~ instance. It outputs MPE MIDI data.

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Messages
"""
polymidiin = MaxObject("polymidiin")

"""
polyout - Send MIDI poly pressure

Args:
  port-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): Key Pressure
  1 (INLET_TYPE): Key Number
  2 (INLET_TYPE): MIDI Channel

Messages: int, float, anything, (mouse), in1, in2, port, list

Attributes: matchport, name
"""
polyout = MaxObject("polyout")

"""
pong - Range limiting

Use the pong object to clip, fold, or wrap its input within the range of a low value and a high value.

Args:
  low-value (float, optional)
  high-value (float, optional)

Inlets:
  0 (INLET_TYPE): Float Input
  1 (INLET_TYPE): (float) Lo val
  2 (INLET_TYPE): (float) Hi val

Outlets:
  0 (signal): (float) Output

Messages: int, float, list

Attributes: mode, range
"""
pong = MaxObject("pong")

"""
pow - Computes input to the nth power

Raises the base value (set in the left inlet) to the power of the exponent (set in the right inlet).

Args:
  exponent (int, float, optional)

Inlets:
  0 (INLET_TYPE): Raise X...
  1 (INLET_TYPE): ...To Y

Outlets:
  0 (OUTLET_TYPE): Result of X ^ Y

Messages: bang, int, float, list, ft1
"""
pow_ = MaxObject("pow")

"""
prepend - Add a message in front of input

Prepends any input messages with a stored message.

Args:
  message (symbol, required)

Inlets:
  0 (INLET_TYPE): Message to be Followed by Arguments

Outlets:
  0 (OUTLET_TYPE): Resulting Prepended Message

Messages: bang, int, float, list, anything, set
"""
prepend = MaxObject("prepend")

"""
preset - Store and recall settings

preset can store and recall the parameters of any specified user interface objects at the click of a mouse. It can also be used as a user interface link to a named pattrstorage object.

Inlets:
  0 (INLET_TYPE): Store or Recall Presets

Outlets:
  0 (OUTLET_TYPE): Connect to Objects to Include in a Preset
  1 (OUTLET_TYPE): Preset Number When Recalled
  2 (OUTLET_TYPE): Connect to Objects to Exclude From a Preset
  3 (OUTLET_TYPE): Preset Number When Stored
  4 (OUTLET_TYPE): Connect to Object to Capture Attributes

Messages: bang, int, float, list, anything, clear, clearall, (mouse), name, read, recallmulti, setname, store, write

Attributes: active1, active2, bgcolor, bordercolor, bubblesize, circlecolor, clicked1, clicked2, dirty, embed, emptycolor, ignoreconnected, ignoreemptyinterp, margin, paramonly, pattrstorage, showtrack, spacing, stored1, stored2, style, textcolor
"""
preset = MaxObject("preset")

"""
print - Print any message in the Max Console

print will print any input into the Max Console for debugging, messaging, or analysis purposes. Selecting "Show Output" from the Object Action Menu opens the sidebar Max console and filters output to only show posts from the print object.

Args:
  identifier (any, optional)

Inlets:
  0 (INLET_TYPE): Anything to be Printed in Max Console

Messages: bang, int, float, list, anything, array, (mouse), dictionary, string

Attributes: bettersymquotes, deltatime, floatprecision, level, popup, time
"""
print_ = MaxObject("print")

"""
prob - Create a weighted transition table

Creates a table of weighted numeric transitions. A bang message causes the weighting, and a potential transition, to be calculated.

Inlets:
  0 (INLET_TYPE): bang Chooses State, list Assigns Transition

Outlets:
  0 (OUTLET_TYPE): State Chosen (int)
  1 (OUTLET_TYPE): bang When In Stuck State

Messages: bang, int, list, clear, dump, reset

Attributes: embed
"""
prob = MaxObject("prob")

"""
project

Attributes: amxdtype, autolocalize, autoorganize, browsertext, devpath, devpathtype, hideprojectwindow, includepackages, readonly, showdependencies, sortmode
"""
project = MaxObject("project")

"""
pv - Share data within a patch hierarchy

Creates a value that is shared between objects, but only within a single patcher. Unlike the value object, pv objects that share the same name only share the same value if they are in the same patcher, or one of its subpatches.

Args:
  name (symbol, required)
  message (any, optional)

Inlets:
  0 (INLET_TYPE): bang Outputs, Anything Else Sets Value

Outlets:
  0 (OUTLET_TYPE): Value Shared by All Local pv a Objects

Messages: bang, int, float, list, anything, status, symbol
"""
pv = MaxObject("pv")

"""
pvar - Connect to a named object in a patcher

The pvar object lets you build user interfaces in one part of your patcher that are associated with the "process" part in another part of the patcher. Unlike the send and receive objects, pvar does not work globally; the pvar object and its associated object must be in the same patcher. To set an object's name, select the object, open the inspector, then enter a name under Name > Scripting Name. The name cannot be a number, although it can contain numbers.

Args:
  object-name (symbol, optional)
  number-of-outlets (int, optional)

Inlets:
  0 (INLET_TYPE): Messages for Object (Disconnected)

Outlets:
  0 (OUTLET_TYPE): Object (Disconnected) output 1 (Not In Use)

Messages: bang, int, float, list, anything, loadbang, setname
"""
pvar = MaxObject("pvar")

"""
qlim - Queue-based message limiter

Slows down throughput of messages. Limiting is based on a minimum amount of time between passed message.

Args:
  minimum (int, required)

Inlets:
  0 (INLET_TYPE): Incoming Continuous Value
  1 (INLET_TYPE): Sets Output Interval to Send Input

Outlets:
  0 (OUTLET_TYPE): Speed-limited Output of Incoming Values

Messages: bang, int, float, list, anything

Attributes: defer, quantize, threshold, usurp
"""
qlim = MaxObject("qlim")

"""
qlist - Store a collection of messages

Stores a collection of timed or untimed "cues" in the form of messages which can be sent either out its outlet or remotely to various receive objects in your patch.

Inlets:
  0 (INLET_TYPE): Control Messages to qlist

Outlets:
  0 (OUTLET_TYPE): Direct Message Output
  1 (OUTLET_TYPE): bang on End of Cue List
  2 (OUTLET_TYPE): bang When File Is Read

Messages: bang, append, clear, (mouse), fwd, insert, next, open, read, rewind, set, stop, tempo, wclose, write
"""
qlist = MaxObject("qlist")

"""
qmetro - Queue-based metronome

Outputs bang messages at a set interval. Similar to metro, qmetro will drop output if the processing queue is overloaded.

Args:
  interval (number, optional)

Inlets:
  0 (INLET_TYPE): Start/Stop Metronome
  1 (INLET_TYPE): Set Metronome Time Interval

Outlets:
  0 (OUTLET_TYPE): Output Ticks of Metronome

Messages: bang, int, float, list, anything, clock, reset, stop

Attributes: active, autostart, autostarttime, defer, interval, quantize, transport
"""
qmetro = MaxObject("qmetro")

"""
quickthresh - Fast chord detection

Combines numbers when they are received close together. quickthresh is a faster, low-latency alternative to thresh that is optimized for chord detection.

Args:
  threshold (number, optional)
  fudge (number, optional)
  extension (number, optional)

Inlets:
  0 (INLET_TYPE): Numbers to be Gathered Into list
  1 (INLET_TYPE): First Arrival Threshold (ms.)
  2 (INLET_TYPE): Fudge Factor Threshold (ms.)
  3 (INLET_TYPE): Threshold Extension (ms.)

Outlets:
  0 (OUTLET_TYPE): List of Gathered Values

Messages: bang, int, float, ft1, ft2, ft3, in1, in2, in3, set
"""
quickthresh = MaxObject("quickthresh")

"""
radiogroup - Radio button or check box

Provides a user interface for option selection. There are two modes of operation: radio button and checkbox.

Inlets:
  0 (INLET_TYPE): Flags and Commands

Outlets:
  0 (OUTLET_TYPE): Flags Out

Messages: bang, int, list, disableitem, enableitem, (mouse), set

Attributes: activecolor, annotation_name, bgcolor, elementcolor, enabled, flagmode, inactive, inactivecolor, itemtype, offset, param_connect, parameter_enable, parameter_mappable, selection, shape, size, style
"""
radiogroup = MaxObject("radiogroup")

"""
random - Generate a random number

Outputs random numbers within the range between 0 and 1 less than the argument specified.

Args:
  range (int, float, optional)
  seed (int, optional)

Inlets:
  0 (INLET_TYPE): Causes Random Number Output
  1 (INLET_TYPE): Causes Random Number Output

Outlets:
  0 (OUTLET_TYPE): Random Number Output

Messages: bang, int, float, list, reset

Attributes: classic, cycle, floatoutput, range, seed
"""
random = MaxObject("random")

"""
rdiv - Divide input from a number

Divides a number by the incoming value. Functions like the / object, but the inlets' functions are reversed.

Args:
  divisor (number, optional)

Inlets:
  0 (INLET_TYPE): Right Operand in Left Inlet!
  1 (INLET_TYPE): Left Operand in Right Inlet!

Outlets:
  0 (OUTLET_TYPE): Quotient Out

Messages: bang, int, float, ft1, in1
"""
rdiv = MaxObject("rdiv")

"""
receive - Receive messages without patch cords

Receives and outputs messages from send objects sharing the same name. This allows you to send any kind of message between Patcher windows or within a window without using patch cords. A receive object can be instantiated with its short-form: the letter "r".

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): set Changes Receive Source (currently not assigned)

Outlets:
  0 (OUTLET_TYPE): Message Received from send

Messages: (mouse), set
"""
receive = MaxObject("receive")

"""
regexp - Use regular expressions to process input

With regexp, it's possible to use PERL-compatible regular expressions (PCRE) to match or make substitutions within symbols and lists.

Args:
  expression (symbol, optional)
  substitution (symbol, optional)

Inlets:
  0 (INLET_TYPE): re, substitute, or text in

Outlets:
  0 (OUTLET_TYPE): substitutions
  1 (OUTLET_TYPE): capture groups
  2 (OUTLET_TYPE): substrings
  3 (OUTLET_TYPE): unmatched
  4 (OUTLET_TYPE): dumpout

Messages: int, float, list, anything

Attributes: binary, legacyoutputorder, re, substitute, tosymbol
"""
regexp = MaxObject("regexp")

"""
relativepath - Convert an absolute to a relative path

Converts an absolute path to one that is relative to the Max application folder.

Inlets:
  0 (INLET_TYPE): Absolute Path

Outlets:
  0 (OUTLET_TYPE): Path Relative to Max Application Folder

Messages: anything
"""
relativepath = MaxObject("relativepath")

"""
repl - Send, receive and output Max Console commands

The repl object creates a named destination ("target") for commands you type into the Max Console window, creating a way to control patchers with a command line interface. (REPL is an acronym for Read Evaluate Print Loop.) Targets are either global -- receiving commands from any Max Console window -- or local to the patcher for which a Max Console is in the right sidebar. You can send a response to a command back to the user by sending any message to the inlet of repl.

		You can also use repl as a way to evalate commands as if they were typed into the Max Console window as well as print to the console.

Inlets:
  0 (INLET_TYPE): Evaluate and/or Print Messages to the Max Console
  1 (INLET_TYPE)

Outlets:
  0 (OUTLET_TYPE): Message to Target
  1 (OUTLET_TYPE): Target Name + Message
  2 (OUTLET_TYPE): Command Result

Messages: anything, array, string

Attributes: defer, exec, global, mirror, name, print
"""
repl = MaxObject("repl")

"""
rminus - Subtraction object (inlets reversed)

Subtracts the input from a specified value The !- object functions just like the - object, but the inlets' functions are reversed.

Args:
  value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Right Operand in Left Inlet!
  1 (INLET_TYPE): Left Operand in Right Inlet!

Outlets:
  0 (OUTLET_TYPE): Difference Out

Messages: bang, int, float, ft1, in1
"""
rminus = MaxObject("rminus")

"""
round - Round to a value

Calculates and outputs an integer multiple of any given number.

Args:
  multiple (number, optional)

Inlets:
  0 (int/float/list): Value(s) to be Rounded
  1 (int/float): Round to nearest this

Outlets:
  0 (OUTLET_TYPE): Rounded Value(s)

Messages: int, float, list, anything

Attributes: nearest
"""
round_ = MaxObject("round")

"""
route - Select outlet based on input matching

Tries to match a message's first argument to the route object's own arguments. The rightmost outlet passes any message that matched no other choice, so you may gang route objects to get more choices

Args:
  selectors (any, optional)

Inlets:
  0 (INLET_TYPE): Number or list to be Routed
  1 (INLET_TYPE): Number or list to be Routed

Outlets:
  0 (OUTLET_TYPE): Outputs if Input Matches 0
  1 (OUTLET_TYPE): Input if Input Doesn't Match

Messages: bang, int, float, list, anything
"""
route = MaxObject("route")

"""
routepass - Route a complete incoming message based on input matching

Tries to match a message's first argument to the route object's own arguments. Unlike the route object, routepass does not strip off the matched portion of the message. The rightmost outlet passes any message that matched no other choice, so you may gang route objects to get more choices

Args:
  selectors (any, optional)

Inlets:
  0 (INLET_TYPE): Number or list to be Routed
  1 (INLET_TYPE): Change Selector for Match 0

Outlets:
  0 (OUTLET_TYPE): Outputs if Input Matches 0
  1 (OUTLET_TYPE): Output if Input Doesn't Match

Messages: bang, int, float, list, anything
"""
routepass = MaxObject("routepass")

"""
router - Route messages to multiple locations

Args:
  inlets (int, required)
  outlets (int, required)

Inlets:
  0 (INLET_TYPE): connect, disconnect, patch, clear, dump, print
  1 (INLET_TYPE): inlet 0

Outlets:
  0 (OUTLET_TYPE): outlet 0
  1 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, clear, connect, disconnect, dump, patch, print
"""
router = MaxObject("router")

"""
rpnin - Output received RPN values

Output the value from a specific Registered Parameter Number (RPN) and MIDI channel.

Args:
  param-channel (list, optional)

Inlets:
  0 (INLET_TYPE): Raw MIDI Bytes From midiin

Outlets:
  0 (OUTLET_TYPE): Parameter Value
  1 (OUTLET_TYPE): Parameter Change Delta
  2 (OUTLET_TYPE): Registered Parameter Number
  3 (OUTLET_TYPE): MIDI Channel

Messages: int, list, set

Attributes: hires, permissive
"""
rpnin = MaxObject("rpnin")

"""
rpnout - Format 14-bit MIDI RPN messages

Format 14-bit MIDI Registered Parameter Number messages to be transmitted using the midiout object.

Args:
  parameter-channel (list, optional)

Inlets:
  0 (INLET_TYPE): Parameter Value (0-16383 or list MSB LSB)
  1 (INLET_TYPE): Parameter Delta Change (0-127)
  2 (INLET_TYPE): Registered Parameter Number (0-16383)
  3 (INLET_TYPE): MIDI Channel

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Bytes to midiout

Messages: bang, int, list, in1, in2, in3, set

Attributes: hires, running
"""
rpnout = MaxObject("rpnout")

"""
rslider - Display or change a range of numbers

Set a value range from the lowest to highest value.

Inlets:
  0 (INLET_TYPE): int Sets Current Low Value
  1 (INLET_TYPE): int Sets Current High Value

Outlets:
  0 (OUTLET_TYPE): Low Value
  1 (OUTLET_TYPE): High Value

Messages: bang, int, float, list, ft1, in1, (mouse), set, setminmax

Attributes: annotation_name, bgcolor, bordercolor, drawline, fgcolor, floatoutput, listmode, min, mult, orientation, param_connect, parameter_enable, parameter_mappable, size, style, thickness
"""
rslider = MaxObject("rslider")

"""
rtin - Receive MIDI real time messages

Receives and outputs MIDI real time messages transmitted from a specified MIDI input device.

Args:
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): Incoming MIDI Clock Messages

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name
"""
rtin = MaxObject("rtin")

"""
savebang - Send a bang on save

Sends a bang whenever the patcher window within which it resides is saved.

Inlets:
  0 (INLET_TYPE): Sends bang When Patcher Window Is Saved

Outlets:
  0 (OUTLET_TYPE): Output bang

Messages: bang, (mouse)
"""
savebang = MaxObject("savebang")

"""
savedialog - Open a dialog asking for a filename

Opens a file dialog for selection or entry of a filename to write. savedialog takes a list of one or more file types and allows you to save a file and choose a type for it.

Args:
  filetypes (symbol, optional)

Inlets:
  0 (INLET_TYPE): bang Shows Dialog, symbol(s) Set File Types

Outlets:
  0 (OUTLET_TYPE): Complete Path of Chosen Filename
  1 (OUTLET_TYPE): Type Code Chosen
  2 (OUTLET_TYPE): bang When User Cancels

Messages: bang, anything, name, path, set, setpath
"""
savedialog = MaxObject("savedialog")

"""
scale - Map values to an output range

The scale object maps an input range of float or integer values to an output range. The output range can be larger or smaller than the input range, can be inverted and can change numeric type. If specified, the mapping can also be exponential. Please note that unanticipated results can occur when using integer arguments, as output values are truncated rather than rounded. For precise output values, use float arguments.

Args:
  input-low (number, optional)
  input-high (number, optional)
  output-low (number, optional)
  output-high (number, optional)
  exponential (float, optional)

Inlets:
  0 (INLET_TYPE): input value
  1 (INLET_TYPE): input range low
  2 (INLET_TYPE): input range high
  3 (INLET_TYPE): output range low
  4 (INLET_TYPE): output range high
  5 (INLET_TYPE): exponential base value

Outlets:
  0 (OUTLET_TYPE): Output value

Messages: bang, int, float, list

Attributes: classic
"""
scale = MaxObject("scale")

"""
schedule - Schedule messages on high priority scheduler thread

The schedule object schedules messages to execute on the high priority scheduler thread, optionally with a delay time (ms), and optionally deferring to main application thread after the delay time. Similar to the delay and pipe objects, but supports arbitrary messages.

Args:
  delay (number, optional)

Inlets:
  0 (INLET_TYPE): Message To Be Scheduled

Outlets:
  0 (OUTLET_TYPE): The Scheduled Message

Messages: bang, int, float, list, anything, clear, sendmessage

Attributes: defer, delay
"""
schedule = MaxObject("schedule")

"""
screensize - Output the monitor size

Reports the resolution to which the computer's monitor is currently set.

Inlets:
  0 (INLET_TYPE): bang Reports Screen Size

Outlets:
  0 (list): (list) Coordinates of Main Screen
  1 (list): (list) Coordinates Enclosing All Monitors

Messages: bang
"""
screensize = MaxObject("screensize")

"""
select - Output bangs based on input matching

Selectively outputs a bang in response to any input which matches its arguments and will output non-matching messages out its right-most outlet.

Args:
  inlet (int, required)
  selectors (any, optional)

Inlets:
  0 (INLET_TYPE): Number to be Tested
  1 (INLET_TYPE): Set Number to Match

Outlets:
  0 (OUTLET_TYPE): bang if Input Matches 0
  1 (OUTLET_TYPE): Input if Input Doesn't Match

Messages: bang, int, float, list, anything, in1, symbol

Attributes: fuzzy, matchfloat
"""
select = MaxObject("select")

"""
send - Send messages without patch cords

send will transmit given messages to receive objects which are named by the same argument and will allow you to send any kind of message between Patcher windows or within a window without using patch cords. A send object can be instantiated simply by typing into an object box the short-form letter "s".

Args:
  name (symbol, required)

Inlets:
  0 (INLET_TYPE): Message to Send to receive a

Messages: bang, int, float, list, anything, (mouse)
"""
send = MaxObject("send")

"""
seq - Sequencer for recording and playing MIDI data

seq is a sequencer of raw MIDI bytes. You can control the speed of playback (only at the time you start it), read and write from files, and record from live MIDI input. See also midiparse and midiformat which you may need to get to and from raw MIDI.

Args:
  filename (symbol, optional)

Inlets:
  0 (INLET_TYPE): MIDI Data In, Many Other Messages

Outlets:
  0 (OUTLET_TYPE): MIDI Data Out When Playing
  1 (OUTLET_TYPE): bang When Sequence Done
  2 (OUTLET_TYPE): MIDI Meta Messages When Playing

Messages: bang, int, float, addeventdelay, append, clear, delay, dump, hook, print, read, record, start, stop, tick, write

Attributes: overridetempo, sequencetempo, tempo
"""
seq = MaxObject("seq")

"""
serial - Send and receive from a serial port

Args:
  portname (symbol, required)
  port (symbol, optional)
  rate (int, optional)
  data (int, optional)
  stop (int, optional)
  parity (int, symbol, optional)

Inlets:
  0 (INLET_TYPE): int, list Send Chars, bang Polls Output

Outlets:
  0 (OUTLET_TYPE): Output As Integer Characters
  1 (OUTLET_TYPE): Status Signals

Messages: bang, int, float, list, break, close, getport, open, port, print, refresh, reset

Attributes: asyncread, autoopen, baud, break_duration, bufsize, chunk, databits, defer, drain, dtr, parity, poll, postbreak_duration, rts, serport, stopbits, xonxoff
"""
serial = MaxObject("serial")

"""
setclock - Create and control an alternative clock

Allows the creation of alternatives to the standard millisecond clock. This includes several modes of timing that change timing activities. Each setclock is associated with a name (its first argument), and this name may be passed as the argument to a "clock" message to numerous objects that use timing in Max, such as metro, line, and pipe.

Args:
  name (symbol, required)
  mode (symbol, required)
  multiplier (float, required)

Inlets:
  0 (INLET_TYPE): Time Values or Adjustments
  1 (INLET_TYPE): Time Update Interval

Outlets:
  0 (OUTLET_TYPE): Current Time of Clock (bang Message)

Messages: bang, int, float, clock, (mouse), ft1, reset, set
"""
setclock = MaxObject("setclock")

"""
shiftleft - Bit shift to the left

Shifts all bit values to the left. This has the effect of multiplying a number by a power of two, but is less expensive in computer time.

Args:
  initial-value (int, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left << Right

Messages: bang, int, float, in1, set, list
"""
shiftleft = MaxObject("shiftleft")

"""
shiftright - Bit shift to the right

Shifts all bit values to the right. This has the effect of dividing a number by a power of two, but is less expensive in computer resources.

Args:
  initial-value (int, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left >> Right

Messages: bang, int, float, in1, set, list
"""
shiftright = MaxObject("shiftright")

"""
sin - Sine function

Use the sin object to calculate and output the sine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Sin (x)

Messages: bang, int, float
"""
sin = MaxObject("sin")

"""
sinh - Hyperbolic sine function

Use the sinh object to calculate and output the hyperbolic sine of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Sinh (x)

Messages: bang, int, float
"""
sinh = MaxObject("sinh")

"""
slide - Smooth values logarithmically

Filters an input value logarithmically between changes. It's particularly useful for envelope following and lowpass filtering to smooth a stream of continuous data.

Args:
  slide-up (float, optional)
  slide-down-value (float, optional)

Inlets:
  0 (float): (float) Input
  1 (float): (float) Slide up
  2 (float): (float) Slide down

Outlets:
  0 (float): (float) Output

Messages: bang, int, float, ft1, ft2, reset, set
"""
slide = MaxObject("slide")

"""
slider - Move a slider to output values

Resembles a sliding potentiometer, outputting numbers restricted to a specified range, offset by a specified number, and multiplied by a specified number.

Inlets:
  0 (INLET_TYPE): Displays Value Received

Outlets:
  0 (OUTLET_TYPE): Outputs Value When Slider is Changed

Messages: bang, int, float, (mouse), set, setminmax

Attributes: annotation_name, bgcolor, bordercolor, drawoffcolor, elementcolor, floatoutput, inputrangemode, knobcolor, knobshape, min, mult, orientation, param_connect, parameter_enable, parameter_mappable, relative, size, style, thickness
"""
slider = MaxObject("slider")

"""
speedlim - Limit the speed of message throughput

Limit the throughput speed of incoming messages to a fixed time limit. The time can be specified in milliseconds or using a tempo-relative interval.

Args:
  delta-time (int, symbol, float, optional)

Inlets:
  0 (INLET_TYPE): Incoming Continuous Value
  1 (INLET_TYPE): Sets Output Interval to Send Input

Outlets:
  0 (OUTLET_TYPE): Speed-limited Output of Incoming Values

Messages: bang, int, float, list, anything

Attributes: defer, quantize, threshold, usurp
"""
speedlim = MaxObject("speedlim")

"""
spell - Convert input to UTF-8 (Unicode) codes

Accepts a numeric stream and outputs and outputs ASCII characters. Options arguments provide for forced-length messages.

Args:
  size (int, optional)
  character (int, optional)

Inlets:
  0 (INLET_TYPE): Symbol to be Spelled

Outlets:
  0 (OUTLET_TYPE): Symbol's Characters, As Integers

Messages: int, list, anything, symbol
"""
spell = MaxObject("spell")

"""
split - Look for a range of numbers

Check if input can fall within a specified range. If it falls within that range, it sends the number out the left outlet and if it does not, then it sends the number out the right outlet.

Args:
  minimum (number, required)
  maximum (number, required)

Inlets:
  0 (INLET_TYPE): Value to be Sent to One of the Two Outlets
  1 (INLET_TYPE): Set Lower Limit for Left Outlet
  2 (INLET_TYPE): Set Upper Limit for Left Outlet

Outlets:
  0 (OUTLET_TYPE): Output if Within Limits
  1 (OUTLET_TYPE): Output if Not Within Limits

Messages: int, float
"""
split = MaxObject("split")

"""
spray - Distribute a value to a numbered outlet

Accepts lists as input, where the first number is taken as the outlet number, and one or more values that follow are sent out that outlet and those to its right, in right-to-left order. The argument sets the number of outlets (there is no set limit). The default number of outlets is 2.

Args:
  outlets (int, optional)
  offset (int, optional)
  listmode (int, optional)

Inlets:
  0 (INLET_TYPE): list: Outlet Number Followed by Data

Outlets:
  0 (OUTLET_TYPE): Outlet Number 0
  1 (OUTLET_TYPE): Outlet Number 1

Messages: int, list, offset
"""
spray = MaxObject("spray")

"""
sprintf - Format a message of words and numbers

Uses the common C-language "printf" function inside Max. You can combine symbols, organize lists of numbers, or format messages or menu items. For complete documentation, refer to a standard C library reference manual.

Args:
  format (symbol, required)
  symout (symbol, optional)

Inlets:
  0 (INLET_TYPE): First Argument, Causes Output

Outlets:
  0 (OUTLET_TYPE): Formatted String as a Message

Messages: bang, int, float, list, anything, symbol
"""
sprintf = MaxObject("sprintf")

"""
sqrt - Square root function

Calculates and outputs the square-root of any given number.

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Sqrt (x)

Messages: bang, int, float
"""
sqrt = MaxObject("sqrt")

"""
standalone - Configure parameters for standalone applications

The standalone object lets you set options for creating a standalone application from a Max patch, and is used in conjunction with the Build Application/Collective... item found in the File menu. You should only have one standalone object in your top-level patch.

Inlets:
  0 (INLET_TYPE): Messages to Configure Standalone Application

Attributes: allwindowsactive, appicon_mac, appicon_win, bundleidentifier, cantclosetoplevelpatchers, cefsupport, copysupport, database, gensupport, noloadbangdefeating, overdrive, preffilename, searchformissingfiles, statusvisible, usesearchpath
"""
standalone = MaxObject("standalone")

"""
stepcounter - Count messages in a sequence

The stepcounter object uses messages to advance through a sequence of count values. The output producted by stepcounter can be used to generate a sequence of durations from a metro.

Inlets:
  0 (anything): Messages to Count

Outlets:
  0 (OUTLET_TYPE): bang When Current Count Reached
  1 (OUTLET_TYPE): bang When Sequence Resets
  2 (OUTLET_TYPE): Step Index (0-Relative)
  3 (OUTLET_TYPE): Counter Index (0-Relative)

Messages: bang, int, float, anything, reset

Attributes: seq, startmode, syncupdate
"""
stepcounter = MaxObject("stepcounter")

"""
string - Create or duplicate a string object

Create a string, a block of text in memory that is not stored in Max's symbol table.

Args:
  value (, optional)

Inlets:
  0 (INLET_TYPE): messages in
  1 (INLET_TYPE): set string value, messages in

Outlets:
  0 (OUTLET_TYPE): string out
  1 (OUTLET_TYPE): atoms out
  2 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, append, atoms, clear, prepend, string

Attributes: length, name, parameter_enable, parameter_mappable
"""
string = MaxObject("string")

"""
string.append - Append a string to another string object, with an optional separator

Args:
  initial-string (list, optional)

Inlets:
  0 (INLET_TYPE): left string in will be concatenated with the right string
  1 (INLET_TYPE): set right string
  2 (INLET_TYPE): set separator string (or 'emptystring')

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, string

Attributes: separator
"""
string_append = MaxObject("string.append")

"""
string.bytes - Iterate the UTF-8 data in a string as bytes (ints)

Multibyte characters are output as a series of up to 4 bytes.

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): iterated string characters

Messages: bang, int, float, list, anything, string
"""
string_bytes = MaxObject("string.bytes")

"""
string.change - Detect string changes

Output a string if it is different from the previous string received. Use the right inlet to set the test string without output.

Args:
  comparison-string (list, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): 1 if different, 0 if identical
  1 (OUTLET_TYPE): 1 if different, 0 if identical

Messages: bang, int, float, list, anything, string
"""
string_change = MaxObject("string.change")

"""
string.compare - Compare two string objects for equality

When triggered, the object will reveal a '0' if unequal and a '1' if equal.

Args:
  initial-string (list, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): 1 if identical, 0 if different

Messages: bang, int, float, list, anything, string

Attributes: casemode, cmpmode
"""
string_compare = MaxObject("string.compare")

"""
string.concat - Concatenate two string objects

The string received in the right inlet will be appended to the string received in the left inlet, and a new string will be set to the outlet. The original string objects are not modified.

Inlets:
  0 (INLET_TYPE): left string in will be concatenated with the right string
  1 (INLET_TYPE): set right string

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, string
"""
string_concat = MaxObject("string.concat")

"""
string.contains - Test whether a string object contains another string

The substring can occur at any point inside the test string object.

Args:
  search string (symbol, optional)

Inlets:
  0 (INLET_TYPE): string to search in (string1)
  1 (INLET_TYPE): string to search for (string2)

Outlets:
  0 (OUTLET_TYPE): 1 if string1 contains string2, 0 if not

Messages: bang, int, float, list, anything, string
"""
string_contains = MaxObject("string.contains")

"""
string.endswith - Test whether a string object ends with a substring

Args:
  comparison-string (list, optional)

Inlets:
  0 (INLET_TYPE): string in (string1)
  1 (INLET_TYPE): comparison string in (string2)

Outlets:
  0 (OUTLET_TYPE): 1 if string1 ends with string2, 0 if not

Messages: bang, int, float, list, anything, string
"""
string_endswith = MaxObject("string.endswith")

"""
string.frombytes - Construct a new string object from bytes (ints)

Construct a new string object from bytes (ints). Similar to itoa.

Inlets:
  0 (INLET_TYPE): chars (list) in
  1 (INLET_TYPE): chars (list) in

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything
"""
string_frombytes = MaxObject("string.frombytes")

"""
string.fromsymlist - Construct a new string from a list of symbols

The individual symbols are concatenated together to create a new string, without any separator.

Inlets:
  0 (INLET_TYPE): symbol list in

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything
"""
string_fromsymlist = MaxObject("string.fromsymlist")

"""
string.fromutf8 - Construct a new string object from UTF-8 characters, as integer values

Multibyte characters are expected to arrive as a single large value.

Inlets:
  0 (INLET_TYPE): bytes (list) in
  1 (INLET_TYPE): bytes (list) in

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything
"""
string_fromutf8 = MaxObject("string.fromutf8")

"""
string.index - Output the character at an index in a string object (0-based)

Extract a character from an incoming string object, based on the index specified (0-based). Indices can be negative to refer to characters starting from the end of the string (-1 refers to the last character, -2 the next to last character, etc.).

Args:
  index (int, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): position in string to extract (0-based)

Outlets:
  0 (OUTLET_TYPE): nth element of the string
  1 (OUTLET_TYPE): string with nth element removed

Messages: bang, int, float, list, anything, in1, string
"""
string_index = MaxObject("string.index")

"""
string.indexof - Search for the index of a string

Output the position of a substring within a longer string as an integer index. Output -1 if the substring cannot be found.

Inlets:
  0 (INLET_TYPE): string to search for
  1 (INLET_TYPE): string to search in

Outlets:
  0 (OUTLET_TYPE): index out

Messages: bang, int, float, list, anything, string
"""
string_indexof = MaxObject("string.indexof")

"""
string.iter - Iterate the UTF-8 characters of a string object as individual symbols

Inlets:
  0 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): iterated string characters

Messages: bang, int, float, list, anything, string
"""
string_iter = MaxObject("string.iter")

"""
string.length - Determine the length of a string object

The length is the number of UTF-8 characters in the string, and might be different from the count of bytes in the string.

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): length out

Messages: bang, int, float, list, anything, clear, string
"""
string_length = MaxObject("string.length")

"""
string.prepend - Prepend a string to another string object, with an optional separator

Args:
  initial-string (list, optional)

Inlets:
  0 (INLET_TYPE): left string in will be concatenated with the right string
  1 (INLET_TYPE): set right string
  2 (INLET_TYPE): set separator string (or 'emptystring')

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, string

Attributes: separator
"""
string_prepend = MaxObject("string.prepend")

"""
string.regexp - Use regular expressions to process input

Args:
  expression (symbol, optional)
  substitution (symbol, list, optional)

Inlets:
  0 (INLET_TYPE): re, substitute, or text in
  1 (INLET_TYPE): re (regular expression) in
  2 (INLET_TYPE): substitute in

Outlets:
  0 (OUTLET_TYPE): substitutions
  1 (OUTLET_TYPE): capture groups
  2 (OUTLET_TYPE): substrings
  3 (OUTLET_TYPE): unmatched
  4 (OUTLET_TYPE): substring offsets

Messages: bang, int, float, list, anything, string

Attributes: re, substitute
"""
string_regexp = MaxObject("string.regexp")

"""
string.remove - Remove a range of characters from a string object

The range is 0-based and inclusive (a range of 2 4 removes character 2, 3 and 4 of the incoming string).

Args:
  remove-start (int, optional)
  remove-end (int, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): remove start index (0-based)
  2 (INLET_TYPE): remove end index (0-based)

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, in1, in2, string
"""
string_remove = MaxObject("string.remove")

"""
string.replace - Replace the first instance of a substring with a substitution string

Will only replace the first instance of the substring, if any. To replace all occurences of a given string, use string.replaceall.

Args:
  search string (any, optional)
  replacement (any, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): search string
  2 (INLET_TYPE): replacement string

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, string
"""
string_replace = MaxObject("string.replace")

"""
string.replaceall - Replace all instances of a substring with a substitution string

Args:
  substitution-string (list, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): search string
  2 (INLET_TYPE): replacement string

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, string
"""
string_replaceall = MaxObject("string.replaceall")

"""
string.reverse - Reverse a string

The output string will contain the characters of the incoming string, but backward.

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): reversed string out

Messages: bang, int, float, list, anything, string
"""
string_reverse = MaxObject("string.reverse")

"""
string.rotate - Rotate the characters within a string object

Positive values rotate right, negative rotate left.

Args:
  rotations (int, optional)

Inlets:
  0 (INLET_TYPE): string to rotate
  1 (INLET_TYPE): positions to rotate (negative to rotate left)

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, in1, string
"""
string_rotate = MaxObject("string.rotate")

"""
string.slice - Generate a new string from a range of characters in an incoming string

string.slice is similar to string.substring, but attempts to conform to the JavaScript String.slice() behavior.

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): slice indices (0-based)

Outlets:
  0 (OUTLET_TYPE): sliced string out

Messages: bang, int, float, list, anything, string
"""
string_slice = MaxObject("string.slice")

"""
string.split - Split a string object

Two new string objects will be generated from the characters in the incoming string, one from the beginning up to the split point (0-indexed, but not including the split point character itself), and one from the split point to the end.

Args:
  split-point (int, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): string &lt; of split point
  1 (OUTLET_TYPE): string &gt;= of split point

Messages: bang, int, float, list, anything, in1, string
"""
string_split = MaxObject("string.split")

"""
string.sprintf - Generate a string using C-style message formatting

Uses the common C-language "printf" function to generate strings. You can combine symbols, organize lists of numbers, or format messages or menu items. For complete documentation, refer to a standard C library reference manual.

Args:
  format (symbol, required)

Inlets:
  0 (INLET_TYPE): string in (argument 1 in format string)
  1 (INLET_TYPE): string in (argument 2 in format string)

Outlets:
  0 (OUTLET_TYPE): string out

Messages: bang, int, float, list, anything, string
"""
string_sprintf = MaxObject("string.sprintf")

"""
string.startswith - Test whether a string object starts with a substring

Args:
  comparison string (any, optional)

Inlets:
  0 (INLET_TYPE): string in (string1)
  1 (INLET_TYPE): comparison string in (string2)

Outlets:
  0 (OUTLET_TYPE): 1 if string1 starts with string2, 0 if not

Messages: bang, int, float, list, anything, string
"""
string_startswith = MaxObject("string.startswith")

"""
string.substring - Generate a new string from a range of characters in an incoming string

string.substring is similar to string.slice, but doesn't conform to the JavaScript String.slice() behavior.

Args:
  substring-indexes (list, optional)

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): substring indices (0-based)

Outlets:
  0 (OUTLET_TYPE): substring out

Messages: bang, int, float, list, anything, string
"""
string_substring = MaxObject("string.substring")

"""
string.toarray - Construct a new array object from a string object

Construct a new array object from a string object. An optional separator string can be used to determine how the string is split into individual array entries (default = <space>).

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): separator string in

Outlets:
  0 (OUTLET_TYPE): array out

Messages: bang, int, float, list, anything, clear, string
"""
string_toarray = MaxObject("string.toarray")

"""
string.tolist - Construct a new list from a string object

An optional separator string can be used to determine how the string is split into individual list entries (default = <space>).

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): separator string in

Outlets:
  0 (OUTLET_TYPE): list out

Messages: bang, int, float, list, anything, clear, string
"""
string_tolist = MaxObject("string.tolist")

"""
string.tolower - Convert uppercase characters in a string object to lowercase

Note that this only works for ASCII letters, not for complex UTF-8 characters.

Inlets:
  0 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): lowercase string out

Messages: bang, int, float, list, anything, string
"""
string_tolower = MaxObject("string.tolower")

"""
string.tosymbol - Convert a string to a symbol

Adds a string to Max's symbol table (if not already present) and sends the symbol to the outlet.

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): symbol out

Messages: bang, int, float, list, anything, string
"""
string_tosymbol = MaxObject("string.tosymbol")

"""
string.toupper - Convert lowercase characters in a string object to uppercase

Note that this only works for ASCII letters, not for complex UTF-8 characters.

Inlets:
  0 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): uppercase string out

Messages: bang, int, float, list, anything, string
"""
string_toupper = MaxObject("string.toupper")

"""
string.trim - Trim whitespace from the beginning and end of a string object

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): trimmed string out

Messages: bang, int, float, list, anything, string
"""
string_trim = MaxObject("string.trim")

"""
string.trimend - Trim whitespace from the end of a string object

Whitespace at the beginning of the string is ignored. Whitespace includes space, tab and newline characters.

Inlets:
  0 (INLET_TYPE): string in
  1 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): trimmed string out

Messages: bang, int, float, list, anything, string
"""
string_trimend = MaxObject("string.trimend")

"""
string.trimstart - Remove whitespace from the beginning of a string object

Whitespace includes spaces, newlines, tabs, and carriage returns.

Inlets:
  0 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): trimmed string out

Messages: bang, int, float, list, anything, string
"""
string_trimstart = MaxObject("string.trimstart")

"""
string.utf8 - Iterate the UTF-8 characters in a string as integers

Multibyte characters are output as multibyte (> 127) integers.

Inlets:
  0 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): iterated string characters

Messages: bang, int, float, list, anything, string
"""
string_utf8 = MaxObject("string.utf8")

"""
string.withpass - Route a string by a matching substring

Route a string to one of several outputs based on a substring match. Similar to routepass but for strings. This object was renamed from string.passcmp (which is now an alias to this object), the functionality remains the same.

Args:
  match substrings (list, required)

Inlets:
  0 (INLET_TYPE): string in

Outlets:
  0 (OUTLET_TYPE): string out if there are no matches

Messages: bang, int, float, list, anything, string

Attributes: match
"""
string_withpass = MaxObject("string.withpass")

"""
stripnote - Filter out note-off messages

Only pass note-on messages: those having any velocity above 0.

Inlets:
  0 (INLET_TYPE): Pitch (from notein Left Outlet)
  1 (INLET_TYPE): Velocity (from notein Velocity Outlet)

Outlets:
  0 (OUTLET_TYPE): Pitch
  1 (OUTLET_TYPE): Velocity if Note-on Message

Messages: int, float, ft1, in1
"""
stripnote = MaxObject("stripnote")

"""
strippath - Separate filename from a full pathname

Removes path information from a complete file path. strippath also tells you whether the resulting filename is in the search path or not.

Inlets:
  0 (INLET_TYPE): Absolute Path

Outlets:
  0 (OUTLET_TYPE): File Name
  1 (OUTLET_TYPE): 1 if Path/File in Search Path, 0 Otherwise

Messages: anything
"""
strippath = MaxObject("strippath")

"""
substitute - Substitute symbols within a message

Matches messages to its own arguments; whenever it finds a match, will make the appropriate substitution.

Args:
  match (any, optional)
  replacement (any, optional)
  mode (any, optional)

Inlets:
  0 (INLET_TYPE): Message For Substitution
  1 (INLET_TYPE): Change Substitution

Outlets:
  0 (OUTLET_TYPE): Newly Substituted Message
  1 (OUTLET_TYPE): Message That Was Left Intact

Messages: bang, int, float, list, anything, set
"""
substitute = MaxObject("substitute")

"""
suckah - Get a pixel from the display

Creates a click-able area that returns an RGB color, or can accept any screen coordinates to get the RGB values of that pixel.

Inlets:
  0 (INLET_TYPE): Coordinates in

Outlets:
  0 (OUTLET_TYPE): RGBA list Out (0. -1.)

Messages: list, (mouse)

Attributes: boundmode, compatibility, outputalpha
"""
suckah = MaxObject("suckah")

"""
suspend - Reports when the application is in the background or foreground

Outputs a 0 when the application is in the background, and a 1 when the application is in the foreground.

Inlets:
  0 (INLET_TYPE): 1 When Application Is In Foreground, 0 When In Background

Outlets:
  0 (OUTLET_TYPE): 1 When Application Is In Foreground, 0 When In Background
"""
suspend = MaxObject("suspend")

"""
sustain - Hold note-off messages for release

Holds any notes with velocities of 0 until instructed to release them. This provides for sustain pedal-like behavior when working with incoming MIDI notes.

Inlets:
  0 (INLET_TYPE): Pitch
  1 (INLET_TYPE): Velocity
  2 (INLET_TYPE): Sustain On = 1, Off = 0

Outlets:
  0 (OUTLET_TYPE): Pitch
  1 (OUTLET_TYPE): Velocity

Messages: int, float, list, clear, flush

Attributes: repeatmode, sustain
"""
sustain = MaxObject("sustain")

"""
swap - Swap position of two numbers

Swaps the values of its inlets, preserving right-to-left ordering. The first outlet type is determined by its argument. The second outlet's type is always an int.

Args:
  initial (number, optional)

Inlets:
  0 (INLET_TYPE): Sets Value for Right Outlet, Causes Output
  1 (INLET_TYPE): Sets Value for Left Outlet

Outlets:
  0 (OUTLET_TYPE): Value From Right Inlet
  1 (OUTLET_TYPE): Value From Left Inlet

Messages: bang, int, float, ft1, in1, list
"""
swap = MaxObject("swap")

"""
swatch - Choose a color

Provides 2-dimensional selection and display of colors, representing hue along the horizontal axis and lightness along the vertical axis. A third color dimension, saturation, may be set by means of the saturation message.

Inlets:
  0 (INLET_TYPE): RGB List(0. -1.)
  1 (INLET_TYPE): Green (0. -1.)
  2 (INLET_TYPE): Blue (0. -1.)

Outlets:
  0 (OUTLET_TYPE): RGBA Color List Out
  1 (OUTLET_TYPE): Saturation (0. -1.)

Messages: bang, int, float, list, alpha, ft1, ft2, hsl, in1, in2, (mouse), set, sethsl

Attributes: annotation_name, compatibility, param_connect, parameter_enable, parameter_mappable, saturation
"""
swatch = MaxObject("swatch")

"""
switch - Accept messages from a specific inlet

Args:
  inlets (int, optional)
  initial (int, optional)

Inlets:
  0 (INLET_TYPE): 0 Turns switch Off, Non-zero Selects Input
  1 (INLET_TYPE): Switch Input 1 (Currently Closed)
  2 (INLET_TYPE): Switch Input 2 (Currently Closed)

Outlets:
  0 (OUTLET_TYPE): Output if Open Input Receives Anything

Messages: bang, int, float, next
"""
switch = MaxObject("switch")

"""
sxformat - Prepare MIDI system exclusive messages

Accepts, as arguments, a list of numbers which are sent out sequentially. In addition, you may use one or more "expr"-style statements, which start with the word "is", which will be evaluated with the result being sent out. "is" statements need to be separated by slashes.

Args:
  SysEx (list, required)

Inlets:
  0 (INLET_TYPE): First Argument, Causes Output

Outlets:
  0 (OUTLET_TYPE): Formatted String as a Message

Messages: bang, int, in1, in2, in3, in4, in5, in6, in7, in8, in9, list
"""
sxformat = MaxObject("sxformat")

"""
sysexin - Receive MIDI system exclusive messages

Receives MIDI system exclusive messages from a MIDI input device. It takes an optional argument for port selection. If a MIDI system exclusive message is received, sysexin outputs raw MIDI as integers.

Args:
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): System-Exclusive Messages

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name
"""
sysexin = MaxObject("sysexin")

"""
tab - Tab control

The tab can be used to create multiple button and multiple column displays and interfaces.

Inlets:
  0 (INLET_TYPE): Messages in

Outlets:
  0 (OUTLET_TYPE): Item Index
  1 (OUTLET_TYPE): Item Symbol
  2 (OUTLET_TYPE): Idle Item Index

Messages: bang, int, float, (mouse), next, prev, set, setsymbol, symbol

Attributes: activesafe, annotation_name, bgcolor, border, bordercolor, borderoncolor, button, clicktabcolor, clicktextcolor, colorselectedtext, contrastactivetab, fadetime, fadeunselect, gradient, hovertabcolor, hovertextcolor, htabcolor, htextcolor, margin, mode, multiline, param_connect, parameter_enable, parameter_mappable, rounded, segmented, spacing_x, spacing_y, style, tabcolor, tabs, textcolor, truncate, valign
"""
tab = MaxObject("tab")

"""
tabcontroller - Define and manage patcher tabs

The tabcontroller object is placed in a top-level patcher. When provided with a dictionary of tab names and patcher references, it will create tabs in that patcher for each dictionary entry. References in the dictionary can refer to embedded subpatchers or patcher files. When patcher files are used, they will not be loaded until you click on the tab. Moreoever, tabs can be shown in a vertically scrolling list at the left side of the patcher window. These features permit a patcher using tabcontroller to show an arbitrarily large list of patchers, making it ideal for presenting a set of related tutorials or examples. Note that RNBO or Gen subpatchers in tabs are loaded immediately when loading their parent patchers.

Inlets:
  0 (INLET_TYPE): Control Tabs

Messages: add, addfolder, clear, dictionary, remove, setactive, setactivefilename

Attributes: disableinternals, exclusivemidi, folder, orientation, recursive, showroot
"""
tabcontroller = MaxObject("tabcontroller")

"""
table - Store and edit an array of numbers

Args:
  name (symbol, optional)

Inlets:
  0 (INLET_TYPE): int Outputs Value at Index, list Stores Value
  1 (INLET_TYPE): Sets Y Value for Next int at Left Inlet

Outlets:
  0 (OUTLET_TYPE): Outputs Requested Indices or Values
  1 (OUTLET_TYPE): Outputs bang When Table Window Edited

Messages: bang, int, float, list, cancel, clear, const, (mouse), dump, flags, fquantile, getbits, goto, in1, inv, length, load, max, min, next, normal, open, prev, quantile, read, refer, send, set, setbits, sum, wclose, write

Attributes: annotation_name, embed, name, notename, parameter_enable, parameter_mappable, range, setresizes, signed, size
"""
table = MaxObject("table")

"""
tan - Tangent function

Use the tan object to calculate and output the tangent of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Tan (x)

Messages: bang, int, float
"""
tan = MaxObject("tan")

"""
tanh - Hyperbolic tangent function

Use the tanh object to calculate and output the hyperbolic tangent of any given number.

Args:
  initial-value (int, float, optional)

Inlets:
  0 (INLET_TYPE): Input Value X

Outlets:
  0 (OUTLET_TYPE): Tanh (x)

Messages: bang, int, float
"""
tanh = MaxObject("tanh")

"""
tempo - Output numbers at a metronomic tempo

Produces metronomic output controllable in beats per minute, and with specifiable whole-note divisions.

Args:
  tempo (int, float, optional)

Inlets:
  0 (INLET_TYPE): Start/Stop Metronome
  1 (INLET_TYPE): Tempo in Beats Per Minute
  2 (INLET_TYPE): Beat Multiplier
  3 (INLET_TYPE): Beat Divisions of a Whole Note

Outlets:
  0 (OUTLET_TYPE): Beat Count at Current Tempo

Messages: bang, int, float, clock, stop, tempo
"""
tempo = MaxObject("tempo")

"""
text - Format messages as a text file

Collects and formats incoming messages as text to be output as lines of text.

Args:
  filename (symbol, required)

Inlets:
  0 (INLET_TYPE): Incoming Numbers or Symbols to be Collected

Outlets:
  0 (OUTLET_TYPE): Text Output By line Message
  1 (OUTLET_TYPE): bang When Read Completed
  2 (OUTLET_TYPE): query result (number of lines)

Messages: int, float, list, anything, clear, cr, (mouse), dump, filetype, line, open, query, read, settitle, symbol, t_symbol, tab, wclose, write

Attributes: precision, stringout
"""
text = MaxObject("text")

"""
text.codebox - Display and edit text

The dict.codebox object is a UI object for display and editing of text, optionally backed by a file on disk.  Clicking on the hammer icon will output the text as a string and optionally write the file back to disk (autowrite must be enabled). Beware of losing work when enabling autowrite. It can use a syntax highlighting style (.js, .json, ,jxs, .coll, .genexpr, .lua, or .cpp)

Inlets:
  0 (INLET_TYPE): messages in
  1 (INLET_TYPE): set string value, messages in

Outlets:
  0 (OUTLET_TYPE): string out
  1 (OUTLET_TYPE): atoms out
  2 (OUTLET_TYPE): dumpout

Messages: bang, int, float, list, anything, append, atoms, clear, prepend, string

Attributes: autowatch, autowrite, bgcolor, editlocked, embed, filename, linenumbers, linenumberwidth, margin, style, syntax, textcolor, length, name
"""
text_codebox = MaxObject("text.codebox")

"""
textbox - Display and output numbers, lists, and messages

The number object displays and outputs either integers, floats, lists of numbers, or any message, depending on its format. flonum is a synonym for number in Float format, listbox is a synonym for number in List format, and textbox is a synonym for number in Text format.

Inlets:
  0 (INLET_TYPE): Set Displayed Input and Repeat to Output

Outlets:
  0 (OUTLET_TYPE): Output Incoming or Entered Values
  1 (OUTLET_TYPE): bang When Tab Key Pressed

Messages: bang, int, float, list, anything, clear, max, min, (mouse), select, set

Attributes: annotation_name, bgcolor, bordercolor, cantchange, format, hbgcolor, htextcolor, htricolor, maximum, minimum, mousefilter, numdecimalplaces, outputonclick, param_connect, parameter_enable, parameter_mappable, style, textcolor, triangle, tricolor, triscale
"""
textbox = MaxObject("textbox")

"""
textbutton - Button with text

Inlets:
  0 (INLET_TYPE): Messages in

Outlets:
  0 (OUTLET_TYPE): Bang when Mouse Up
  1 (OUTLET_TYPE): Item Symbol
  2 (OUTLET_TYPE): Idle Mouse

Messages: bang, int, float, (mouse), set, setsymbol, symbol

Attributes: active, align, annotation_name, bgcolor, bgoncolor, bgovercolor, bgoveroncolor, blinktime, border, bordercolor, borderoncolor, fontlink, legacytextcolor, mode, outputmode, param_connect, parameter_enable, parameter_mappable, rounded, spacing_x, spacing_y, style, text, textcolor, texton, textoncolor, textovercolor, textoveroncolor, tosymbol, truncate, underline, usebgoncolor, usetextovercolor
"""
textbutton = MaxObject("textbutton")

"""
textedit - Enter text

Provides a user interface within a patcher which can collect typed-in text from the computer-keyboard.

Inlets:
  0 (INLET_TYPE): bang Reports Text

Outlets:
  0 (OUTLET_TYPE): Text Output
  1 (OUTLET_TYPE): ASCII Value of Character Typed
  2 (OUTLET_TYPE): Item or Character Clicked On
  3 (OUTLET_TYPE): Sends &quot;textchanged&quot; when Text is Changed While Typing

Messages: bang, int, float, list, (mouse), (typing), (drag), append, clear, dictionary, select, set

Attributes: annotation_name, autoscroll, bangmode, bgcolor, border, bordercolor, clickmode, keymode, lines, nosymquotes, outputmode, param_connect, parameter_enable, parameter_mappable, readonly, rounded, separator, style, tabmode, textcolor, valuemode, wordwrap
"""
textedit = MaxObject("textedit")

"""
themecolor - Change and/or listen for changes in interface colors

Output the interface color whenever it changes, query Max for current color settings, and change an interface color.

Args:
  UI element name (symbol, required)

Inlets:
  0 (INLET_TYPE): list Sets Color, bang Reports Color

Outlets:
  0 (list): Color Value
  1 (list): bang When Color Changes

Messages: bang, list, anything

Attributes: color
"""
themecolor = MaxObject("themecolor")

"""
thisobject - Monitor and control named objects

Use thisobject as a connection to objects by name. thisobject can be used with global named objects including max and dsp. It can also connect to objects with scripting names, including objects in other patchers using pattr paths. You can also use the name patcher for connecting to the patcher containing the thisobject, which works similarly to the thispatcher object. thisobject is similar to getattr but permits you to change the named target object using the target attribute, whereas getattr uses a patch cord to establish a connection to its target.

Args:
  target object (symbol, optional)
  selected attribute (symbol, optional)

Inlets:
  0 (INLET_TYPE): Control Inlet, bang Reports Attribute Value

Outlets:
  0 (OUTLET_TYPE): Attribute Value
  1 (dictionary): Response to targetattrdict

Messages: bang, int, float, list, anything, targetattach, targetattrdict, targetbang, targetvalue

Attributes: attr, listen, listenvalue, prefix, target
"""
thisobject = MaxObject("thisobject")

"""
thispatcher - Send messages to a patcher

Allows modification of a patcher window with Max messages.

Inlets:
  0 (INLET_TYPE): Messages (write,dispose,etc.) to Send to Patcher

Outlets:
  0 (OUTLET_TYPE): Output From window info Messages
  1 (OUTLET_TYPE): Pathname If patcher Is a File

Messages: anything, end, savewindow, setactivetab
"""
thispatcher = MaxObject("thispatcher")

"""
threadcheck - Report the thread of execution for a message

Report the thread of execution for a message

Inlets:
  0 (anything): message to check

Outlets:
  0 (bang): message received on main thread
  1 (bang): message received on scheduler thread
  2 (bang): message received on audio thread
  3 (bang): message received on unknown thread

Messages: bang, int, float, list, anything
"""
threadcheck = MaxObject("threadcheck")

"""
thresh - Combine numbers, symbols and lists when received close together

Collects items into a list if they appear within a certain specifiable amount of time. Each time an item arrives, the time is reset.

Args:
  threshold (int, optional)
  threshold-time (float, optional)

Inlets:
  0 (INLET_TYPE): Numbers to be Gathered Into list
  1 (INLET_TYPE): Arrival Threshold

Outlets:
  0 (OUTLET_TYPE): Gathered list

Messages: int, float, list, anything, symbol
"""
thresh = MaxObject("thresh")

"""
timepoint - Bang at a specific time

Outputs a bang when the clock reaches a specific time. The clock has to be moving forward in order for the bang to be output.

Args:
  time (int, symbol, float, required)

Inlets:
  0 (INLET_TYPE): Set Target Time

Outlets:
  0 (OUTLET_TYPE): bang When Clock Reaches Target Time

Messages: int, float, list, anything

Attributes: time, transport
"""
timepoint = MaxObject("timepoint")

"""
timer - Report elapsed time between two events

timer starts keeping time when a bang is sent to the left inlet. The right outlet can report elapsed time in milliseconds or one of the Max time format options, with the exception of notevalues.

Inlets:
  0 (INLET_TYPE): Start of Interval to be Timed
  1 (INLET_TYPE): End of Interval to be Timed

Outlets:
  0 (OUTLET_TYPE): Milliseconds Between bangs at Left and Right Inlets
  1 (OUTLET_TYPE): Formatted Value of Time Between bangs at Left and Right Inlets

Messages: bang, clock

Attributes: format, transport
"""
timer = MaxObject("timer")

"""
times - Multiply two numbers

Multiplies two numbers together, and outputs the result upon receiving input in the left inlet.

Args:
  initial (int, float, optional)

Inlets:
  0 (INLET_TYPE): Set Left Operand, Trigger the Calculation
  1 (INLET_TYPE): Set Right Operand

Outlets:
  0 (OUTLET_TYPE): Result = Left * Right

Messages: bang, int, float, in1, set, list
"""
times = MaxObject("times")

"""
togedge - Report zero/non-zero transitions

Output bangs for 0-to-1 transitions out the left outlet, and bangs the right outlet for 1-to-0 transitions. Outlets alternate output when bangs are received.

Inlets:
  0 (INLET_TYPE): Connect to a checkbox (or bang it to toggle outs)

Outlets:
  0 (OUTLET_TYPE): In Phase With Input
  1 (OUTLET_TYPE): Out Of Phase With Input

Messages: bang, int
"""
togedge = MaxObject("togedge")

"""
toggle - Switch between off and on (0 and 1)

When clicked, toggle outputs a 0 when turned off and a 1 when turned on. When giving input, a non-zero number will turn it on, a 0 will turn it off, and a bang will alternate the state of the toggle. All numbers are converted to integer and passed through unchanged.

Inlets:
  0 (INLET_TYPE): int Sets Toggle, bang Reverses It

Outlets:
  0 (OUTLET_TYPE): Output 1 or 0 When Toggle Is Set

Messages: bang, int, float, (drag), (mouse), outputvalue, read, set

Attributes: annotation_name, bgcolor, bordercolor, checkedcolor, param_connect, parameter_enable, parameter_mappable, size, style, svg, thickness, uncheckedcolor
"""
toggle = MaxObject("toggle")

"""
tosymbol - Convert messages, numbers, or lists to a single symbol

tosymbol accepts any message, number, or list, and converts it into a single symbol. The symbol has a maximum length of 2048 characters.

Inlets:
  0 (INLET_TYPE): Input to be Symbolized

Outlets:
  0 (OUTLET_TYPE): The Input, as a Symbol

Messages: bang, int, float, list, anything

Attributes: separator
"""
tosymbol = MaxObject("tosymbol")

"""
touchin - Receive MIDI aftertouch values

Outputs from MIDI aftertouch (channel pressure) messages received from a MIDI input device.

Args:
  port-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device

Outlets:
  0 (OUTLET_TYPE): After Touch
  1 (OUTLET_TYPE): MIDI Channel

Messages: anything, (mouse), (MIDI), port

Attributes: matchport, name
"""
touchin = MaxObject("touchin")

"""
touchout - Transmit MIDI aftertouch messages

Transmits MIDI aftertouch values to a MIDI device.

Args:
  port-channel (list, required)
  channel (int, required)
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): After Touch
  1 (INLET_TYPE): MIDI Channel

Messages: int, float, anything, (mouse), in1, port

Attributes: matchport, name
"""
touchout = MaxObject("touchout")

"""
translate - Convert between different time formats

The translate object converts from any of the fixed or relative Max time values to any other fixed or relative time value.

Args:
  input-format (symbol, optional)
  output-format (symbol, optional)

Inlets:
  0 (INLET_TYPE): Time Value to Convert

Outlets:
  0 (OUTLET_TYPE): Converted time value

Messages: bang, int, float, list, anything

Attributes: in, listen, listmode, mode, out, transport
"""
translate = MaxObject("translate")

"""
transport - Control a clock

Starts and stops the passage of time for objects linked to a transport. If given a name, the transport object will control a time context of the given name, otherwise it will control Max's global 'internal' transport. The transport object reports time consistent with the time formats used in Max.

Inlets:
  0 (INLET_TYPE): Non-zero Starts Transport, Zero Stops
  1 (INLET_TYPE): Set Transport Position in Ticks

Outlets:
  0 (OUTLET_TYPE): Bars
  1 (OUTLET_TYPE): Beats
  2 (OUTLET_TYPE): Units
  3 (OUTLET_TYPE): Current Resolution (PPQ)
  4 (OUTLET_TYPE): Tempo
  5 (OUTLET_TYPE): Time Signature (list)
  6 (OUTLET_TYPE): Transport State
  7 (OUTLET_TYPE): Raw Ticks
  8 (OUTLET_TYPE): Clocksource List

Messages: bang, int, float, list, (mouse), dump, getclocksources, timesig

Attributes: clocksource, name, resetbarcount, tempo
"""
transport = MaxObject("transport")

"""
trigger - Send input to many places

Outputs any input received in order from right to left and formatted according to the object-argument specified.

Args:
  formats (symbol, optional)
  constant (any, optional)

Inlets:
  0 (INLET_TYPE): Message to be Fanned to Multiple Outputs

Outlets:
  0 (OUTLET_TYPE): Output Order 2 (int)
  1 (OUTLET_TYPE): Output Order 1 (int)

Messages: bang, int, float, list, anything
"""
trigger = MaxObject("trigger")

"""
trough - Output a number if it is less than previous numbers

Compares an input value to the stored value and, if it's smaller it sends the input to the output and sets it as the new trough.

Args:
  value (float, optional)

Inlets:
  0 (INLET_TYPE): Find the lowest number (the trough)
  1 (INLET_TYPE): Sets the trough

Outlets:
  0 (OUTLET_TYPE): The lowest number so far
  1 (OUTLET_TYPE): Goes high for a new trough
  2 (OUTLET_TYPE): Goes high for a number that isn't a new trough

Messages: bang, int, float, ft1, in1
"""
trough = MaxObject("trough")

"""
ubutton - Transparent button

Creates a transparant click-able region that can be placed over graphics or other objects. Produces a bang message when clicked.

Inlets:
  0 (INLET_TYPE): Inverts Button and Sends bang out Left Outlet

Outlets:
  0 (OUTLET_TYPE): bang on Mouse-Up or Incoming Message
  1 (OUTLET_TYPE): bang on Mouse-Down
  2 (OUTLET_TYPE): Click Cursor Location
  3 (OUTLET_TYPE): 1 if Mouse Clicked Inside Button, 0 if Outside

Messages: bang, int, float, anything, (mouse), set

Attributes: annotation_name, dragtrack, hilite, hltcolor, param_connect, parameter_enable, parameter_mappable, rounded, stay, toggle
"""
ubutton = MaxObject("ubutton")

"""
udpreceive - Receive messages over a network

Receives messages transmitted over a network using the User Datagram Protocol (UDP).

Args:
  port (int, required)
  full-packet (symbol, optional)

Inlets:
  0 (INLET_TYPE): messages in

Outlets:
  0 (OUTLET_TYPE): data out

Messages: maxqueuesize, port

Attributes: defer, outputformat, quiet
"""
udpreceive = MaxObject("udpreceive")

"""
udpsend - Send messages over a network

Transmits messages over a network using the User Datagram Protocol (UDP).

Args:
  host (symbol, required)
  port (int, required)

Messages: bang, int, float, list, anything, FullPacket, host, maxpacketsize, maxqueuesize, osc_packet, port, rawbytes
"""
udpsend = MaxObject("udpsend")

"""
umenu - Pop-up menu

Displays text as a pop-up menu. Selections can be made manually, or set incoming numbers. Outputs both selection number and selection text.

Inlets:
  0 (INLET_TYPE): Messages in

Outlets:
  0 (OUTLET_TYPE): Item Number Chosen
  1 (OUTLET_TYPE): Menu Item Text Evaluated as a Message
  2 (OUTLET_TYPE): Dumpout

Messages: bang, int, float, (drag), append, checkitem, clear, clearchecks, count, delete, dictionary, dump, enableitem, gettoggle, insert, mode, (mouse), next, populate, prev, set, setcheck, setitem, setrgb, setsymbol, settoggle, showchecked, symbol, toggle

Attributes: align, allowdrag, annotation_name, applycolors, arrow, arrowbgcolor, arrowcolor, arrowframe, arrowlink, autopopulate, bgcolor, bgcolor2, bgfillcolor, blanksym, collection, color, depth, discolor, elementcolor, framecolor, hltcolor, items, labelclick, menumode, param_connect, parameter_enable, parameter_mappable, pattrmode, prefix, prefix_mode, rounded, showdotfiles, style, textcolor, textcolor2, togcolor, truncate, types, underline
"""
umenu = MaxObject("umenu")

"""
universal - Send messages to all objects of the same type

Sends a input message to all instances of the same class of object in a patcher. The order in which the messages arrive is not defined.

Args:
  mode (int, optional)

Inlets:
  0 (INLET_TYPE): Class Name and Message

Messages: bang, int, float, list, anything, send

Attributes: descend
"""
universal = MaxObject("universal")

"""
unjoin - Break a list into messages

Separates a list's elements by group, and send each group of items out a separate outlet.

Args:
  outlets (int, required)

Inlets:
  0 (INLET_TYPE): Input

Outlets:
  0 (OUTLET_TYPE): Output (0)
  1 (OUTLET_TYPE): Output (1)
  2 (OUTLET_TYPE): Extra

Messages: int, float, list, anything

Attributes: outsize
"""
unjoin = MaxObject("unjoin")

"""
unpack - Break a list into individual messages

Breaks a list into its elements, and sends each item out a separate outlet.

Args:
  list-elements (any, optional)

Inlets:
  0 (INLET_TYPE): List to be Unpacked

Outlets:
  0 (OUTLET_TYPE): Element 1 of List
  1 (OUTLET_TYPE): Element 2 of List

Messages: bang, int, float, list, anything
"""
unpack = MaxObject("unpack")

"""
urn - Generate random numbers without duplicates

Outputs random numbers and keeps track of each number that has been generated. When all numbers up to the maximum (set via an argument or the right inlet) have been output, a bang is send out the right outlet.

Args:
  limit (int, optional)
  seed (int, optional)

Inlets:
  0 (INLET_TYPE): bang Generates Random Number
  1 (INLET_TYPE): Set Range of Random Numbers

Outlets:
  0 (OUTLET_TYPE): Random Number Output
  1 (OUTLET_TYPE): bang If All Numbers in Range Chosen

Messages: bang, int, clear, in1, seed
"""
urn = MaxObject("urn")

"""
uzi - Send many bang messages

Outputs a specified number of bang messages quickly. uzi is designed for rapid-fire output of a large number of bang messages.

Args:
  initial (int, optional)
  base (int, optional)

Inlets:
  0 (INLET_TYPE): Start shooting bangs (bang or int)
  1 (INLET_TYPE): How many bangs to shoot (int)

Outlets:
  0 (OUTLET_TYPE): Watch out
  1 (OUTLET_TYPE): Done banging bang (carry)
  2 (OUTLET_TYPE): Current Index

Messages: bang, int, break, continue, in1, offset, pause, resume
"""
uzi = MaxObject("uzi")

"""
v8 - Execute Javascript (Modern Engine)

Exposes the Javascript language (ECMAScript 6+) and some Max specific extensions. The v8 object can be instantiated with a javascript filename or with numerical arguments to specify the number of outlets and inlets respectively. The default number of outlets and inlets are both 1.

Args:
  filename (symbol, optional)
  inlets-outlets (list, optional)
  jsarguments (list, optional)

Inlets:
  0 (INLET_TYPE): none: Inlet 0

Outlets:
  0 (OUTLET_TYPE): none: Outlet 0

Messages: bang, int, float, list, anything, array, assiststr, compile, (mouse), delprop, getprop, jsfile, loadbang, open, read, setprop, string, wclose

Attributes: embed, parameter_enable, parameter_mappable
"""
v8 = MaxObject("v8")

"""
v8.codebox - Execute Javascript (Modern Engine)

Exposes the Javascript language (ECMAScript 6+) and some Max specific extensions, using a UI object for display and editing.

Inlets:
  0 (INLET_TYPE): none: Inlet 0
  1 (INLET_TYPE): none: Inlet 1

Outlets:
  0 (OUTLET_TYPE): none: Outlet 0

Messages: wclose, open, int, float, bang, list, anything, compile, setprop, getprop, delprop, loadbang, autowatch

Attributes: bgcolor, linenumbers, linenumberwidth, margin, textcolor
"""
v8_codebox = MaxObject("v8.codebox")

"""
v8ui - Javascript user interfaces and graphics (Modern Engine)

Provides an environment to make user interface elements using Javascript (ECMAScript 6+). This provides all of the programming tools available in the v8 object, but also exposes the mgraphics and sketch drawing routines for visual output.

Inlets:
  0 (INLET_TYPE): none: Inlet 0

Outlets:
  0 (OUTLET_TYPE): none: Outlet 0

Messages: bang, int, float, list, anything, (drag), array, assiststr, compile, (mouse), delprop, getprop, jsfile, loadbang, open, read, setprop, size, string, wclose

Attributes: border, embed, filename, jsarguments, nofsaa, param_connect, parameter_enable, parameter_mappable
"""
v8ui = MaxObject("v8ui")

"""
value - Share data between other value objects

Shares data with other value objects of the same name. If you send a message (number, list, or anything else) to one value object, it is set for all value objects with the same 'name' argument. You can get the contents out of a particular value object by sending it a bang.

Args:
  name (symbol, required)
  initial (any, optional)

Inlets:
  0 (INLET_TYPE): int or float Sets Value, bang Causes Output

Outlets:
  0 (OUTLET_TYPE): Value Shared by All value a Objects

Messages: bang, int, float, list, anything, (mouse), symbol
"""
value = MaxObject("value")

"""
vdp - Control a videodisk player through a serial port

The vdp object works with serially-controlled videodisk players (remember them?) that are compatible with the Pioneer 4200 or 8000 standard. Each command received by the vdp object sends a stream of numbers out the object's left outlet, intended to be connected to the serial object. The description of each command discusses what effect the command has on the player, not the exact character stream sent by vdp.

		Because videodisc players have relatively buffer-less serial interfaces, vdp places each command it receives in a queue, and sends it out only when the player has finished executing its most recent command. This "feature" may cause a delay between the time a command is sent to the vdp object and the time it is actually sent out the serial port.


		Any message received in the right inlet will behave exactly as if it had been received in the left inlet, except that it will be put at the front of the queue, to be the very next command sent out to the player.

Inlets:
  0 (INLET_TYPE): Regular Priority Commands
  1 (INLET_TYPE): Handshaking from Serial Port
  2 (INLET_TYPE): High Priority Commands

Outlets:
  0 (OUTLET_TYPE): Command Output
  1 (OUTLET_TYPE): Polling Output
  2 (OUTLET_TYPE): Frame Number
  3 (OUTLET_TYPE): Time

Messages: int, chapter, clear, cmd, control, fps, frame, name, play, restrict, scan, search, setskip, skip, step, stop
"""
vdp = MaxObject("vdp")

"""
vexpr - Evaluate a math expression for a list

Performs mathematical calculations using C language-style mathematical operations. Operates on inputs that are lists rather than collections of single values.

Args:
  expression (list, required)
  constant (number, required)
  format (symbol, required)
  table (symbol, required)
  (other) (symbol, required)

Inlets:
  0 (INLET_TYPE): Evaluate Expression, int $i1, float $f1, table $s1

Outlets:
  0 (OUTLET_TYPE): Expression Result

Messages: bang, int, float, list, anything

Attributes: maxsize, scalarmode
"""
vexpr = MaxObject("vexpr")

"""
vstscan - Audio Plugin Scanner

Vstscan provides an interface to audio plugin scanning so that this functionality may be presented in patchers and standalones. You can use vstscan to build menus of plugin names for use in patcher code.

Inlets:
  0 (symbol): scan, listvst, listvst3, listau

Outlets:
  0 (list): plug_vst/plug_vst3/plug_au &lt;pluginname&gt;
  1 (list): scan status (0/1)

Messages: cancel, listau, listvst, listvst3, scan, skip
"""
vstscan = MaxObject("vstscan")

"""
when - Report the current transport time

Outputs the current time of a transport in ticks and bars/beats/units.

Args:
  transport (symbol, required)

Inlets:
  0 (INLET_TYPE): bang Reports Current Transport Time

Outlets:
  0 (OUTLET_TYPE): Time in Bars/Beats/Units
  1 (OUTLET_TYPE): Time in Ticks

Messages: bang, int, float, list, anything

Attributes: transport
"""
when = MaxObject("when")

"""
xbendin - Interpret extra precision MIDI pitch bend values

Acts as a 14-bit pitch bend filter/processor and xbendin2 serves as a 2-byte pitch bend filter/processor.

Args:
  channel (int, optional)
  xbendin2 (symbol, optional)

Inlets:
  0 (INLET_TYPE): Raw MIDI Bytes From midiin

Outlets:
  0 (OUTLET_TYPE): MSB of Pitch Bend
  1 (OUTLET_TYPE): LSB of Pitch Bend

Messages: int
"""
xbendin = MaxObject("xbendin")

"""
xbendout - Format extra precision MIDI pitch bend messages

Formats messages which occupy both bytes of the MIDI pitch bend message. xbendout2 allows you to send the pitch bend data as two bytes from 0-127.

Args:
  xbendout2 (symbol, required)
  channel (int, optional)

Inlets:
  0 (INLET_TYPE): MSB of Pitch Bend
  1 (INLET_TYPE): LSB of Pitch Bend

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Bytes to midiout

Messages: bang, int, in1, in2, list
"""
xbendout = MaxObject("xbendout")

"""
xctlin - Output received 14-bit MIDI control values

Output the value from a specific 14-bit controller pair and MIDI channel.

Args:
  ctrllr-channel (list, optional)

Inlets:
  0 (INLET_TYPE): Raw MIDI Bytes From midiin

Outlets:
  0 (OUTLET_TYPE): Controller Value (14-bit)
  1 (OUTLET_TYPE): Controller Number (MSB)
  2 (OUTLET_TYPE): MIDI Channel

Messages: int, list, set

Attributes: lsbfirst
"""
xctlin = MaxObject("xctlin")

"""
xctlout - Format 14-bit MIDI controller messages

Format 14-bit MIDI continuous controller messages to be transmitted using the midiout object.

Args:
  ctrllr-channel (list, optional)

Inlets:
  0 (INLET_TYPE): Controller Value (0-16383 or list MSB LSB)
  1 (INLET_TYPE): Controller Number (MSB, 0-95)
  2 (INLET_TYPE): MIDI Channel

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Bytes to midiout

Messages: bang, int, float, list, in1, in2

Attributes: lsbfirst, running
"""
xctlout = MaxObject("xctlout")

"""
xmidiin - Output raw MIDI data

Listens to a specified MIDI port and output the raw MIDI data received.

Args:
  port (symbol, optional)
  device (symbol, optional)

Inlets:
  0 (INLET_TYPE): port Message Sets MIDI Input Port/Device
  1 (INLET_TYPE)

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Messages

Messages: anything, (mouse), (MIDI), lastport, port

Attributes: matchport, name
"""
xmidiin = MaxObject("xmidiin")

"""
xnotein - Interpret MIDI note messages with release velocity

Args:
  channel (int, optional)

Inlets:
  0 (INLET_TYPE): Raw MIDI Bytes From midiin

Outlets:
  0 (OUTLET_TYPE): Pitch
  1 (OUTLET_TYPE): Velocity
  2 (OUTLET_TYPE): 1 for Note-on, 0 for Note-off
  3 (OUTLET_TYPE): MIDI Channel

Messages: int
"""
xnotein = MaxObject("xnotein")

"""
xnoteout - Format MIDI note messages with release velocity

Args:
  channel (int, optional)

Inlets:
  0 (INLET_TYPE): Pitch
  1 (INLET_TYPE): Velocity
  2 (INLET_TYPE): 1 for Note-on, 0 for Note-off
  3 (INLET_TYPE): MIDI Channel

Outlets:
  0 (OUTLET_TYPE): Raw MIDI Bytes to midiout

Messages: bang, int, list, in1, in2, in3
"""
xnoteout = MaxObject("xnoteout")

"""
zl - Process lists in many ways

Performs several kinds of list processing functions. You set the function with an argument, and can change the function performed with the mode message. The behavior of the zl object's inlets and outlets vary according to the selected mode.

Args:
  mode (symbol, required)
  length (int, optional)
  function-int (int, optional)
  function-list (list, optional)

Inlets:
  0 (inactive): (inactive)
  1 (inactive): (inactive)

Outlets:
  0 (inactive): (inactive)
  1 (inactive): (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl = MaxObject("zl")

"""
zl.change - Filter out list repetitions

Args:
  initial-list (list, optional)

Inlets:
  0 (anything): list input
  1 (anything): set input list (without triggering)

Outlets:
  0 (anything): output list if it changed
  1 (int): 1 if the list changed, 0 otherwise

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_change = MaxObject("zl.change")

"""
zl.compare - Compare two lists

Args:
  initial-list (list, optional)

Inlets:
  0 (anything): left list input
  1 (anything): right list input

Outlets:
  0 (int): 1 when lists are equals, 0 otherwise
  1 (int/list): index(es) of the non equal item(s)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_compare = MaxObject("zl.compare")

"""
zl.delace - De-interleave a list

Splits the input list into two lists consisting of every other item. The left outlet will start with the first element, and the right outlet will start with the second element. If the input list is 6.2 3 5.6 5.3 3.8 2.4 the left output list is 6.2 5.6 3.8 and the right output list is 3 5.3 2.4).

Inlets:
  0 (anything): list input
  1 (inactive)

Outlets:
  0 (anything): deinterlaced left list
  1 (anything): deinterlaced right list

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_delace = MaxObject("zl.delace")

"""
zl.ecils - Slice a list in reverse order

Slices a list in reverse order: output last N out right outlet, the rest out the left. The right outlet is output first.

Args:
  size (int, optional)

Inlets:
  0 (anything): list to split into two slices
  1 (int): size of right slice

Outlets:
  0 (list): left slice of input list
  1 (list): right slice of input list

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_ecils = MaxObject("zl.ecils")

"""
zl.filter - Remove items in a list

Args:
  filter (list, optional)

Inlets:
  0 (anything): items to filter
  1 (anything): filtering items

Outlets:
  0 (anything): filtered items
  1 (list): indexes of filtered items

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_filter = MaxObject("zl.filter")

"""
zl.group - Store and output a list

Output a list after the number of items specified by the maximum output length are received.

Args:
  initial-size (int, optional)

Inlets:
  0 (anything): items to collect into a list
  1 (int): maximum output list length

Outlets:
  0 (list): collected items as list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_group = MaxObject("zl.group")

"""
zl.indexmap - Create new list from list of indexes

Creates a new list based on indices of an incoming list (0-based).

Args:
  initial-index (list, optional)

Inlets:
  0 (anything): list input
  1 (anything): indices list

Outlets:
  0 (anything): output list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_indexmap = MaxObject("zl.indexmap")

"""
zl.iter - Successively output lists of specific size

Outputs lists of specific sizes successively. The final list output may be shorter than the number, depending on the stored contents of the object.

Args:
  initial-size (int, optional)

Inlets:
  0 (anything): list to output in chunks
  1 (int): maximum size of output chunk

Outlets:
  0 (list): input list as consecutive chunks
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_iter = MaxObject("zl.iter")

"""
zl.join - Combine two lists

Combines two lists and outputs a single list.

Args:
  initial-list (list, optional)

Inlets:
  0 (anything): first segment of list
  1 (anything): second segment of list

Outlets:
  0 (list): both segments joined as one list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_join = MaxObject("zl.join")

"""
zl.lace - Interleave two lists

Interleaves two lists. If the left input list is 6.2 5.6 3.8 and the right input list is 3 5.3 2.4 the output list is 6.2 3 5.6 5.3 3.8 2.4

Args:
  initial-list (list, optional)

Inlets:
  0 (anything): left list input
  1 (anything): right list input

Outlets:
  0 (anything): interlaced list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_lace = MaxObject("zl.lace")

"""
zl.len - Get list length

Inlets:
  0 (anything): list to test
  1 (inactive)

Outlets:
  0 (int): length of list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_len = MaxObject("zl.len")

"""
zl.lookup - Output elements of a list

Outputs the elements of an input list. Element indexing begins at 0.

Args:
  initial-list (list, optional)

Inlets:
  0 (int/list): position (start at 0) in list to extract
  1 (anything): list from which to extract element

Outlets:
  0 (anything): extracted element(s) of list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_lookup = MaxObject("zl.lookup")

"""
zl.median - Get the median value of a list of numbers

Inlets:
  0 (list): list of numbers
  1 (inactive)

Outlets:
  0 (int/float): median value of the list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_median = MaxObject("zl.median")

"""
zl.mth - Extract item from list

Extract an item from an incoming list, based on the index specified (zero-based).

Args:
  initial-index (int, optional)
  index-sub (list, optional)

Inlets:
  0 (anything): list from which to extract mth element
  1 (int): position in list to extract

Outlets:
  0 (anything): mth element of list
  1 (list): source list with mth element removed

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_mth = MaxObject("zl.mth")

"""
zl.nth - Extract item from list

Extract an item from an incoming list, based on the index specified (starting at 1).

Args:
  initial-index (int, optional)
  index-sub (list, optional)

Inlets:
  0 (anything): list from which to extract nth element
  1 (int): position in list to extract

Outlets:
  0 (anything): nth element of list
  1 (list): source list with nth element removed

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_nth = MaxObject("zl.nth")

"""
zl.queue - Output elements of a list in the order they are received

Functions as a first-in-first-out (FIFO) stack. Each bang outputs the oldest message received and removes it from the list.

Inlets:
  0 (anything): items to add to queue
  1 (inactive)

Outlets:
  0 (anything): FIFO items on bang
  1 (int): number of items in queue

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_queue = MaxObject("zl.queue")

"""
zl.reg - Store and output a list

zl.reg functions as a register that holds a list. A list received in the left inlet is sent out the left outlet immediately. A list received in the right inlet is stored. A bang sends the stored list out the left outlet.

Args:
  initial-list (list, optional)

Inlets:
  0 (anything): list to store and output immediately
  1 (anything): list to store

Outlets:
  0 (list): stored list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_reg = MaxObject("zl.reg")

"""
zl.rev - Reverse a list

Inlets:
  0 (anything): list to reverse
  1 (inactive)

Outlets:
  0 (list): input list in reverse order
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_rev = MaxObject("zl.rev")

"""
zl.rot - Rotate a list

zl.rot is used to rotate the contents of a list. An argument is used to specify the number of places a list item is to be rotated - positive numbers rotate the list to the right, and negative numbers rotate left. This value can also be specified as an int input in the right inlet.

Args:
  distance (int, optional)

Inlets:
  0 (anything): list to rotate
  1 (int): rotate distance (positive=right, negative=left)

Outlets:
  0 (list): rotated input list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_rot = MaxObject("zl.rot")

"""
zl.scramble - Scramble a list

zl.scramble accepts a list in either inlet. A list in the left inlet will cause the object to immediately output a scrambled version of the input-list (a list containing the same elements as the input-list but in a randomized order). A list in the right inlet will set the input-list without causing output (a subsequent bang received in the left inlet will cause a randomized version of the right-input list to be sent out the object's left outlet). On receiving a bang or list in the left inlet, a list of the reordered indexes will be sent out the objects' right outlet.

Args:
  initial-list (list, optional)

Inlets:
  0 (anything): items to scramble
  1 (anything): set items to scramble

Outlets:
  0 (anything): scrambled items
  1 (list): reordered indexes list

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_scramble = MaxObject("zl.scramble")

"""
zl.sect - Find common items between two lists

zl.sect accepts a list in both inlets and sends a list out the left outlet that contains the elements that are common to both lists.

Args:
  initial-list (list, required)

Inlets:
  0 (anything): first list to intersect
  1 (anything): second list to intersect

Outlets:
  0 (list): list of items in common between both input lists
  1 (bang): bang if nothing is common between both input lists

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_sect = MaxObject("zl.sect")

"""
zl.slice - Slice a list in two

zl.slice is used to divide a list into two lists. This mode takes a number argument which specifies the size, in elements, of a list. This value can also be specified as an int to the right inlet. A list received in the left inlet will be split into two lists - the first list contains the number of items specified by the argument, and is sent out the left outlet. Any remaining list elements are sent out the right outlet of the object. Note: Lists are sent out the right outlet first.

Args:
  initial-size (int, optional)

Inlets:
  0 (anything): list to split into two slices
  1 (int): size of left slice

Outlets:
  0 (list): left slice of input list
  1 (list): right slice of input list

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_slice = MaxObject("zl.slice")

"""
zl.sort - Arrange a list in alphanumeric order

zl.sort is used to sort the contents of a list. An argument is used to specify the sorting order. An int of -1 sorts the input list in descending order, and any other value sorts the input list in ascending order. This value can also be specified as an input in the right inlet.

Args:
  order (int, optional)

Inlets:
  0 (anything): list to sort
  1 (int): sort direction

Outlets:
  0 (anything): sorted list
  1 (list): reordered indexes list

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_sort = MaxObject("zl.sort")

"""
zl.stack - Output elements of a list in reverse order

zl.stack functions as a last-in-first-out (LIFO) stack; a bang outputs the most recently received message and removes it from the currently held list.

Inlets:
  0 (anything): items to push onto stack
  1 (inactive)

Outlets:
  0 (anything): items popped from stack
  1 (int): number of items in stack

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_stack = MaxObject("zl.stack")

"""
zl.stream - Make a list of a certain size

zl.stream accepts a number in the right inlet which specifies the length of the output list. Following the receipt of this number, the object will collect this number of items input through the left inlet. After the list-length is complete, and with each subsequent input, the list will be output the left outlet. A 1 or a 0 will be output from the right outlet depending on whether the list-length has been reached or not. A 1 signifies that the list-length has been reached and that the object is now collecting the stream. Use the zlclear message to reset the list.

Args:
  initial-length (int, required)

Inlets:
  0 (anything): item to collect
  1 (int): defines output list length

Outlets:
  0 (anything): last 256 elements
  1 (int): 1 if the 256 elements are defined

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_stream = MaxObject("zl.stream")

"""
zl.sub - Output position for each occurance of right list in left

zl.sub accepts a list in both inlets and sends the output position (starting at 1) for each occurrence of right list in the left list out the left outlet.

Args:
  initial-list (list, required)

Inlets:
  0 (anything): list to search for sub-list
  1 (anything): sub-list to find

Outlets:
  0 (int): position of sub-list matches (if any)
  1 (int): number of sub-list matches (0 if none)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_sub = MaxObject("zl.sub")

"""
zl.sum - Sum a list of numbers

Returns the sum of a list of numbers.

Inlets:
  0 (list): list of numbers
  1 (inactive)

Outlets:
  0 (int/float): sum of the list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_sum = MaxObject("zl.sum")

"""
zl.swap - Swap two list indexes

zl.swap accepts a list as an argument. A list of two numbers (0-based) in the right inlet will indicate a pair of indices to swap in the list received in the left inlet. For instance, if 1 3 is received in the right inlet, A B C D received in the left inlet will cause the object to output A D C B from the left output.

Args:
  initial-swap (list, required)

Inlets:
  0 (anything): list input
  1 (anything): indices to swap

Outlets:
  0 (anything): output list
  1 (list): reordered indices list

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_swap = MaxObject("zl.swap")

"""
zl.thin - Remove duplicates from list

Removes all of the elements of the input list which are duplicates and outputs a new list.

Inlets:
  0 (anything): list input
  1 (inactive)

Outlets:
  0 (anything): output list with no duplicate
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_thin = MaxObject("zl.thin")

"""
zl.union - Combine two lists without duplicating shared items

zl.union accepts a list in both inlets and sends a list out the left outlet that contains the contents of both input lists. The output does not duplicate items in the right list that are in the left list.

Args:
  initial-list (list, required)

Inlets:
  0 (anything): first list to union
  1 (anything): second list to union

Outlets:
  0 (list): all items in both input lists, no duplicates
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_union = MaxObject("zl.union")

"""
zl.unique - Remove items from a list

zl.unique removes any items found in the left input that are present in the right input and outputs a new list.

Args:
  initial-list (list, required)

Inlets:
  0 (anything): left list input
  1 (anything): right list input

Outlets:
  0 (anything): list of items in the left list which are not in the right list
  1 (inactive)

Messages: bang, int, float, list, anything, mode, zlclear, zlreset

Attributes: fuzzy, zlmaxsize
"""
zl_unique = MaxObject("zl.unique")

"""
zmap - Maps input range of values to output range

Maps an input range of values and to an output range of values. Similar to scale, but clips values to the ranges, and does not allow inverted scaling.

Args:
  minimum-input (number, required)
  maximum-input (number, required)
  minimum-output (number, required)
  maximum-output (number, required)

Inlets:
  0 (INLET_TYPE): Input to be scaled
  1 (INLET_TYPE): Input Minimum
  2 (INLET_TYPE): Input Maximum
  3 (INLET_TYPE): Output Minimum
  4 (INLET_TYPE): Output Maximum

Outlets:
  0 (OUTLET_TYPE): Scaled Output

Messages: bang, int, float, list, ft1, ft2, ft3, ft4
"""
zmap = MaxObject("zmap")

_sys.stdout = _old_stdout
_devnull.close()
del _devnull, _old_stdout
